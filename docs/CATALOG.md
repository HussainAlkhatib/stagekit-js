# Stage Catalog

> Auto-generated by `npm run generate`. Do not edit by hand.

The full, authoritative list of every stage, in id order.

| id | name | description |
| --- | --- | --- |
| `mod-0001` | Replace "a" with "b" | Replaces every a with b. |
| `mod-0002` | Caesar shift +1 | Shifts latin letters forward by 1. |
| `mod-0003` | Prefix "-" | Prepends "-" to the value. |
| `mod-0004` | Suffix "-" | Appends "-" to the value. |
| `mod-0005` | Repeat x2 | Repeats the value 2 times. |
| `mod-0006` | Truncate to 1 | Keeps the first 1 characters. |
| `mod-0007` | Pad start to 4 with "-" | Left-pads the value to width 4 with "-". |
| `mod-0008` | Pad end to 4 with "-" | Right-pads the value to width 4 with "-". |
| `mod-0009` | Indent by 1 | Indents every line by 1 spaces. |
| `mod-0010` | Wrap in () | Wraps the value in (). |
| `mod-0011` | Mask all but last 1 | Masks all but the last 1 characters. |
| `mod-0012` | Slug with "-" | Builds a lowercase slug joined by "-". |
| `mod-0013` | Whitespace to "-" | Replaces whitespace runs with "-". |
| `mod-0014` | Uppercase | Converts the value to upper case. |
| `mod-0015` | Base64 encode | Encodes the value as base64. |
| `mod-0016` | Reverse characters | Reverses the order of characters. |
| `mod-0017` | Mirror | Appends the reversed value. |
| `mod-0018` | Replace "a" with "c" | Replaces every a with c. |
| `mod-0019` | Caesar shift +2 | Shifts latin letters forward by 2. |
| `mod-0020` | Prefix "_" | Prepends "_" to the value. |
| `mod-0021` | Suffix "_" | Appends "_" to the value. |
| `mod-0022` | Repeat x3 | Repeats the value 3 times. |
| `mod-0023` | Truncate to 2 | Keeps the first 2 characters. |
| `mod-0024` | Pad start to 4 with "_" | Left-pads the value to width 4 with "_". |
| `mod-0025` | Pad end to 4 with "_" | Right-pads the value to width 4 with "_". |
| `mod-0026` | Indent by 2 | Indents every line by 2 spaces. |
| `mod-0027` | Wrap in [] | Wraps the value in []. |
| `mod-0028` | Mask all but last 2 | Masks all but the last 2 characters. |
| `mod-0029` | Slug with "_" | Builds a lowercase slug joined by "_". |
| `mod-0030` | Whitespace to "_" | Replaces whitespace runs with "_". |
| `mod-0031` | Lowercase | Converts the value to lower case. |
| `mod-0032` | Base64 decode | Decodes a base64 string. |
| `mod-0033` | Reverse lines | Reverses the order of lines. |
| `mod-0034` | Interleave with "-" | Joins every character with "-". |
| `mod-0035` | Replace "a" with "d" | Replaces every a with d. |
| `mod-0036` | Caesar shift +3 | Shifts latin letters forward by 3. |
| `mod-0037` | Prefix "*" | Prepends "*" to the value. |
| `mod-0038` | Suffix "*" | Appends "*" to the value. |
| `mod-0039` | Repeat x4 | Repeats the value 4 times. |
| `mod-0040` | Truncate to 3 | Keeps the first 3 characters. |
| `mod-0041` | Pad start to 4 with "*" | Left-pads the value to width 4 with "*". |
| `mod-0042` | Pad end to 4 with "*" | Right-pads the value to width 4 with "*". |
| `mod-0043` | Indent by 3 | Indents every line by 3 spaces. |
| `mod-0044` | Wrap in {} | Wraps the value in {}. |
| `mod-0045` | Mask all but last 3 | Masks all but the last 3 characters. |
| `mod-0046` | Slug with "." | Builds a lowercase slug joined by ".". |
| `mod-0047` | Whitespace to "." | Replaces whitespace runs with ".". |
| `mod-0048` | Capitalize | Upper-cases the first character only. |
| `mod-0049` | Hex encode | Encodes the value as hexadecimal. |
| `mod-0050` | Sort lines | Sorts lines ascending. |
| `mod-0051` | Interleave with "_" | Joins every character with "_". |
| `mod-0052` | Replace "a" with "e" | Replaces every a with e. |
| `mod-0053` | Caesar shift +4 | Shifts latin letters forward by 4. |
| `mod-0054` | Prefix "#" | Prepends "#" to the value. |
| `mod-0055` | Suffix "#" | Appends "#" to the value. |
| `mod-0056` | Repeat x5 | Repeats the value 5 times. |
| `mod-0057` | Truncate to 4 | Keeps the first 4 characters. |
| `mod-0058` | Pad start to 4 with "#" | Left-pads the value to width 4 with "#". |
| `mod-0059` | Pad end to 4 with "#" | Right-pads the value to width 4 with "#". |
| `mod-0060` | Indent by 4 | Indents every line by 4 spaces. |
| `mod-0061` | Wrap in <> | Wraps the value in <>. |
| `mod-0062` | Mask all but last 4 | Masks all but the last 4 characters. |
| `mod-0063` | Slug with "~" | Builds a lowercase slug joined by "~". |
| `mod-0064` | Whitespace to "~" | Replaces whitespace runs with "~". |
| `mod-0065` | Uncapitalize | Lower-cases the first character only. |
| `mod-0066` | Hex decode | Decodes a hexadecimal string. |
| `mod-0067` | Unique lines | Removes duplicate lines. |
| `mod-0068` | Interleave with "*" | Joins every character with "*". |
| `mod-0069` | Replace "a" with "f" | Replaces every a with f. |
| `mod-0070` | Caesar shift +5 | Shifts latin letters forward by 5. |
| `mod-0071` | Prefix "+" | Prepends "+" to the value. |
| `mod-0072` | Suffix "+" | Appends "+" to the value. |
| `mod-0073` | Repeat x6 | Repeats the value 6 times. |
| `mod-0074` | Truncate to 5 | Keeps the first 5 characters. |
| `mod-0075` | Pad start to 4 with "+" | Left-pads the value to width 4 with "+". |
| `mod-0076` | Pad end to 4 with "+" | Right-pads the value to width 4 with "+". |
| `mod-0077` | Indent by 5 | Indents every line by 5 spaces. |
| `mod-0078` | Wrap in "" | Wraps the value in "". |
| `mod-0079` | Mask all but last 5 | Masks all but the last 5 characters. |
| `mod-0080` | Slug with "+" | Builds a lowercase slug joined by "+". |
| `mod-0081` | Whitespace to "+" | Replaces whitespace runs with "+". |
| `mod-0082` | Swap case | Swaps the case of every character. |
| `mod-0083` | Binary encode | Encodes each character as 8 bits. |
| `mod-0084` | Trim lines | Trims whitespace on every line. |
| `mod-0085` | Interleave with "#" | Joins every character with "#". |
| `mod-0086` | Replace "a" with "g" | Replaces every a with g. |
| `mod-0087` | Caesar shift +6 | Shifts latin letters forward by 6. |
| `mod-0088` | Prefix "." | Prepends "." to the value. |
| `mod-0089` | Suffix "." | Appends "." to the value. |
| `mod-0090` | Repeat x7 | Repeats the value 7 times. |
| `mod-0091` | Truncate to 6 | Keeps the first 6 characters. |
| `mod-0092` | Pad start to 4 with "." | Left-pads the value to width 4 with ".". |
| `mod-0093` | Pad end to 4 with "." | Right-pads the value to width 4 with ".". |
| `mod-0094` | Indent by 6 | Indents every line by 6 spaces. |
| `mod-0095` | Wrap in '' | Wraps the value in ''. |
| `mod-0096` | Mask all but last 6 | Masks all but the last 6 characters. |
| `mod-0097` | Slug with "*" | Builds a lowercase slug joined by "*". |
| `mod-0098` | Whitespace to "*" | Replaces whitespace runs with "*". |
| `mod-0099` | Camel case | Converts the value to camelCase. |
| `mod-0100` | URI encode | Percent-encodes the value. |
| `mod-0101` | Drop empty lines | Removes blank lines. |
| `mod-0102` | Interleave with "+" | Joins every character with "+". |
| `mod-0103` | Replace "a" with "h" | Replaces every a with h. |
| `mod-0104` | Caesar shift +7 | Shifts latin letters forward by 7. |
| `mod-0105` | Prefix "~" | Prepends "~" to the value. |
| `mod-0106` | Suffix "~" | Appends "~" to the value. |
| `mod-0107` | Repeat x8 | Repeats the value 8 times. |
| `mod-0108` | Truncate to 7 | Keeps the first 7 characters. |
| `mod-0109` | Pad start to 4 with "~" | Left-pads the value to width 4 with "~". |
| `mod-0110` | Pad end to 4 with "~" | Right-pads the value to width 4 with "~". |
| `mod-0111` | Indent by 7 | Indents every line by 7 spaces. |
| `mod-0112` | Wrap in \|\| | Wraps the value in \|\|. |
| `mod-0113` | Mask all but last 7 | Masks all but the last 7 characters. |
| `mod-0114` | Slug with "#" | Builds a lowercase slug joined by "#". |
| `mod-0115` | Whitespace to "#" | Replaces whitespace runs with "#". |
| `mod-0116` | Snake case | Converts the value to snake_case. |
| `mod-0117` | URI decode | Decodes a percent-encoded value. |
| `mod-0118` | Number lines | Numbers each line. |
| `mod-0119` | Interleave with "." | Joins every character with ".". |
| `mod-0120` | Replace "a" with "i" | Replaces every a with i. |
| `mod-0121` | Caesar shift +8 | Shifts latin letters forward by 8. |
| `mod-0122` | Prefix "=" | Prepends "=" to the value. |
| `mod-0123` | Suffix "=" | Appends "=" to the value. |
| `mod-0124` | Repeat x9 | Repeats the value 9 times. |
| `mod-0125` | Truncate to 8 | Keeps the first 8 characters. |
| `mod-0126` | Pad start to 4 with "=" | Left-pads the value to width 4 with "=". |
| `mod-0127` | Pad end to 4 with "=" | Right-pads the value to width 4 with "=". |
| `mod-0128` | Indent by 8 | Indents every line by 8 spaces. |
| `mod-0129` | Mask all but last 8 | Masks all but the last 8 characters. |
| `mod-0130` | Slug with "/" | Builds a lowercase slug joined by "/". |
| `mod-0131` | Whitespace to "/" | Replaces whitespace runs with "/". |
| `mod-0132` | Kebab case | Converts the value to kebab-case. |
| `mod-0133` | HTML escape | Escapes ampersands and angle brackets. |
| `mod-0134` | Bullet "* " | Prefixes each line with "* ". |
| `mod-0135` | Interleave with "~" | Joins every character with "~". |
| `mod-0136` | Replace "a" with "j" | Replaces every a with j. |
| `mod-0137` | Caesar shift +9 | Shifts latin letters forward by 9. |
| `mod-0138` | Prefix ">" | Prepends ">" to the value. |
| `mod-0139` | Suffix ">" | Appends ">" to the value. |
| `mod-0140` | Repeat x10 | Repeats the value 10 times. |
| `mod-0141` | Truncate to 9 | Keeps the first 9 characters. |
| `mod-0142` | Pad start to 5 with "-" | Left-pads the value to width 5 with "-". |
| `mod-0143` | Pad end to 5 with "-" | Right-pads the value to width 5 with "-". |
| `mod-0144` | Mask all but last 9 | Masks all but the last 9 characters. |
| `mod-0145` | Title case | Capitalizes the first letter of every word. |
| `mod-0146` | HTML unescape | Unescapes HTML entities. |
| `mod-0147` | Bullet "- " | Prefixes each line with "- ". |
| `mod-0148` | Interleave with "=" | Joins every character with "=". |
| `mod-0149` | Replace "a" with "k" | Replaces every a with k. |
| `mod-0150` | Caesar shift +10 | Shifts latin letters forward by 10. |
| `mod-0151` | Prefix "<" | Prepends "<" to the value. |
| `mod-0152` | Suffix "<" | Appends "<" to the value. |
| `mod-0153` | Truncate to 10 | Keeps the first 10 characters. |
| `mod-0154` | Pad start to 5 with "_" | Left-pads the value to width 5 with "_". |
| `mod-0155` | Pad end to 5 with "_" | Right-pads the value to width 5 with "_". |
| `mod-0156` | Mask all but last 10 | Masks all but the last 10 characters. |
| `mod-0157` | Atbash | Applies the Atbash cipher. |
| `mod-0158` | Bullet "+ " | Prefixes each line with "+ ". |
| `mod-0159` | Interleave with ">" | Joins every character with ">". |
| `mod-0160` | Replace "a" with "l" | Replaces every a with l. |
| `mod-0161` | Caesar shift +11 | Shifts latin letters forward by 11. |
| `mod-0162` | Prefix "!" | Prepends "!" to the value. |
| `mod-0163` | Suffix "!" | Appends "!" to the value. |
| `mod-0164` | Truncate to 11 | Keeps the first 11 characters. |
| `mod-0165` | Pad start to 5 with "*" | Left-pads the value to width 5 with "*". |
| `mod-0166` | Pad end to 5 with "*" | Right-pads the value to width 5 with "*". |
| `mod-0167` | Mask all but last 11 | Masks all but the last 11 characters. |
| `mod-0168` | Bullet "> " | Prefixes each line with "> ". |
| `mod-0169` | Interleave with "<" | Joins every character with "<". |
| `mod-0170` | Replace "a" with "m" | Replaces every a with m. |
| `mod-0171` | Caesar shift +12 | Shifts latin letters forward by 12. |
| `mod-0172` | Prefix "?" | Prepends "?" to the value. |
| `mod-0173` | Suffix "?" | Appends "?" to the value. |
| `mod-0174` | Truncate to 12 | Keeps the first 12 characters. |
| `mod-0175` | Pad start to 5 with "#" | Left-pads the value to width 5 with "#". |
| `mod-0176` | Pad end to 5 with "#" | Right-pads the value to width 5 with "#". |
| `mod-0177` | Mask all but last 12 | Masks all but the last 12 characters. |
| `mod-0178` | Bullet "~ " | Prefixes each line with "~ ". |
| `mod-0179` | Interleave with "!" | Joins every character with "!". |
| `mod-0180` | Replace "a" with "n" | Replaces every a with n. |
| `mod-0181` | Caesar shift +13 | Shifts latin letters forward by 13. |
| `mod-0182` | Prefix ":" | Prepends ":" to the value. |
| `mod-0183` | Suffix ":" | Appends ":" to the value. |
| `mod-0184` | Truncate to 13 | Keeps the first 13 characters. |
| `mod-0185` | Pad start to 5 with "+" | Left-pads the value to width 5 with "+". |
| `mod-0186` | Pad end to 5 with "+" | Right-pads the value to width 5 with "+". |
| `mod-0187` | Drop first 1 lines | Drops the first 1 lines. |
| `mod-0188` | Interleave with "?" | Joins every character with "?". |
| `mod-0189` | Replace "a" with "o" | Replaces every a with o. |
| `mod-0190` | Caesar shift +14 | Shifts latin letters forward by 14. |
| `mod-0191` | Prefix ";" | Prepends ";" to the value. |
| `mod-0192` | Suffix ";" | Appends ";" to the value. |
| `mod-0193` | Truncate to 14 | Keeps the first 14 characters. |
| `mod-0194` | Pad start to 5 with "." | Left-pads the value to width 5 with ".". |
| `mod-0195` | Pad end to 5 with "." | Right-pads the value to width 5 with ".". |
| `mod-0196` | Drop last 1 lines | Drops the last 1 lines. |
| `mod-0197` | Interleave with ":" | Joins every character with ":". |
| `mod-0198` | Replace "a" with "p" | Replaces every a with p. |
| `mod-0199` | Caesar shift +15 | Shifts latin letters forward by 15. |
| `mod-0200` | Prefix "\|" | Prepends "\|" to the value. |
| `mod-0201` | Suffix "\|" | Appends "\|" to the value. |
| `mod-0202` | Truncate to 15 | Keeps the first 15 characters. |
| `mod-0203` | Pad start to 5 with "~" | Left-pads the value to width 5 with "~". |
| `mod-0204` | Pad end to 5 with "~" | Right-pads the value to width 5 with "~". |
| `mod-0205` | Take first 1 lines | Keeps the first 1 lines. |
| `mod-0206` | Interleave with ";" | Joins every character with ";". |
| `mod-0207` | Replace "a" with "q" | Replaces every a with q. |
| `mod-0208` | Caesar shift +16 | Shifts latin letters forward by 16. |
| `mod-0209` | Prefix "/" | Prepends "/" to the value. |
| `mod-0210` | Suffix "/" | Appends "/" to the value. |
| `mod-0211` | Truncate to 16 | Keeps the first 16 characters. |
| `mod-0212` | Pad start to 5 with "=" | Left-pads the value to width 5 with "=". |
| `mod-0213` | Pad end to 5 with "=" | Right-pads the value to width 5 with "=". |
| `mod-0214` | Take last 1 lines | Keeps the last 1 lines. |
| `mod-0215` | Interleave with "\|" | Joins every character with "\|". |
| `mod-0216` | Replace "a" with "r" | Replaces every a with r. |
| `mod-0217` | Caesar shift +17 | Shifts latin letters forward by 17. |
| `mod-0218` | Prefix "@" | Prepends "@" to the value. |
| `mod-0219` | Suffix "@" | Appends "@" to the value. |
| `mod-0220` | Truncate to 17 | Keeps the first 17 characters. |
| `mod-0221` | Pad start to 6 with "-" | Left-pads the value to width 6 with "-". |
| `mod-0222` | Pad end to 6 with "-" | Right-pads the value to width 6 with "-". |
| `mod-0223` | Drop first 2 lines | Drops the first 2 lines. |
| `mod-0224` | Interleave with "/" | Joins every character with "/". |
| `mod-0225` | Replace "a" with "s" | Replaces every a with s. |
| `mod-0226` | Caesar shift +18 | Shifts latin letters forward by 18. |
| `mod-0227` | Prefix "$" | Prepends "$" to the value. |
| `mod-0228` | Suffix "$" | Appends "$" to the value. |
| `mod-0229` | Truncate to 18 | Keeps the first 18 characters. |
| `mod-0230` | Pad start to 6 with "_" | Left-pads the value to width 6 with "_". |
| `mod-0231` | Pad end to 6 with "_" | Right-pads the value to width 6 with "_". |
| `mod-0232` | Drop last 2 lines | Drops the last 2 lines. |
| `mod-0233` | Interleave with "@" | Joins every character with "@". |
| `mod-0234` | Replace "a" with "t" | Replaces every a with t. |
| `mod-0235` | Caesar shift +19 | Shifts latin letters forward by 19. |
| `mod-0236` | Prefix "%" | Prepends "%" to the value. |
| `mod-0237` | Suffix "%" | Appends "%" to the value. |
| `mod-0238` | Truncate to 19 | Keeps the first 19 characters. |
| `mod-0239` | Pad start to 6 with "*" | Left-pads the value to width 6 with "*". |
| `mod-0240` | Pad end to 6 with "*" | Right-pads the value to width 6 with "*". |
| `mod-0241` | Take first 2 lines | Keeps the first 2 lines. |
| `mod-0242` | Interleave with "$" | Joins every character with "$". |
| `mod-0243` | Replace "a" with "u" | Replaces every a with u. |
| `mod-0244` | Caesar shift +20 | Shifts latin letters forward by 20. |
| `mod-0245` | Prefix "^" | Prepends "^" to the value. |
| `mod-0246` | Suffix "^" | Appends "^" to the value. |
| `mod-0247` | Truncate to 20 | Keeps the first 20 characters. |
| `mod-0248` | Pad start to 6 with "#" | Left-pads the value to width 6 with "#". |
| `mod-0249` | Pad end to 6 with "#" | Right-pads the value to width 6 with "#". |
| `mod-0250` | Take last 2 lines | Keeps the last 2 lines. |
| `mod-0251` | Interleave with "%" | Joins every character with "%". |
| `mod-0252` | Replace "a" with "v" | Replaces every a with v. |
| `mod-0253` | Caesar shift +21 | Shifts latin letters forward by 21. |
| `mod-0254` | Prefix "&" | Prepends "&" to the value. |
| `mod-0255` | Suffix "&" | Appends "&" to the value. |
| `mod-0256` | Truncate to 21 | Keeps the first 21 characters. |
| `mod-0257` | Pad start to 6 with "+" | Left-pads the value to width 6 with "+". |
| `mod-0258` | Pad end to 6 with "+" | Right-pads the value to width 6 with "+". |
| `mod-0259` | Drop first 3 lines | Drops the first 3 lines. |
| `mod-0260` | Interleave with "^" | Joins every character with "^". |
| `mod-0261` | Replace "a" with "w" | Replaces every a with w. |
| `mod-0262` | Caesar shift +22 | Shifts latin letters forward by 22. |
| `mod-0263` | Truncate to 22 | Keeps the first 22 characters. |
| `mod-0264` | Pad start to 6 with "." | Left-pads the value to width 6 with ".". |
| `mod-0265` | Pad end to 6 with "." | Right-pads the value to width 6 with ".". |
| `mod-0266` | Drop last 3 lines | Drops the last 3 lines. |
| `mod-0267` | Interleave with "&" | Joins every character with "&". |
| `mod-0268` | Replace "a" with "x" | Replaces every a with x. |
| `mod-0269` | Caesar shift +23 | Shifts latin letters forward by 23. |
| `mod-0270` | Truncate to 23 | Keeps the first 23 characters. |
| `mod-0271` | Pad start to 6 with "~" | Left-pads the value to width 6 with "~". |
| `mod-0272` | Pad end to 6 with "~" | Right-pads the value to width 6 with "~". |
| `mod-0273` | Take first 3 lines | Keeps the first 3 lines. |
| `mod-0274` | Replace "a" with "y" | Replaces every a with y. |
| `mod-0275` | Caesar shift +24 | Shifts latin letters forward by 24. |
| `mod-0276` | Truncate to 24 | Keeps the first 24 characters. |
| `mod-0277` | Pad start to 6 with "=" | Left-pads the value to width 6 with "=". |
| `mod-0278` | Pad end to 6 with "=" | Right-pads the value to width 6 with "=". |
| `mod-0279` | Take last 3 lines | Keeps the last 3 lines. |
| `mod-0280` | Replace "a" with "z" | Replaces every a with z. |
| `mod-0281` | Caesar shift +25 | Shifts latin letters forward by 25. |
| `mod-0282` | Truncate to 25 | Keeps the first 25 characters. |
| `mod-0283` | Pad start to 7 with "-" | Left-pads the value to width 7 with "-". |
| `mod-0284` | Pad end to 7 with "-" | Right-pads the value to width 7 with "-". |
| `mod-0285` | Drop first 4 lines | Drops the first 4 lines. |
| `mod-0286` | Replace "a" with "A" | Replaces every a with A. |
| `mod-0287` | Truncate to 26 | Keeps the first 26 characters. |
| `mod-0288` | Pad start to 7 with "_" | Left-pads the value to width 7 with "_". |
| `mod-0289` | Pad end to 7 with "_" | Right-pads the value to width 7 with "_". |
| `mod-0290` | Drop last 4 lines | Drops the last 4 lines. |
| `mod-0291` | Replace "a" with "B" | Replaces every a with B. |
| `mod-0292` | Truncate to 27 | Keeps the first 27 characters. |
| `mod-0293` | Pad start to 7 with "*" | Left-pads the value to width 7 with "*". |
| `mod-0294` | Pad end to 7 with "*" | Right-pads the value to width 7 with "*". |
| `mod-0295` | Take first 4 lines | Keeps the first 4 lines. |
| `mod-0296` | Replace "a" with "C" | Replaces every a with C. |
| `mod-0297` | Truncate to 28 | Keeps the first 28 characters. |
| `mod-0298` | Pad start to 7 with "#" | Left-pads the value to width 7 with "#". |
| `mod-0299` | Pad end to 7 with "#" | Right-pads the value to width 7 with "#". |
| `mod-0300` | Take last 4 lines | Keeps the last 4 lines. |
| `mod-0301` | Replace "a" with "D" | Replaces every a with D. |
| `mod-0302` | Truncate to 29 | Keeps the first 29 characters. |
| `mod-0303` | Pad start to 7 with "+" | Left-pads the value to width 7 with "+". |
| `mod-0304` | Pad end to 7 with "+" | Right-pads the value to width 7 with "+". |
| `mod-0305` | Drop first 5 lines | Drops the first 5 lines. |
| `mod-0306` | Replace "a" with "E" | Replaces every a with E. |
| `mod-0307` | Truncate to 30 | Keeps the first 30 characters. |
| `mod-0308` | Pad start to 7 with "." | Left-pads the value to width 7 with ".". |
| `mod-0309` | Pad end to 7 with "." | Right-pads the value to width 7 with ".". |
| `mod-0310` | Drop last 5 lines | Drops the last 5 lines. |
| `mod-0311` | Replace "a" with "F" | Replaces every a with F. |
| `mod-0312` | Truncate to 31 | Keeps the first 31 characters. |
| `mod-0313` | Pad start to 7 with "~" | Left-pads the value to width 7 with "~". |
| `mod-0314` | Pad end to 7 with "~" | Right-pads the value to width 7 with "~". |
| `mod-0315` | Take first 5 lines | Keeps the first 5 lines. |
| `mod-0316` | Replace "a" with "G" | Replaces every a with G. |
| `mod-0317` | Truncate to 32 | Keeps the first 32 characters. |
| `mod-0318` | Pad start to 7 with "=" | Left-pads the value to width 7 with "=". |
| `mod-0319` | Pad end to 7 with "=" | Right-pads the value to width 7 with "=". |
| `mod-0320` | Take last 5 lines | Keeps the last 5 lines. |
| `mod-0321` | Replace "a" with "H" | Replaces every a with H. |
| `mod-0322` | Truncate to 33 | Keeps the first 33 characters. |
| `mod-0323` | Pad start to 8 with "-" | Left-pads the value to width 8 with "-". |
| `mod-0324` | Pad end to 8 with "-" | Right-pads the value to width 8 with "-". |
| `mod-0325` | Drop first 6 lines | Drops the first 6 lines. |
| `mod-0326` | Replace "a" with "I" | Replaces every a with I. |
| `mod-0327` | Truncate to 34 | Keeps the first 34 characters. |
| `mod-0328` | Pad start to 8 with "_" | Left-pads the value to width 8 with "_". |
| `mod-0329` | Pad end to 8 with "_" | Right-pads the value to width 8 with "_". |
| `mod-0330` | Drop last 6 lines | Drops the last 6 lines. |
| `mod-0331` | Replace "a" with "J" | Replaces every a with J. |
| `mod-0332` | Truncate to 35 | Keeps the first 35 characters. |
| `mod-0333` | Pad start to 8 with "*" | Left-pads the value to width 8 with "*". |
| `mod-0334` | Pad end to 8 with "*" | Right-pads the value to width 8 with "*". |
| `mod-0335` | Take first 6 lines | Keeps the first 6 lines. |
| `mod-0336` | Replace "a" with "K" | Replaces every a with K. |
| `mod-0337` | Truncate to 36 | Keeps the first 36 characters. |
| `mod-0338` | Pad start to 8 with "#" | Left-pads the value to width 8 with "#". |
| `mod-0339` | Pad end to 8 with "#" | Right-pads the value to width 8 with "#". |
| `mod-0340` | Take last 6 lines | Keeps the last 6 lines. |
| `mod-0341` | Replace "a" with "L" | Replaces every a with L. |
| `mod-0342` | Truncate to 37 | Keeps the first 37 characters. |
| `mod-0343` | Pad start to 8 with "+" | Left-pads the value to width 8 with "+". |
| `mod-0344` | Pad end to 8 with "+" | Right-pads the value to width 8 with "+". |
| `mod-0345` | Drop first 7 lines | Drops the first 7 lines. |
| `mod-0346` | Replace "a" with "M" | Replaces every a with M. |
| `mod-0347` | Truncate to 38 | Keeps the first 38 characters. |
| `mod-0348` | Pad start to 8 with "." | Left-pads the value to width 8 with ".". |
| `mod-0349` | Pad end to 8 with "." | Right-pads the value to width 8 with ".". |
| `mod-0350` | Drop last 7 lines | Drops the last 7 lines. |
| `mod-0351` | Replace "a" with "N" | Replaces every a with N. |
| `mod-0352` | Truncate to 39 | Keeps the first 39 characters. |
| `mod-0353` | Pad start to 8 with "~" | Left-pads the value to width 8 with "~". |
| `mod-0354` | Pad end to 8 with "~" | Right-pads the value to width 8 with "~". |
| `mod-0355` | Take first 7 lines | Keeps the first 7 lines. |
| `mod-0356` | Replace "a" with "O" | Replaces every a with O. |
| `mod-0357` | Truncate to 40 | Keeps the first 40 characters. |
| `mod-0358` | Pad start to 8 with "=" | Left-pads the value to width 8 with "=". |
| `mod-0359` | Pad end to 8 with "=" | Right-pads the value to width 8 with "=". |
| `mod-0360` | Take last 7 lines | Keeps the last 7 lines. |
| `mod-0361` | Replace "a" with "P" | Replaces every a with P. |
| `mod-0362` | Truncate to 41 | Keeps the first 41 characters. |
| `mod-0363` | Pad start to 9 with "-" | Left-pads the value to width 9 with "-". |
| `mod-0364` | Pad end to 9 with "-" | Right-pads the value to width 9 with "-". |
| `mod-0365` | Drop first 8 lines | Drops the first 8 lines. |
| `mod-0366` | Replace "a" with "Q" | Replaces every a with Q. |
| `mod-0367` | Truncate to 42 | Keeps the first 42 characters. |
| `mod-0368` | Pad start to 9 with "_" | Left-pads the value to width 9 with "_". |
| `mod-0369` | Pad end to 9 with "_" | Right-pads the value to width 9 with "_". |
| `mod-0370` | Drop last 8 lines | Drops the last 8 lines. |
| `mod-0371` | Replace "a" with "R" | Replaces every a with R. |
| `mod-0372` | Truncate to 43 | Keeps the first 43 characters. |
| `mod-0373` | Pad start to 9 with "*" | Left-pads the value to width 9 with "*". |
| `mod-0374` | Pad end to 9 with "*" | Right-pads the value to width 9 with "*". |
| `mod-0375` | Take first 8 lines | Keeps the first 8 lines. |
| `mod-0376` | Replace "a" with "S" | Replaces every a with S. |
| `mod-0377` | Truncate to 44 | Keeps the first 44 characters. |
| `mod-0378` | Pad start to 9 with "#" | Left-pads the value to width 9 with "#". |
| `mod-0379` | Pad end to 9 with "#" | Right-pads the value to width 9 with "#". |
| `mod-0380` | Take last 8 lines | Keeps the last 8 lines. |
| `mod-0381` | Replace "a" with "T" | Replaces every a with T. |
| `mod-0382` | Truncate to 45 | Keeps the first 45 characters. |
| `mod-0383` | Pad start to 9 with "+" | Left-pads the value to width 9 with "+". |
| `mod-0384` | Pad end to 9 with "+" | Right-pads the value to width 9 with "+". |
| `mod-0385` | Replace "a" with "U" | Replaces every a with U. |
| `mod-0386` | Truncate to 46 | Keeps the first 46 characters. |
| `mod-0387` | Pad start to 9 with "." | Left-pads the value to width 9 with ".". |
| `mod-0388` | Pad end to 9 with "." | Right-pads the value to width 9 with ".". |
| `mod-0389` | Replace "a" with "V" | Replaces every a with V. |
| `mod-0390` | Truncate to 47 | Keeps the first 47 characters. |
| `mod-0391` | Pad start to 9 with "~" | Left-pads the value to width 9 with "~". |
| `mod-0392` | Pad end to 9 with "~" | Right-pads the value to width 9 with "~". |
| `mod-0393` | Replace "a" with "W" | Replaces every a with W. |
| `mod-0394` | Truncate to 48 | Keeps the first 48 characters. |
| `mod-0395` | Pad start to 9 with "=" | Left-pads the value to width 9 with "=". |
| `mod-0396` | Pad end to 9 with "=" | Right-pads the value to width 9 with "=". |
| `mod-0397` | Replace "a" with "X" | Replaces every a with X. |
| `mod-0398` | Truncate to 49 | Keeps the first 49 characters. |
| `mod-0399` | Pad start to 10 with "-" | Left-pads the value to width 10 with "-". |
| `mod-0400` | Pad end to 10 with "-" | Right-pads the value to width 10 with "-". |
| `mod-0401` | Replace "a" with "Y" | Replaces every a with Y. |
| `mod-0402` | Truncate to 50 | Keeps the first 50 characters. |
| `mod-0403` | Pad start to 10 with "_" | Left-pads the value to width 10 with "_". |
| `mod-0404` | Pad end to 10 with "_" | Right-pads the value to width 10 with "_". |
| `mod-0405` | Replace "a" with "Z" | Replaces every a with Z. |
| `mod-0406` | Truncate to 51 | Keeps the first 51 characters. |
| `mod-0407` | Pad start to 10 with "*" | Left-pads the value to width 10 with "*". |
| `mod-0408` | Pad end to 10 with "*" | Right-pads the value to width 10 with "*". |
| `mod-0409` | Replace "a" with "0" | Replaces every a with 0. |
| `mod-0410` | Truncate to 52 | Keeps the first 52 characters. |
| `mod-0411` | Pad start to 10 with "#" | Left-pads the value to width 10 with "#". |
| `mod-0412` | Pad end to 10 with "#" | Right-pads the value to width 10 with "#". |
| `mod-0413` | Replace "a" with "1" | Replaces every a with 1. |
| `mod-0414` | Truncate to 53 | Keeps the first 53 characters. |
| `mod-0415` | Pad start to 10 with "+" | Left-pads the value to width 10 with "+". |
| `mod-0416` | Pad end to 10 with "+" | Right-pads the value to width 10 with "+". |
| `mod-0417` | Replace "a" with "2" | Replaces every a with 2. |
| `mod-0418` | Truncate to 54 | Keeps the first 54 characters. |
| `mod-0419` | Pad start to 10 with "." | Left-pads the value to width 10 with ".". |
| `mod-0420` | Pad end to 10 with "." | Right-pads the value to width 10 with ".". |
| `mod-0421` | Replace "a" with "3" | Replaces every a with 3. |
| `mod-0422` | Truncate to 55 | Keeps the first 55 characters. |
| `mod-0423` | Pad start to 10 with "~" | Left-pads the value to width 10 with "~". |
| `mod-0424` | Pad end to 10 with "~" | Right-pads the value to width 10 with "~". |
| `mod-0425` | Replace "a" with "4" | Replaces every a with 4. |
| `mod-0426` | Truncate to 56 | Keeps the first 56 characters. |
| `mod-0427` | Pad start to 10 with "=" | Left-pads the value to width 10 with "=". |
| `mod-0428` | Pad end to 10 with "=" | Right-pads the value to width 10 with "=". |
| `mod-0429` | Replace "a" with "5" | Replaces every a with 5. |
| `mod-0430` | Truncate to 57 | Keeps the first 57 characters. |
| `mod-0431` | Pad start to 11 with "-" | Left-pads the value to width 11 with "-". |
| `mod-0432` | Pad end to 11 with "-" | Right-pads the value to width 11 with "-". |
| `mod-0433` | Replace "a" with "6" | Replaces every a with 6. |
| `mod-0434` | Truncate to 58 | Keeps the first 58 characters. |
| `mod-0435` | Pad start to 11 with "_" | Left-pads the value to width 11 with "_". |
| `mod-0436` | Pad end to 11 with "_" | Right-pads the value to width 11 with "_". |
| `mod-0437` | Replace "a" with "7" | Replaces every a with 7. |
| `mod-0438` | Truncate to 59 | Keeps the first 59 characters. |
| `mod-0439` | Pad start to 11 with "*" | Left-pads the value to width 11 with "*". |
| `mod-0440` | Pad end to 11 with "*" | Right-pads the value to width 11 with "*". |
| `mod-0441` | Replace "a" with "8" | Replaces every a with 8. |
| `mod-0442` | Truncate to 60 | Keeps the first 60 characters. |
| `mod-0443` | Pad start to 11 with "#" | Left-pads the value to width 11 with "#". |
| `mod-0444` | Pad end to 11 with "#" | Right-pads the value to width 11 with "#". |
| `mod-0445` | Replace "a" with "9" | Replaces every a with 9. |
| `mod-0446` | Truncate to 61 | Keeps the first 61 characters. |
| `mod-0447` | Pad start to 11 with "+" | Left-pads the value to width 11 with "+". |
| `mod-0448` | Pad end to 11 with "+" | Right-pads the value to width 11 with "+". |
| `mod-0449` | Replace "b" with "a" | Replaces every b with a. |
| `mod-0450` | Truncate to 62 | Keeps the first 62 characters. |
| `mod-0451` | Pad start to 11 with "." | Left-pads the value to width 11 with ".". |
| `mod-0452` | Pad end to 11 with "." | Right-pads the value to width 11 with ".". |
| `mod-0453` | Replace "b" with "c" | Replaces every b with c. |
| `mod-0454` | Truncate to 63 | Keeps the first 63 characters. |
| `mod-0455` | Pad start to 11 with "~" | Left-pads the value to width 11 with "~". |
| `mod-0456` | Pad end to 11 with "~" | Right-pads the value to width 11 with "~". |
| `mod-0457` | Replace "b" with "d" | Replaces every b with d. |
| `mod-0458` | Truncate to 64 | Keeps the first 64 characters. |
| `mod-0459` | Pad start to 11 with "=" | Left-pads the value to width 11 with "=". |
| `mod-0460` | Pad end to 11 with "=" | Right-pads the value to width 11 with "=". |
| `mod-0461` | Replace "b" with "e" | Replaces every b with e. |
| `mod-0462` | Truncate to 65 | Keeps the first 65 characters. |
| `mod-0463` | Pad start to 12 with "-" | Left-pads the value to width 12 with "-". |
| `mod-0464` | Pad end to 12 with "-" | Right-pads the value to width 12 with "-". |
| `mod-0465` | Replace "b" with "f" | Replaces every b with f. |
| `mod-0466` | Truncate to 66 | Keeps the first 66 characters. |
| `mod-0467` | Pad start to 12 with "_" | Left-pads the value to width 12 with "_". |
| `mod-0468` | Pad end to 12 with "_" | Right-pads the value to width 12 with "_". |
| `mod-0469` | Replace "b" with "g" | Replaces every b with g. |
| `mod-0470` | Truncate to 67 | Keeps the first 67 characters. |
| `mod-0471` | Pad start to 12 with "*" | Left-pads the value to width 12 with "*". |
| `mod-0472` | Pad end to 12 with "*" | Right-pads the value to width 12 with "*". |
| `mod-0473` | Replace "b" with "h" | Replaces every b with h. |
| `mod-0474` | Truncate to 68 | Keeps the first 68 characters. |
| `mod-0475` | Pad start to 12 with "#" | Left-pads the value to width 12 with "#". |
| `mod-0476` | Pad end to 12 with "#" | Right-pads the value to width 12 with "#". |
| `mod-0477` | Replace "b" with "i" | Replaces every b with i. |
| `mod-0478` | Truncate to 69 | Keeps the first 69 characters. |
| `mod-0479` | Pad start to 12 with "+" | Left-pads the value to width 12 with "+". |
| `mod-0480` | Pad end to 12 with "+" | Right-pads the value to width 12 with "+". |
| `mod-0481` | Replace "b" with "j" | Replaces every b with j. |
| `mod-0482` | Truncate to 70 | Keeps the first 70 characters. |
| `mod-0483` | Pad start to 12 with "." | Left-pads the value to width 12 with ".". |
| `mod-0484` | Pad end to 12 with "." | Right-pads the value to width 12 with ".". |
| `mod-0485` | Replace "b" with "k" | Replaces every b with k. |
| `mod-0486` | Truncate to 71 | Keeps the first 71 characters. |
| `mod-0487` | Pad start to 12 with "~" | Left-pads the value to width 12 with "~". |
| `mod-0488` | Pad end to 12 with "~" | Right-pads the value to width 12 with "~". |
| `mod-0489` | Replace "b" with "l" | Replaces every b with l. |
| `mod-0490` | Truncate to 72 | Keeps the first 72 characters. |
| `mod-0491` | Pad start to 12 with "=" | Left-pads the value to width 12 with "=". |
| `mod-0492` | Pad end to 12 with "=" | Right-pads the value to width 12 with "=". |
| `mod-0493` | Replace "b" with "m" | Replaces every b with m. |
| `mod-0494` | Truncate to 73 | Keeps the first 73 characters. |
| `mod-0495` | Pad start to 13 with "-" | Left-pads the value to width 13 with "-". |
| `mod-0496` | Pad end to 13 with "-" | Right-pads the value to width 13 with "-". |
| `mod-0497` | Replace "b" with "n" | Replaces every b with n. |
| `mod-0498` | Truncate to 74 | Keeps the first 74 characters. |
| `mod-0499` | Pad start to 13 with "_" | Left-pads the value to width 13 with "_". |
| `mod-0500` | Pad end to 13 with "_" | Right-pads the value to width 13 with "_". |
| `mod-0501` | Replace "b" with "o" | Replaces every b with o. |
| `mod-0502` | Truncate to 75 | Keeps the first 75 characters. |
| `mod-0503` | Pad start to 13 with "*" | Left-pads the value to width 13 with "*". |
| `mod-0504` | Pad end to 13 with "*" | Right-pads the value to width 13 with "*". |
| `mod-0505` | Replace "b" with "p" | Replaces every b with p. |
| `mod-0506` | Truncate to 76 | Keeps the first 76 characters. |
| `mod-0507` | Pad start to 13 with "#" | Left-pads the value to width 13 with "#". |
| `mod-0508` | Pad end to 13 with "#" | Right-pads the value to width 13 with "#". |
| `mod-0509` | Replace "b" with "q" | Replaces every b with q. |
| `mod-0510` | Truncate to 77 | Keeps the first 77 characters. |
| `mod-0511` | Pad start to 13 with "+" | Left-pads the value to width 13 with "+". |
| `mod-0512` | Pad end to 13 with "+" | Right-pads the value to width 13 with "+". |
| `mod-0513` | Replace "b" with "r" | Replaces every b with r. |
| `mod-0514` | Truncate to 78 | Keeps the first 78 characters. |
| `mod-0515` | Pad start to 13 with "." | Left-pads the value to width 13 with ".". |
| `mod-0516` | Pad end to 13 with "." | Right-pads the value to width 13 with ".". |
| `mod-0517` | Replace "b" with "s" | Replaces every b with s. |
| `mod-0518` | Truncate to 79 | Keeps the first 79 characters. |
| `mod-0519` | Pad start to 13 with "~" | Left-pads the value to width 13 with "~". |
| `mod-0520` | Pad end to 13 with "~" | Right-pads the value to width 13 with "~". |
| `mod-0521` | Replace "b" with "t" | Replaces every b with t. |
| `mod-0522` | Truncate to 80 | Keeps the first 80 characters. |
| `mod-0523` | Pad start to 13 with "=" | Left-pads the value to width 13 with "=". |
| `mod-0524` | Pad end to 13 with "=" | Right-pads the value to width 13 with "=". |
| `mod-0525` | Replace "b" with "u" | Replaces every b with u. |
| `mod-0526` | Pad start to 14 with "-" | Left-pads the value to width 14 with "-". |
| `mod-0527` | Pad end to 14 with "-" | Right-pads the value to width 14 with "-". |
| `mod-0528` | Replace "b" with "v" | Replaces every b with v. |
| `mod-0529` | Pad start to 14 with "_" | Left-pads the value to width 14 with "_". |
| `mod-0530` | Pad end to 14 with "_" | Right-pads the value to width 14 with "_". |
| `mod-0531` | Replace "b" with "w" | Replaces every b with w. |
| `mod-0532` | Pad start to 14 with "*" | Left-pads the value to width 14 with "*". |
| `mod-0533` | Pad end to 14 with "*" | Right-pads the value to width 14 with "*". |
| `mod-0534` | Replace "b" with "x" | Replaces every b with x. |
| `mod-0535` | Pad start to 14 with "#" | Left-pads the value to width 14 with "#". |
| `mod-0536` | Pad end to 14 with "#" | Right-pads the value to width 14 with "#". |
| `mod-0537` | Replace "b" with "y" | Replaces every b with y. |
| `mod-0538` | Pad start to 14 with "+" | Left-pads the value to width 14 with "+". |
| `mod-0539` | Pad end to 14 with "+" | Right-pads the value to width 14 with "+". |
| `mod-0540` | Replace "b" with "z" | Replaces every b with z. |
| `mod-0541` | Pad start to 14 with "." | Left-pads the value to width 14 with ".". |
| `mod-0542` | Pad end to 14 with "." | Right-pads the value to width 14 with ".". |
| `mod-0543` | Replace "b" with "A" | Replaces every b with A. |
| `mod-0544` | Pad start to 14 with "~" | Left-pads the value to width 14 with "~". |
| `mod-0545` | Pad end to 14 with "~" | Right-pads the value to width 14 with "~". |
| `mod-0546` | Replace "b" with "B" | Replaces every b with B. |
| `mod-0547` | Pad start to 14 with "=" | Left-pads the value to width 14 with "=". |
| `mod-0548` | Pad end to 14 with "=" | Right-pads the value to width 14 with "=". |
| `mod-0549` | Replace "b" with "C" | Replaces every b with C. |
| `mod-0550` | Pad start to 15 with "-" | Left-pads the value to width 15 with "-". |
| `mod-0551` | Pad end to 15 with "-" | Right-pads the value to width 15 with "-". |
| `mod-0552` | Replace "b" with "D" | Replaces every b with D. |
| `mod-0553` | Pad start to 15 with "_" | Left-pads the value to width 15 with "_". |
| `mod-0554` | Pad end to 15 with "_" | Right-pads the value to width 15 with "_". |
| `mod-0555` | Replace "b" with "E" | Replaces every b with E. |
| `mod-0556` | Pad start to 15 with "*" | Left-pads the value to width 15 with "*". |
| `mod-0557` | Pad end to 15 with "*" | Right-pads the value to width 15 with "*". |
| `mod-0558` | Replace "b" with "F" | Replaces every b with F. |
| `mod-0559` | Pad start to 15 with "#" | Left-pads the value to width 15 with "#". |
| `mod-0560` | Pad end to 15 with "#" | Right-pads the value to width 15 with "#". |
| `mod-0561` | Replace "b" with "G" | Replaces every b with G. |
| `mod-0562` | Pad start to 15 with "+" | Left-pads the value to width 15 with "+". |
| `mod-0563` | Pad end to 15 with "+" | Right-pads the value to width 15 with "+". |
| `mod-0564` | Replace "b" with "H" | Replaces every b with H. |
| `mod-0565` | Pad start to 15 with "." | Left-pads the value to width 15 with ".". |
| `mod-0566` | Pad end to 15 with "." | Right-pads the value to width 15 with ".". |
| `mod-0567` | Replace "b" with "I" | Replaces every b with I. |
| `mod-0568` | Pad start to 15 with "~" | Left-pads the value to width 15 with "~". |
| `mod-0569` | Pad end to 15 with "~" | Right-pads the value to width 15 with "~". |
| `mod-0570` | Replace "b" with "J" | Replaces every b with J. |
| `mod-0571` | Pad start to 15 with "=" | Left-pads the value to width 15 with "=". |
| `mod-0572` | Pad end to 15 with "=" | Right-pads the value to width 15 with "=". |
| `mod-0573` | Replace "b" with "K" | Replaces every b with K. |
| `mod-0574` | Pad start to 16 with "-" | Left-pads the value to width 16 with "-". |
| `mod-0575` | Pad end to 16 with "-" | Right-pads the value to width 16 with "-". |
| `mod-0576` | Replace "b" with "L" | Replaces every b with L. |
| `mod-0577` | Pad start to 16 with "_" | Left-pads the value to width 16 with "_". |
| `mod-0578` | Pad end to 16 with "_" | Right-pads the value to width 16 with "_". |
| `mod-0579` | Replace "b" with "M" | Replaces every b with M. |
| `mod-0580` | Pad start to 16 with "*" | Left-pads the value to width 16 with "*". |
| `mod-0581` | Pad end to 16 with "*" | Right-pads the value to width 16 with "*". |
| `mod-0582` | Replace "b" with "N" | Replaces every b with N. |
| `mod-0583` | Pad start to 16 with "#" | Left-pads the value to width 16 with "#". |
| `mod-0584` | Pad end to 16 with "#" | Right-pads the value to width 16 with "#". |
| `mod-0585` | Replace "b" with "O" | Replaces every b with O. |
| `mod-0586` | Pad start to 16 with "+" | Left-pads the value to width 16 with "+". |
| `mod-0587` | Pad end to 16 with "+" | Right-pads the value to width 16 with "+". |
| `mod-0588` | Replace "b" with "P" | Replaces every b with P. |
| `mod-0589` | Pad start to 16 with "." | Left-pads the value to width 16 with ".". |
| `mod-0590` | Pad end to 16 with "." | Right-pads the value to width 16 with ".". |
| `mod-0591` | Replace "b" with "Q" | Replaces every b with Q. |
| `mod-0592` | Pad start to 16 with "~" | Left-pads the value to width 16 with "~". |
| `mod-0593` | Pad end to 16 with "~" | Right-pads the value to width 16 with "~". |
| `mod-0594` | Replace "b" with "R" | Replaces every b with R. |
| `mod-0595` | Pad start to 16 with "=" | Left-pads the value to width 16 with "=". |
| `mod-0596` | Pad end to 16 with "=" | Right-pads the value to width 16 with "=". |
| `mod-0597` | Replace "b" with "S" | Replaces every b with S. |
| `mod-0598` | Pad start to 17 with "-" | Left-pads the value to width 17 with "-". |
| `mod-0599` | Pad end to 17 with "-" | Right-pads the value to width 17 with "-". |
| `mod-0600` | Replace "b" with "T" | Replaces every b with T. |
| `mod-0601` | Pad start to 17 with "_" | Left-pads the value to width 17 with "_". |
| `mod-0602` | Pad end to 17 with "_" | Right-pads the value to width 17 with "_". |
| `mod-0603` | Replace "b" with "U" | Replaces every b with U. |
| `mod-0604` | Pad start to 17 with "*" | Left-pads the value to width 17 with "*". |
| `mod-0605` | Pad end to 17 with "*" | Right-pads the value to width 17 with "*". |
| `mod-0606` | Replace "b" with "V" | Replaces every b with V. |
| `mod-0607` | Pad start to 17 with "#" | Left-pads the value to width 17 with "#". |
| `mod-0608` | Pad end to 17 with "#" | Right-pads the value to width 17 with "#". |
| `mod-0609` | Replace "b" with "W" | Replaces every b with W. |
| `mod-0610` | Pad start to 17 with "+" | Left-pads the value to width 17 with "+". |
| `mod-0611` | Pad end to 17 with "+" | Right-pads the value to width 17 with "+". |
| `mod-0612` | Replace "b" with "X" | Replaces every b with X. |
| `mod-0613` | Pad start to 17 with "." | Left-pads the value to width 17 with ".". |
| `mod-0614` | Pad end to 17 with "." | Right-pads the value to width 17 with ".". |
| `mod-0615` | Replace "b" with "Y" | Replaces every b with Y. |
| `mod-0616` | Pad start to 17 with "~" | Left-pads the value to width 17 with "~". |
| `mod-0617` | Pad end to 17 with "~" | Right-pads the value to width 17 with "~". |
| `mod-0618` | Replace "b" with "Z" | Replaces every b with Z. |
| `mod-0619` | Pad start to 17 with "=" | Left-pads the value to width 17 with "=". |
| `mod-0620` | Pad end to 17 with "=" | Right-pads the value to width 17 with "=". |
| `mod-0621` | Replace "b" with "0" | Replaces every b with 0. |
| `mod-0622` | Pad start to 18 with "-" | Left-pads the value to width 18 with "-". |
| `mod-0623` | Pad end to 18 with "-" | Right-pads the value to width 18 with "-". |
| `mod-0624` | Replace "b" with "1" | Replaces every b with 1. |
| `mod-0625` | Pad start to 18 with "_" | Left-pads the value to width 18 with "_". |
| `mod-0626` | Pad end to 18 with "_" | Right-pads the value to width 18 with "_". |
| `mod-0627` | Replace "b" with "2" | Replaces every b with 2. |
| `mod-0628` | Pad start to 18 with "*" | Left-pads the value to width 18 with "*". |
| `mod-0629` | Pad end to 18 with "*" | Right-pads the value to width 18 with "*". |
| `mod-0630` | Replace "b" with "3" | Replaces every b with 3. |
| `mod-0631` | Pad start to 18 with "#" | Left-pads the value to width 18 with "#". |
| `mod-0632` | Pad end to 18 with "#" | Right-pads the value to width 18 with "#". |
| `mod-0633` | Replace "b" with "4" | Replaces every b with 4. |
| `mod-0634` | Pad start to 18 with "+" | Left-pads the value to width 18 with "+". |
| `mod-0635` | Pad end to 18 with "+" | Right-pads the value to width 18 with "+". |
| `mod-0636` | Replace "b" with "5" | Replaces every b with 5. |
| `mod-0637` | Pad start to 18 with "." | Left-pads the value to width 18 with ".". |
| `mod-0638` | Pad end to 18 with "." | Right-pads the value to width 18 with ".". |
| `mod-0639` | Replace "b" with "6" | Replaces every b with 6. |
| `mod-0640` | Pad start to 18 with "~" | Left-pads the value to width 18 with "~". |
| `mod-0641` | Pad end to 18 with "~" | Right-pads the value to width 18 with "~". |
| `mod-0642` | Replace "b" with "7" | Replaces every b with 7. |
| `mod-0643` | Pad start to 18 with "=" | Left-pads the value to width 18 with "=". |
| `mod-0644` | Pad end to 18 with "=" | Right-pads the value to width 18 with "=". |
| `mod-0645` | Replace "b" with "8" | Replaces every b with 8. |
| `mod-0646` | Pad start to 19 with "-" | Left-pads the value to width 19 with "-". |
| `mod-0647` | Pad end to 19 with "-" | Right-pads the value to width 19 with "-". |
| `mod-0648` | Replace "b" with "9" | Replaces every b with 9. |
| `mod-0649` | Pad start to 19 with "_" | Left-pads the value to width 19 with "_". |
| `mod-0650` | Pad end to 19 with "_" | Right-pads the value to width 19 with "_". |
| `mod-0651` | Replace "c" with "a" | Replaces every c with a. |
| `mod-0652` | Pad start to 19 with "*" | Left-pads the value to width 19 with "*". |
| `mod-0653` | Pad end to 19 with "*" | Right-pads the value to width 19 with "*". |
| `mod-0654` | Replace "c" with "b" | Replaces every c with b. |
| `mod-0655` | Pad start to 19 with "#" | Left-pads the value to width 19 with "#". |
| `mod-0656` | Pad end to 19 with "#" | Right-pads the value to width 19 with "#". |
| `mod-0657` | Replace "c" with "d" | Replaces every c with d. |
| `mod-0658` | Pad start to 19 with "+" | Left-pads the value to width 19 with "+". |
| `mod-0659` | Pad end to 19 with "+" | Right-pads the value to width 19 with "+". |
| `mod-0660` | Replace "c" with "e" | Replaces every c with e. |
| `mod-0661` | Pad start to 19 with "." | Left-pads the value to width 19 with ".". |
| `mod-0662` | Pad end to 19 with "." | Right-pads the value to width 19 with ".". |
| `mod-0663` | Replace "c" with "f" | Replaces every c with f. |
| `mod-0664` | Pad start to 19 with "~" | Left-pads the value to width 19 with "~". |
| `mod-0665` | Pad end to 19 with "~" | Right-pads the value to width 19 with "~". |
| `mod-0666` | Replace "c" with "g" | Replaces every c with g. |
| `mod-0667` | Pad start to 19 with "=" | Left-pads the value to width 19 with "=". |
| `mod-0668` | Pad end to 19 with "=" | Right-pads the value to width 19 with "=". |
| `mod-0669` | Replace "c" with "h" | Replaces every c with h. |
| `mod-0670` | Pad start to 20 with "-" | Left-pads the value to width 20 with "-". |
| `mod-0671` | Pad end to 20 with "-" | Right-pads the value to width 20 with "-". |
| `mod-0672` | Replace "c" with "i" | Replaces every c with i. |
| `mod-0673` | Pad start to 20 with "_" | Left-pads the value to width 20 with "_". |
| `mod-0674` | Pad end to 20 with "_" | Right-pads the value to width 20 with "_". |
| `mod-0675` | Replace "c" with "j" | Replaces every c with j. |
| `mod-0676` | Pad start to 20 with "*" | Left-pads the value to width 20 with "*". |
| `mod-0677` | Pad end to 20 with "*" | Right-pads the value to width 20 with "*". |
| `mod-0678` | Replace "c" with "k" | Replaces every c with k. |
| `mod-0679` | Pad start to 20 with "#" | Left-pads the value to width 20 with "#". |
| `mod-0680` | Pad end to 20 with "#" | Right-pads the value to width 20 with "#". |
| `mod-0681` | Replace "c" with "l" | Replaces every c with l. |
| `mod-0682` | Pad start to 20 with "+" | Left-pads the value to width 20 with "+". |
| `mod-0683` | Pad end to 20 with "+" | Right-pads the value to width 20 with "+". |
| `mod-0684` | Replace "c" with "m" | Replaces every c with m. |
| `mod-0685` | Pad start to 20 with "." | Left-pads the value to width 20 with ".". |
| `mod-0686` | Pad end to 20 with "." | Right-pads the value to width 20 with ".". |
| `mod-0687` | Replace "c" with "n" | Replaces every c with n. |
| `mod-0688` | Pad start to 20 with "~" | Left-pads the value to width 20 with "~". |
| `mod-0689` | Pad end to 20 with "~" | Right-pads the value to width 20 with "~". |
| `mod-0690` | Replace "c" with "o" | Replaces every c with o. |
| `mod-0691` | Pad start to 20 with "=" | Left-pads the value to width 20 with "=". |
| `mod-0692` | Pad end to 20 with "=" | Right-pads the value to width 20 with "=". |
| `mod-0693` | Replace "c" with "p" | Replaces every c with p. |
| `mod-0694` | Pad start to 21 with "-" | Left-pads the value to width 21 with "-". |
| `mod-0695` | Pad end to 21 with "-" | Right-pads the value to width 21 with "-". |
| `mod-0696` | Replace "c" with "q" | Replaces every c with q. |
| `mod-0697` | Pad start to 21 with "_" | Left-pads the value to width 21 with "_". |
| `mod-0698` | Pad end to 21 with "_" | Right-pads the value to width 21 with "_". |
| `mod-0699` | Replace "c" with "r" | Replaces every c with r. |
| `mod-0700` | Pad start to 21 with "*" | Left-pads the value to width 21 with "*". |
| `mod-0701` | Pad end to 21 with "*" | Right-pads the value to width 21 with "*". |
| `mod-0702` | Replace "c" with "s" | Replaces every c with s. |
| `mod-0703` | Pad start to 21 with "#" | Left-pads the value to width 21 with "#". |
| `mod-0704` | Pad end to 21 with "#" | Right-pads the value to width 21 with "#". |
| `mod-0705` | Replace "c" with "t" | Replaces every c with t. |
| `mod-0706` | Pad start to 21 with "+" | Left-pads the value to width 21 with "+". |
| `mod-0707` | Pad end to 21 with "+" | Right-pads the value to width 21 with "+". |
| `mod-0708` | Replace "c" with "u" | Replaces every c with u. |
| `mod-0709` | Pad start to 21 with "." | Left-pads the value to width 21 with ".". |
| `mod-0710` | Pad end to 21 with "." | Right-pads the value to width 21 with ".". |
| `mod-0711` | Replace "c" with "v" | Replaces every c with v. |
| `mod-0712` | Pad start to 21 with "~" | Left-pads the value to width 21 with "~". |
| `mod-0713` | Pad end to 21 with "~" | Right-pads the value to width 21 with "~". |
| `mod-0714` | Replace "c" with "w" | Replaces every c with w. |
| `mod-0715` | Pad start to 21 with "=" | Left-pads the value to width 21 with "=". |
| `mod-0716` | Pad end to 21 with "=" | Right-pads the value to width 21 with "=". |
| `mod-0717` | Replace "c" with "x" | Replaces every c with x. |
| `mod-0718` | Pad start to 22 with "-" | Left-pads the value to width 22 with "-". |
| `mod-0719` | Pad end to 22 with "-" | Right-pads the value to width 22 with "-". |
| `mod-0720` | Replace "c" with "y" | Replaces every c with y. |
| `mod-0721` | Pad start to 22 with "_" | Left-pads the value to width 22 with "_". |
| `mod-0722` | Pad end to 22 with "_" | Right-pads the value to width 22 with "_". |
| `mod-0723` | Replace "c" with "z" | Replaces every c with z. |
| `mod-0724` | Pad start to 22 with "*" | Left-pads the value to width 22 with "*". |
| `mod-0725` | Pad end to 22 with "*" | Right-pads the value to width 22 with "*". |
| `mod-0726` | Replace "c" with "A" | Replaces every c with A. |
| `mod-0727` | Pad start to 22 with "#" | Left-pads the value to width 22 with "#". |
| `mod-0728` | Pad end to 22 with "#" | Right-pads the value to width 22 with "#". |
| `mod-0729` | Replace "c" with "B" | Replaces every c with B. |
| `mod-0730` | Pad start to 22 with "+" | Left-pads the value to width 22 with "+". |
| `mod-0731` | Pad end to 22 with "+" | Right-pads the value to width 22 with "+". |
| `mod-0732` | Replace "c" with "C" | Replaces every c with C. |
| `mod-0733` | Pad start to 22 with "." | Left-pads the value to width 22 with ".". |
| `mod-0734` | Pad end to 22 with "." | Right-pads the value to width 22 with ".". |
| `mod-0735` | Replace "c" with "D" | Replaces every c with D. |
| `mod-0736` | Pad start to 22 with "~" | Left-pads the value to width 22 with "~". |
| `mod-0737` | Pad end to 22 with "~" | Right-pads the value to width 22 with "~". |
| `mod-0738` | Replace "c" with "E" | Replaces every c with E. |
| `mod-0739` | Pad start to 22 with "=" | Left-pads the value to width 22 with "=". |
| `mod-0740` | Pad end to 22 with "=" | Right-pads the value to width 22 with "=". |
| `mod-0741` | Replace "c" with "F" | Replaces every c with F. |
| `mod-0742` | Pad start to 23 with "-" | Left-pads the value to width 23 with "-". |
| `mod-0743` | Pad end to 23 with "-" | Right-pads the value to width 23 with "-". |
| `mod-0744` | Replace "c" with "G" | Replaces every c with G. |
| `mod-0745` | Pad start to 23 with "_" | Left-pads the value to width 23 with "_". |
| `mod-0746` | Pad end to 23 with "_" | Right-pads the value to width 23 with "_". |
| `mod-0747` | Replace "c" with "H" | Replaces every c with H. |
| `mod-0748` | Pad start to 23 with "*" | Left-pads the value to width 23 with "*". |
| `mod-0749` | Pad end to 23 with "*" | Right-pads the value to width 23 with "*". |
| `mod-0750` | Replace "c" with "I" | Replaces every c with I. |
| `mod-0751` | Pad start to 23 with "#" | Left-pads the value to width 23 with "#". |
| `mod-0752` | Pad end to 23 with "#" | Right-pads the value to width 23 with "#". |
| `mod-0753` | Replace "c" with "J" | Replaces every c with J. |
| `mod-0754` | Pad start to 23 with "+" | Left-pads the value to width 23 with "+". |
| `mod-0755` | Pad end to 23 with "+" | Right-pads the value to width 23 with "+". |
| `mod-0756` | Replace "c" with "K" | Replaces every c with K. |
| `mod-0757` | Pad start to 23 with "." | Left-pads the value to width 23 with ".". |
| `mod-0758` | Pad end to 23 with "." | Right-pads the value to width 23 with ".". |
| `mod-0759` | Replace "c" with "L" | Replaces every c with L. |
| `mod-0760` | Pad start to 23 with "~" | Left-pads the value to width 23 with "~". |
| `mod-0761` | Pad end to 23 with "~" | Right-pads the value to width 23 with "~". |
| `mod-0762` | Replace "c" with "M" | Replaces every c with M. |
| `mod-0763` | Pad start to 23 with "=" | Left-pads the value to width 23 with "=". |
| `mod-0764` | Pad end to 23 with "=" | Right-pads the value to width 23 with "=". |
| `mod-0765` | Replace "c" with "N" | Replaces every c with N. |
| `mod-0766` | Pad start to 24 with "-" | Left-pads the value to width 24 with "-". |
| `mod-0767` | Pad end to 24 with "-" | Right-pads the value to width 24 with "-". |
| `mod-0768` | Replace "c" with "O" | Replaces every c with O. |
| `mod-0769` | Pad start to 24 with "_" | Left-pads the value to width 24 with "_". |
| `mod-0770` | Pad end to 24 with "_" | Right-pads the value to width 24 with "_". |
| `mod-0771` | Replace "c" with "P" | Replaces every c with P. |
| `mod-0772` | Pad start to 24 with "*" | Left-pads the value to width 24 with "*". |
| `mod-0773` | Pad end to 24 with "*" | Right-pads the value to width 24 with "*". |
| `mod-0774` | Replace "c" with "Q" | Replaces every c with Q. |
| `mod-0775` | Pad start to 24 with "#" | Left-pads the value to width 24 with "#". |
| `mod-0776` | Pad end to 24 with "#" | Right-pads the value to width 24 with "#". |
| `mod-0777` | Replace "c" with "R" | Replaces every c with R. |
| `mod-0778` | Pad start to 24 with "+" | Left-pads the value to width 24 with "+". |
| `mod-0779` | Pad end to 24 with "+" | Right-pads the value to width 24 with "+". |
| `mod-0780` | Replace "c" with "S" | Replaces every c with S. |
| `mod-0781` | Pad start to 24 with "." | Left-pads the value to width 24 with ".". |
| `mod-0782` | Pad end to 24 with "." | Right-pads the value to width 24 with ".". |
| `mod-0783` | Replace "c" with "T" | Replaces every c with T. |
| `mod-0784` | Pad start to 24 with "~" | Left-pads the value to width 24 with "~". |
| `mod-0785` | Pad end to 24 with "~" | Right-pads the value to width 24 with "~". |
| `mod-0786` | Replace "c" with "U" | Replaces every c with U. |
| `mod-0787` | Pad start to 24 with "=" | Left-pads the value to width 24 with "=". |
| `mod-0788` | Pad end to 24 with "=" | Right-pads the value to width 24 with "=". |
| `mod-0789` | Replace "c" with "V" | Replaces every c with V. |
| `mod-0790` | Pad start to 25 with "-" | Left-pads the value to width 25 with "-". |
| `mod-0791` | Pad end to 25 with "-" | Right-pads the value to width 25 with "-". |
| `mod-0792` | Replace "c" with "W" | Replaces every c with W. |
| `mod-0793` | Pad start to 25 with "_" | Left-pads the value to width 25 with "_". |
| `mod-0794` | Pad end to 25 with "_" | Right-pads the value to width 25 with "_". |
| `mod-0795` | Replace "c" with "X" | Replaces every c with X. |
| `mod-0796` | Pad start to 25 with "*" | Left-pads the value to width 25 with "*". |
| `mod-0797` | Pad end to 25 with "*" | Right-pads the value to width 25 with "*". |
| `mod-0798` | Replace "c" with "Y" | Replaces every c with Y. |
| `mod-0799` | Pad start to 25 with "#" | Left-pads the value to width 25 with "#". |
| `mod-0800` | Pad end to 25 with "#" | Right-pads the value to width 25 with "#". |
| `mod-0801` | Replace "c" with "Z" | Replaces every c with Z. |
| `mod-0802` | Pad start to 25 with "+" | Left-pads the value to width 25 with "+". |
| `mod-0803` | Pad end to 25 with "+" | Right-pads the value to width 25 with "+". |
| `mod-0804` | Replace "c" with "0" | Replaces every c with 0. |
| `mod-0805` | Pad start to 25 with "." | Left-pads the value to width 25 with ".". |
| `mod-0806` | Pad end to 25 with "." | Right-pads the value to width 25 with ".". |
| `mod-0807` | Replace "c" with "1" | Replaces every c with 1. |
| `mod-0808` | Pad start to 25 with "~" | Left-pads the value to width 25 with "~". |
| `mod-0809` | Pad end to 25 with "~" | Right-pads the value to width 25 with "~". |
| `mod-0810` | Replace "c" with "2" | Replaces every c with 2. |
| `mod-0811` | Pad start to 25 with "=" | Left-pads the value to width 25 with "=". |
| `mod-0812` | Pad end to 25 with "=" | Right-pads the value to width 25 with "=". |
| `mod-0813` | Replace "c" with "3" | Replaces every c with 3. |
| `mod-0814` | Pad start to 26 with "-" | Left-pads the value to width 26 with "-". |
| `mod-0815` | Pad end to 26 with "-" | Right-pads the value to width 26 with "-". |
| `mod-0816` | Replace "c" with "4" | Replaces every c with 4. |
| `mod-0817` | Pad start to 26 with "_" | Left-pads the value to width 26 with "_". |
| `mod-0818` | Pad end to 26 with "_" | Right-pads the value to width 26 with "_". |
| `mod-0819` | Replace "c" with "5" | Replaces every c with 5. |
| `mod-0820` | Pad start to 26 with "*" | Left-pads the value to width 26 with "*". |
| `mod-0821` | Pad end to 26 with "*" | Right-pads the value to width 26 with "*". |
| `mod-0822` | Replace "c" with "6" | Replaces every c with 6. |
| `mod-0823` | Pad start to 26 with "#" | Left-pads the value to width 26 with "#". |
| `mod-0824` | Pad end to 26 with "#" | Right-pads the value to width 26 with "#". |
| `mod-0825` | Replace "c" with "7" | Replaces every c with 7. |
| `mod-0826` | Pad start to 26 with "+" | Left-pads the value to width 26 with "+". |
| `mod-0827` | Pad end to 26 with "+" | Right-pads the value to width 26 with "+". |
| `mod-0828` | Replace "c" with "8" | Replaces every c with 8. |
| `mod-0829` | Pad start to 26 with "." | Left-pads the value to width 26 with ".". |
| `mod-0830` | Pad end to 26 with "." | Right-pads the value to width 26 with ".". |
| `mod-0831` | Replace "c" with "9" | Replaces every c with 9. |
| `mod-0832` | Pad start to 26 with "~" | Left-pads the value to width 26 with "~". |
| `mod-0833` | Pad end to 26 with "~" | Right-pads the value to width 26 with "~". |
| `mod-0834` | Replace "d" with "a" | Replaces every d with a. |
| `mod-0835` | Pad start to 26 with "=" | Left-pads the value to width 26 with "=". |
| `mod-0836` | Pad end to 26 with "=" | Right-pads the value to width 26 with "=". |
| `mod-0837` | Replace "d" with "b" | Replaces every d with b. |
| `mod-0838` | Pad start to 27 with "-" | Left-pads the value to width 27 with "-". |
| `mod-0839` | Pad end to 27 with "-" | Right-pads the value to width 27 with "-". |
| `mod-0840` | Replace "d" with "c" | Replaces every d with c. |
| `mod-0841` | Pad start to 27 with "_" | Left-pads the value to width 27 with "_". |
| `mod-0842` | Pad end to 27 with "_" | Right-pads the value to width 27 with "_". |
| `mod-0843` | Replace "d" with "e" | Replaces every d with e. |
| `mod-0844` | Pad start to 27 with "*" | Left-pads the value to width 27 with "*". |
| `mod-0845` | Pad end to 27 with "*" | Right-pads the value to width 27 with "*". |
| `mod-0846` | Replace "d" with "f" | Replaces every d with f. |
| `mod-0847` | Pad start to 27 with "#" | Left-pads the value to width 27 with "#". |
| `mod-0848` | Pad end to 27 with "#" | Right-pads the value to width 27 with "#". |
| `mod-0849` | Replace "d" with "g" | Replaces every d with g. |
| `mod-0850` | Pad start to 27 with "+" | Left-pads the value to width 27 with "+". |
| `mod-0851` | Pad end to 27 with "+" | Right-pads the value to width 27 with "+". |
| `mod-0852` | Replace "d" with "h" | Replaces every d with h. |
| `mod-0853` | Pad start to 27 with "." | Left-pads the value to width 27 with ".". |
| `mod-0854` | Pad end to 27 with "." | Right-pads the value to width 27 with ".". |
| `mod-0855` | Replace "d" with "i" | Replaces every d with i. |
| `mod-0856` | Pad start to 27 with "~" | Left-pads the value to width 27 with "~". |
| `mod-0857` | Pad end to 27 with "~" | Right-pads the value to width 27 with "~". |
| `mod-0858` | Replace "d" with "j" | Replaces every d with j. |
| `mod-0859` | Pad start to 27 with "=" | Left-pads the value to width 27 with "=". |
| `mod-0860` | Pad end to 27 with "=" | Right-pads the value to width 27 with "=". |
| `mod-0861` | Replace "d" with "k" | Replaces every d with k. |
| `mod-0862` | Pad start to 28 with "-" | Left-pads the value to width 28 with "-". |
| `mod-0863` | Pad end to 28 with "-" | Right-pads the value to width 28 with "-". |
| `mod-0864` | Replace "d" with "l" | Replaces every d with l. |
| `mod-0865` | Pad start to 28 with "_" | Left-pads the value to width 28 with "_". |
| `mod-0866` | Pad end to 28 with "_" | Right-pads the value to width 28 with "_". |
| `mod-0867` | Replace "d" with "m" | Replaces every d with m. |
| `mod-0868` | Pad start to 28 with "*" | Left-pads the value to width 28 with "*". |
| `mod-0869` | Pad end to 28 with "*" | Right-pads the value to width 28 with "*". |
| `mod-0870` | Replace "d" with "n" | Replaces every d with n. |
| `mod-0871` | Pad start to 28 with "#" | Left-pads the value to width 28 with "#". |
| `mod-0872` | Pad end to 28 with "#" | Right-pads the value to width 28 with "#". |
| `mod-0873` | Replace "d" with "o" | Replaces every d with o. |
| `mod-0874` | Pad start to 28 with "+" | Left-pads the value to width 28 with "+". |
| `mod-0875` | Pad end to 28 with "+" | Right-pads the value to width 28 with "+". |
| `mod-0876` | Replace "d" with "p" | Replaces every d with p. |
| `mod-0877` | Pad start to 28 with "." | Left-pads the value to width 28 with ".". |
| `mod-0878` | Pad end to 28 with "." | Right-pads the value to width 28 with ".". |
| `mod-0879` | Replace "d" with "q" | Replaces every d with q. |
| `mod-0880` | Pad start to 28 with "~" | Left-pads the value to width 28 with "~". |
| `mod-0881` | Pad end to 28 with "~" | Right-pads the value to width 28 with "~". |
| `mod-0882` | Replace "d" with "r" | Replaces every d with r. |
| `mod-0883` | Pad start to 28 with "=" | Left-pads the value to width 28 with "=". |
| `mod-0884` | Pad end to 28 with "=" | Right-pads the value to width 28 with "=". |
| `mod-0885` | Replace "d" with "s" | Replaces every d with s. |
| `mod-0886` | Pad start to 29 with "-" | Left-pads the value to width 29 with "-". |
| `mod-0887` | Pad end to 29 with "-" | Right-pads the value to width 29 with "-". |
| `mod-0888` | Replace "d" with "t" | Replaces every d with t. |
| `mod-0889` | Pad start to 29 with "_" | Left-pads the value to width 29 with "_". |
| `mod-0890` | Pad end to 29 with "_" | Right-pads the value to width 29 with "_". |
| `mod-0891` | Replace "d" with "u" | Replaces every d with u. |
| `mod-0892` | Pad start to 29 with "*" | Left-pads the value to width 29 with "*". |
| `mod-0893` | Pad end to 29 with "*" | Right-pads the value to width 29 with "*". |
| `mod-0894` | Replace "d" with "v" | Replaces every d with v. |
| `mod-0895` | Pad start to 29 with "#" | Left-pads the value to width 29 with "#". |
| `mod-0896` | Pad end to 29 with "#" | Right-pads the value to width 29 with "#". |
| `mod-0897` | Replace "d" with "w" | Replaces every d with w. |
| `mod-0898` | Pad start to 29 with "+" | Left-pads the value to width 29 with "+". |
| `mod-0899` | Pad end to 29 with "+" | Right-pads the value to width 29 with "+". |
| `mod-0900` | Replace "d" with "x" | Replaces every d with x. |
| `mod-0901` | Pad start to 29 with "." | Left-pads the value to width 29 with ".". |
| `mod-0902` | Pad end to 29 with "." | Right-pads the value to width 29 with ".". |
| `mod-0903` | Replace "d" with "y" | Replaces every d with y. |
| `mod-0904` | Pad start to 29 with "~" | Left-pads the value to width 29 with "~". |
| `mod-0905` | Pad end to 29 with "~" | Right-pads the value to width 29 with "~". |
| `mod-0906` | Replace "d" with "z" | Replaces every d with z. |
| `mod-0907` | Pad start to 29 with "=" | Left-pads the value to width 29 with "=". |
| `mod-0908` | Pad end to 29 with "=" | Right-pads the value to width 29 with "=". |
| `mod-0909` | Replace "d" with "A" | Replaces every d with A. |
| `mod-0910` | Pad start to 30 with "-" | Left-pads the value to width 30 with "-". |
| `mod-0911` | Pad end to 30 with "-" | Right-pads the value to width 30 with "-". |
| `mod-0912` | Replace "d" with "B" | Replaces every d with B. |
| `mod-0913` | Pad start to 30 with "_" | Left-pads the value to width 30 with "_". |
| `mod-0914` | Pad end to 30 with "_" | Right-pads the value to width 30 with "_". |
| `mod-0915` | Replace "d" with "C" | Replaces every d with C. |
| `mod-0916` | Pad start to 30 with "*" | Left-pads the value to width 30 with "*". |
| `mod-0917` | Pad end to 30 with "*" | Right-pads the value to width 30 with "*". |
| `mod-0918` | Replace "d" with "D" | Replaces every d with D. |
| `mod-0919` | Pad start to 30 with "#" | Left-pads the value to width 30 with "#". |
| `mod-0920` | Pad end to 30 with "#" | Right-pads the value to width 30 with "#". |
| `mod-0921` | Replace "d" with "E" | Replaces every d with E. |
| `mod-0922` | Pad start to 30 with "+" | Left-pads the value to width 30 with "+". |
| `mod-0923` | Pad end to 30 with "+" | Right-pads the value to width 30 with "+". |
| `mod-0924` | Replace "d" with "F" | Replaces every d with F. |
| `mod-0925` | Pad start to 30 with "." | Left-pads the value to width 30 with ".". |
| `mod-0926` | Pad end to 30 with "." | Right-pads the value to width 30 with ".". |
| `mod-0927` | Replace "d" with "G" | Replaces every d with G. |
| `mod-0928` | Pad start to 30 with "~" | Left-pads the value to width 30 with "~". |
| `mod-0929` | Pad end to 30 with "~" | Right-pads the value to width 30 with "~". |
| `mod-0930` | Replace "d" with "H" | Replaces every d with H. |
| `mod-0931` | Pad start to 30 with "=" | Left-pads the value to width 30 with "=". |
| `mod-0932` | Pad end to 30 with "=" | Right-pads the value to width 30 with "=". |
| `mod-0933` | Replace "d" with "I" | Replaces every d with I. |
| `mod-0934` | Pad start to 31 with "-" | Left-pads the value to width 31 with "-". |
| `mod-0935` | Pad end to 31 with "-" | Right-pads the value to width 31 with "-". |
| `mod-0936` | Replace "d" with "J" | Replaces every d with J. |
| `mod-0937` | Pad start to 31 with "_" | Left-pads the value to width 31 with "_". |
| `mod-0938` | Pad end to 31 with "_" | Right-pads the value to width 31 with "_". |
| `mod-0939` | Replace "d" with "K" | Replaces every d with K. |
| `mod-0940` | Pad start to 31 with "*" | Left-pads the value to width 31 with "*". |
| `mod-0941` | Pad end to 31 with "*" | Right-pads the value to width 31 with "*". |
| `mod-0942` | Replace "d" with "L" | Replaces every d with L. |
| `mod-0943` | Pad start to 31 with "#" | Left-pads the value to width 31 with "#". |
| `mod-0944` | Pad end to 31 with "#" | Right-pads the value to width 31 with "#". |
| `mod-0945` | Replace "d" with "M" | Replaces every d with M. |
| `mod-0946` | Pad start to 31 with "+" | Left-pads the value to width 31 with "+". |
| `mod-0947` | Pad end to 31 with "+" | Right-pads the value to width 31 with "+". |
| `mod-0948` | Replace "d" with "N" | Replaces every d with N. |
| `mod-0949` | Pad start to 31 with "." | Left-pads the value to width 31 with ".". |
| `mod-0950` | Pad end to 31 with "." | Right-pads the value to width 31 with ".". |
| `mod-0951` | Replace "d" with "O" | Replaces every d with O. |
| `mod-0952` | Pad start to 31 with "~" | Left-pads the value to width 31 with "~". |
| `mod-0953` | Pad end to 31 with "~" | Right-pads the value to width 31 with "~". |
| `mod-0954` | Replace "d" with "P" | Replaces every d with P. |
| `mod-0955` | Pad start to 31 with "=" | Left-pads the value to width 31 with "=". |
| `mod-0956` | Pad end to 31 with "=" | Right-pads the value to width 31 with "=". |
| `mod-0957` | Replace "d" with "Q" | Replaces every d with Q. |
| `mod-0958` | Pad start to 32 with "-" | Left-pads the value to width 32 with "-". |
| `mod-0959` | Pad end to 32 with "-" | Right-pads the value to width 32 with "-". |
| `mod-0960` | Replace "d" with "R" | Replaces every d with R. |
| `mod-0961` | Pad start to 32 with "_" | Left-pads the value to width 32 with "_". |
| `mod-0962` | Pad end to 32 with "_" | Right-pads the value to width 32 with "_". |
| `mod-0963` | Replace "d" with "S" | Replaces every d with S. |
| `mod-0964` | Pad start to 32 with "*" | Left-pads the value to width 32 with "*". |
| `mod-0965` | Pad end to 32 with "*" | Right-pads the value to width 32 with "*". |
| `mod-0966` | Replace "d" with "T" | Replaces every d with T. |
| `mod-0967` | Pad start to 32 with "#" | Left-pads the value to width 32 with "#". |
| `mod-0968` | Pad end to 32 with "#" | Right-pads the value to width 32 with "#". |
| `mod-0969` | Replace "d" with "U" | Replaces every d with U. |
| `mod-0970` | Pad start to 32 with "+" | Left-pads the value to width 32 with "+". |
| `mod-0971` | Pad end to 32 with "+" | Right-pads the value to width 32 with "+". |
| `mod-0972` | Replace "d" with "V" | Replaces every d with V. |
| `mod-0973` | Pad start to 32 with "." | Left-pads the value to width 32 with ".". |
| `mod-0974` | Pad end to 32 with "." | Right-pads the value to width 32 with ".". |
| `mod-0975` | Replace "d" with "W" | Replaces every d with W. |
| `mod-0976` | Pad start to 32 with "~" | Left-pads the value to width 32 with "~". |
| `mod-0977` | Pad end to 32 with "~" | Right-pads the value to width 32 with "~". |
| `mod-04658` | Replace "d" with "X" | Replaces every d with X. |
| `mod-0979` | Pad start to 32 with "=" | Left-pads the value to width 32 with "=". |
| `mod-0980` | Pad end to 32 with "=" | Right-pads the value to width 32 with "=". |
| `mod-0981` | Replace "d" with "Y" | Replaces every d with Y. |
| `mod-0982` | Pad start to 33 with "-" | Left-pads the value to width 33 with "-". |
| `mod-0983` | Pad end to 33 with "-" | Right-pads the value to width 33 with "-". |
| `mod-0984` | Replace "d" with "Z" | Replaces every d with Z. |
| `mod-0985` | Pad start to 33 with "_" | Left-pads the value to width 33 with "_". |
| `mod-0986` | Pad end to 33 with "_" | Right-pads the value to width 33 with "_". |
| `mod-0987` | Replace "d" with "0" | Replaces every d with 0. |
| `mod-0988` | Pad start to 33 with "*" | Left-pads the value to width 33 with "*". |
| `mod-0989` | Pad end to 33 with "*" | Right-pads the value to width 33 with "*". |
| `mod-0990` | Replace "d" with "1" | Replaces every d with 1. |
| `mod-0991` | Pad start to 33 with "#" | Left-pads the value to width 33 with "#". |
| `mod-0992` | Pad end to 33 with "#" | Right-pads the value to width 33 with "#". |
| `mod-0993` | Replace "d" with "2" | Replaces every d with 2. |
| `mod-0994` | Pad start to 33 with "+" | Left-pads the value to width 33 with "+". |
| `mod-0995` | Pad end to 33 with "+" | Right-pads the value to width 33 with "+". |
| `mod-0996` | Replace "d" with "3" | Replaces every d with 3. |
| `mod-0997` | Pad start to 33 with "." | Left-pads the value to width 33 with ".". |
| `mod-0998` | Pad end to 33 with "." | Right-pads the value to width 33 with ".". |
| `mod-0999` | Replace "d" with "4" | Replaces every d with 4. |
| `mod-1000` | Pad start to 33 with "~" | Left-pads the value to width 33 with "~". |
| `mod-1001` | Pad end to 33 with "~" | Right-pads the value to width 33 with "~". |
| `mod-1002` | Replace "d" with "5" | Replaces every d with 5. |
| `mod-1003` | Pad start to 33 with "=" | Left-pads the value to width 33 with "=". |
| `mod-1004` | Pad end to 33 with "=" | Right-pads the value to width 33 with "=". |
| `mod-1005` | Replace "d" with "6" | Replaces every d with 6. |
| `mod-1006` | Pad start to 34 with "-" | Left-pads the value to width 34 with "-". |
| `mod-1007` | Pad end to 34 with "-" | Right-pads the value to width 34 with "-". |
| `mod-1008` | Replace "d" with "7" | Replaces every d with 7. |
| `mod-1009` | Pad start to 34 with "_" | Left-pads the value to width 34 with "_". |
| `mod-1010` | Pad end to 34 with "_" | Right-pads the value to width 34 with "_". |
| `mod-1011` | Replace "d" with "8" | Replaces every d with 8. |
| `mod-1012` | Pad start to 34 with "*" | Left-pads the value to width 34 with "*". |
| `mod-1013` | Pad end to 34 with "*" | Right-pads the value to width 34 with "*". |
| `mod-1014` | Replace "d" with "9" | Replaces every d with 9. |
| `mod-1015` | Pad start to 34 with "#" | Left-pads the value to width 34 with "#". |
| `mod-1016` | Pad end to 34 with "#" | Right-pads the value to width 34 with "#". |
| `mod-1017` | Replace "e" with "a" | Replaces every e with a. |
| `mod-1018` | Pad start to 34 with "+" | Left-pads the value to width 34 with "+". |
| `mod-1019` | Pad end to 34 with "+" | Right-pads the value to width 34 with "+". |
| `mod-1020` | Replace "e" with "b" | Replaces every e with b. |
| `mod-1021` | Pad start to 34 with "." | Left-pads the value to width 34 with ".". |
| `mod-1022` | Pad end to 34 with "." | Right-pads the value to width 34 with ".". |
| `mod-1023` | Replace "e" with "c" | Replaces every e with c. |
| `mod-1024` | Pad start to 34 with "~" | Left-pads the value to width 34 with "~". |
| `mod-1025` | Pad end to 34 with "~" | Right-pads the value to width 34 with "~". |
| `mod-1026` | Replace "e" with "d" | Replaces every e with d. |
| `mod-1027` | Pad start to 34 with "=" | Left-pads the value to width 34 with "=". |
| `mod-1028` | Pad end to 34 with "=" | Right-pads the value to width 34 with "=". |
| `mod-1029` | Replace "e" with "f" | Replaces every e with f. |
| `mod-1030` | Pad start to 35 with "-" | Left-pads the value to width 35 with "-". |
| `mod-1031` | Pad end to 35 with "-" | Right-pads the value to width 35 with "-". |
| `mod-1032` | Replace "e" with "g" | Replaces every e with g. |
| `mod-1033` | Pad start to 35 with "_" | Left-pads the value to width 35 with "_". |
| `mod-1034` | Pad end to 35 with "_" | Right-pads the value to width 35 with "_". |
| `mod-1035` | Replace "e" with "h" | Replaces every e with h. |
| `mod-1036` | Pad start to 35 with "*" | Left-pads the value to width 35 with "*". |
| `mod-1037` | Pad end to 35 with "*" | Right-pads the value to width 35 with "*". |
| `mod-1038` | Replace "e" with "i" | Replaces every e with i. |
| `mod-1039` | Pad start to 35 with "#" | Left-pads the value to width 35 with "#". |
| `mod-1040` | Pad end to 35 with "#" | Right-pads the value to width 35 with "#". |
| `mod-1041` | Replace "e" with "j" | Replaces every e with j. |
| `mod-1042` | Pad start to 35 with "+" | Left-pads the value to width 35 with "+". |
| `mod-1043` | Pad end to 35 with "+" | Right-pads the value to width 35 with "+". |
| `mod-1044` | Replace "e" with "k" | Replaces every e with k. |
| `mod-1045` | Pad start to 35 with "." | Left-pads the value to width 35 with ".". |
| `mod-1046` | Pad end to 35 with "." | Right-pads the value to width 35 with ".". |
| `mod-1047` | Replace "e" with "l" | Replaces every e with l. |
| `mod-1048` | Pad start to 35 with "~" | Left-pads the value to width 35 with "~". |
| `mod-1049` | Pad end to 35 with "~" | Right-pads the value to width 35 with "~". |
| `mod-1050` | Replace "e" with "m" | Replaces every e with m. |
| `mod-1051` | Pad start to 35 with "=" | Left-pads the value to width 35 with "=". |
| `mod-1052` | Pad end to 35 with "=" | Right-pads the value to width 35 with "=". |
| `mod-1053` | Replace "e" with "n" | Replaces every e with n. |
| `mod-1054` | Pad start to 36 with "-" | Left-pads the value to width 36 with "-". |
| `mod-1055` | Pad end to 36 with "-" | Right-pads the value to width 36 with "-". |
| `mod-1056` | Replace "e" with "o" | Replaces every e with o. |
| `mod-1057` | Pad start to 36 with "_" | Left-pads the value to width 36 with "_". |
| `mod-1058` | Pad end to 36 with "_" | Right-pads the value to width 36 with "_". |
| `mod-1059` | Replace "e" with "p" | Replaces every e with p. |
| `mod-1060` | Pad start to 36 with "*" | Left-pads the value to width 36 with "*". |
| `mod-1061` | Pad end to 36 with "*" | Right-pads the value to width 36 with "*". |
| `mod-1062` | Replace "e" with "q" | Replaces every e with q. |
| `mod-1063` | Pad start to 36 with "#" | Left-pads the value to width 36 with "#". |
| `mod-1064` | Pad end to 36 with "#" | Right-pads the value to width 36 with "#". |
| `mod-1065` | Replace "e" with "r" | Replaces every e with r. |
| `mod-1066` | Pad start to 36 with "+" | Left-pads the value to width 36 with "+". |
| `mod-1067` | Pad end to 36 with "+" | Right-pads the value to width 36 with "+". |
| `mod-1068` | Replace "e" with "s" | Replaces every e with s. |
| `mod-1069` | Pad start to 36 with "." | Left-pads the value to width 36 with ".". |
| `mod-1070` | Pad end to 36 with "." | Right-pads the value to width 36 with ".". |
| `mod-1071` | Replace "e" with "t" | Replaces every e with t. |
| `mod-1072` | Pad start to 36 with "~" | Left-pads the value to width 36 with "~". |
| `mod-1073` | Pad end to 36 with "~" | Right-pads the value to width 36 with "~". |
| `mod-1074` | Replace "e" with "u" | Replaces every e with u. |
| `mod-1075` | Pad start to 36 with "=" | Left-pads the value to width 36 with "=". |
| `mod-1076` | Pad end to 36 with "=" | Right-pads the value to width 36 with "=". |
| `mod-1077` | Replace "e" with "v" | Replaces every e with v. |
| `mod-1078` | Pad start to 37 with "-" | Left-pads the value to width 37 with "-". |
| `mod-1079` | Pad end to 37 with "-" | Right-pads the value to width 37 with "-". |
| `mod-1080` | Replace "e" with "w" | Replaces every e with w. |
| `mod-1081` | Pad start to 37 with "_" | Left-pads the value to width 37 with "_". |
| `mod-1082` | Pad end to 37 with "_" | Right-pads the value to width 37 with "_". |
| `mod-1083` | Replace "e" with "x" | Replaces every e with x. |
| `mod-1084` | Pad start to 37 with "*" | Left-pads the value to width 37 with "*". |
| `mod-1085` | Pad end to 37 with "*" | Right-pads the value to width 37 with "*". |
| `mod-1086` | Replace "e" with "y" | Replaces every e with y. |
| `mod-1087` | Pad start to 37 with "#" | Left-pads the value to width 37 with "#". |
| `mod-1088` | Pad end to 37 with "#" | Right-pads the value to width 37 with "#". |
| `mod-1089` | Replace "e" with "z" | Replaces every e with z. |
| `mod-1090` | Pad start to 37 with "+" | Left-pads the value to width 37 with "+". |
| `mod-1091` | Pad end to 37 with "+" | Right-pads the value to width 37 with "+". |
| `mod-1092` | Replace "e" with "A" | Replaces every e with A. |
| `mod-1093` | Pad start to 37 with "." | Left-pads the value to width 37 with ".". |
| `mod-1094` | Pad end to 37 with "." | Right-pads the value to width 37 with ".". |
| `mod-1095` | Replace "e" with "B" | Replaces every e with B. |
| `mod-1096` | Pad start to 37 with "~" | Left-pads the value to width 37 with "~". |
| `mod-1097` | Pad end to 37 with "~" | Right-pads the value to width 37 with "~". |
| `mod-1098` | Replace "e" with "C" | Replaces every e with C. |
| `mod-1099` | Pad start to 37 with "=" | Left-pads the value to width 37 with "=". |
| `mod-1100` | Pad end to 37 with "=" | Right-pads the value to width 37 with "=". |
| `mod-1101` | Replace "e" with "D" | Replaces every e with D. |
| `mod-1102` | Pad start to 38 with "-" | Left-pads the value to width 38 with "-". |
| `mod-1103` | Pad end to 38 with "-" | Right-pads the value to width 38 with "-". |
| `mod-1104` | Replace "e" with "E" | Replaces every e with E. |
| `mod-1105` | Pad start to 38 with "_" | Left-pads the value to width 38 with "_". |
| `mod-1106` | Pad end to 38 with "_" | Right-pads the value to width 38 with "_". |
| `mod-1107` | Replace "e" with "F" | Replaces every e with F. |
| `mod-1108` | Pad start to 38 with "*" | Left-pads the value to width 38 with "*". |
| `mod-1109` | Pad end to 38 with "*" | Right-pads the value to width 38 with "*". |
| `mod-1110` | Replace "e" with "G" | Replaces every e with G. |
| `mod-1111` | Pad start to 38 with "#" | Left-pads the value to width 38 with "#". |
| `mod-1112` | Pad end to 38 with "#" | Right-pads the value to width 38 with "#". |
| `mod-1113` | Replace "e" with "H" | Replaces every e with H. |
| `mod-1114` | Pad start to 38 with "+" | Left-pads the value to width 38 with "+". |
| `mod-1115` | Pad end to 38 with "+" | Right-pads the value to width 38 with "+". |
| `mod-1116` | Replace "e" with "I" | Replaces every e with I. |
| `mod-1117` | Pad start to 38 with "." | Left-pads the value to width 38 with ".". |
| `mod-1118` | Pad end to 38 with "." | Right-pads the value to width 38 with ".". |
| `mod-1119` | Replace "e" with "J" | Replaces every e with J. |
| `mod-1120` | Pad start to 38 with "~" | Left-pads the value to width 38 with "~". |
| `mod-1121` | Pad end to 38 with "~" | Right-pads the value to width 38 with "~". |
| `mod-1122` | Replace "e" with "K" | Replaces every e with K. |
| `mod-1123` | Pad start to 38 with "=" | Left-pads the value to width 38 with "=". |
| `mod-1124` | Pad end to 38 with "=" | Right-pads the value to width 38 with "=". |
| `mod-1125` | Replace "e" with "L" | Replaces every e with L. |
| `mod-1126` | Pad start to 39 with "-" | Left-pads the value to width 39 with "-". |
| `mod-1127` | Pad end to 39 with "-" | Right-pads the value to width 39 with "-". |
| `mod-1128` | Replace "e" with "M" | Replaces every e with M. |
| `mod-1129` | Pad start to 39 with "_" | Left-pads the value to width 39 with "_". |
| `mod-1130` | Pad end to 39 with "_" | Right-pads the value to width 39 with "_". |
| `mod-1131` | Replace "e" with "N" | Replaces every e with N. |
| `mod-1132` | Pad start to 39 with "*" | Left-pads the value to width 39 with "*". |
| `mod-1133` | Pad end to 39 with "*" | Right-pads the value to width 39 with "*". |
| `mod-1134` | Replace "e" with "O" | Replaces every e with O. |
| `mod-1135` | Pad start to 39 with "#" | Left-pads the value to width 39 with "#". |
| `mod-1136` | Pad end to 39 with "#" | Right-pads the value to width 39 with "#". |
| `mod-1137` | Replace "e" with "P" | Replaces every e with P. |
| `mod-1138` | Pad start to 39 with "+" | Left-pads the value to width 39 with "+". |
| `mod-1139` | Pad end to 39 with "+" | Right-pads the value to width 39 with "+". |
| `mod-1140` | Replace "e" with "Q" | Replaces every e with Q. |
| `mod-1141` | Pad start to 39 with "." | Left-pads the value to width 39 with ".". |
| `mod-1142` | Pad end to 39 with "." | Right-pads the value to width 39 with ".". |
| `mod-1143` | Replace "e" with "R" | Replaces every e with R. |
| `mod-1144` | Pad start to 39 with "~" | Left-pads the value to width 39 with "~". |
| `mod-1145` | Pad end to 39 with "~" | Right-pads the value to width 39 with "~". |
| `mod-1146` | Replace "e" with "S" | Replaces every e with S. |
| `mod-1147` | Pad start to 39 with "=" | Left-pads the value to width 39 with "=". |
| `mod-1148` | Pad end to 39 with "=" | Right-pads the value to width 39 with "=". |
| `mod-1149` | Replace "e" with "T" | Replaces every e with T. |
| `mod-1150` | Pad start to 40 with "-" | Left-pads the value to width 40 with "-". |
| `mod-1151` | Pad end to 40 with "-" | Right-pads the value to width 40 with "-". |
| `mod-1152` | Replace "e" with "U" | Replaces every e with U. |
| `mod-1153` | Pad start to 40 with "_" | Left-pads the value to width 40 with "_". |
| `mod-1154` | Pad end to 40 with "_" | Right-pads the value to width 40 with "_". |
| `mod-1155` | Replace "e" with "V" | Replaces every e with V. |
| `mod-1156` | Pad start to 40 with "*" | Left-pads the value to width 40 with "*". |
| `mod-1157` | Pad end to 40 with "*" | Right-pads the value to width 40 with "*". |
| `mod-1158` | Replace "e" with "W" | Replaces every e with W. |
| `mod-1159` | Pad start to 40 with "#" | Left-pads the value to width 40 with "#". |
| `mod-1160` | Pad end to 40 with "#" | Right-pads the value to width 40 with "#". |
| `mod-1161` | Replace "e" with "X" | Replaces every e with X. |
| `mod-1162` | Pad start to 40 with "+" | Left-pads the value to width 40 with "+". |
| `mod-1163` | Pad end to 40 with "+" | Right-pads the value to width 40 with "+". |
| `mod-1164` | Replace "e" with "Y" | Replaces every e with Y. |
| `mod-1165` | Pad start to 40 with "." | Left-pads the value to width 40 with ".". |
| `mod-1166` | Pad end to 40 with "." | Right-pads the value to width 40 with ".". |
| `mod-1167` | Replace "e" with "Z" | Replaces every e with Z. |
| `mod-1168` | Pad start to 40 with "~" | Left-pads the value to width 40 with "~". |
| `mod-1169` | Pad end to 40 with "~" | Right-pads the value to width 40 with "~". |
| `mod-1170` | Replace "e" with "0" | Replaces every e with 0. |
| `mod-1171` | Pad start to 40 with "=" | Left-pads the value to width 40 with "=". |
| `mod-1172` | Pad end to 40 with "=" | Right-pads the value to width 40 with "=". |
| `mod-1173` | Replace "e" with "1" | Replaces every e with 1. |
| `mod-1174` | Replace "e" with "2" | Replaces every e with 2. |
| `mod-1175` | Replace "e" with "3" | Replaces every e with 3. |
| `mod-1176` | Replace "e" with "4" | Replaces every e with 4. |
| `mod-1177` | Replace "e" with "5" | Replaces every e with 5. |
| `mod-1178` | Replace "e" with "6" | Replaces every e with 6. |
| `mod-1179` | Replace "e" with "7" | Replaces every e with 7. |
| `mod-1180` | Replace "e" with "8" | Replaces every e with 8. |
| `mod-1181` | Replace "e" with "9" | Replaces every e with 9. |
| `mod-1182` | Replace "f" with "a" | Replaces every f with a. |
| `mod-1183` | Replace "f" with "b" | Replaces every f with b. |
| `mod-1184` | Replace "f" with "c" | Replaces every f with c. |
| `mod-1185` | Replace "f" with "d" | Replaces every f with d. |
| `mod-1186` | Replace "f" with "e" | Replaces every f with e. |
| `mod-1187` | Replace "f" with "g" | Replaces every f with g. |
| `mod-1188` | Replace "f" with "h" | Replaces every f with h. |
| `mod-1189` | Replace "f" with "i" | Replaces every f with i. |
| `mod-1190` | Replace "f" with "j" | Replaces every f with j. |
| `mod-1191` | Replace "f" with "k" | Replaces every f with k. |
| `mod-1192` | Replace "f" with "l" | Replaces every f with l. |
| `mod-1193` | Replace "f" with "m" | Replaces every f with m. |
| `mod-1194` | Replace "f" with "n" | Replaces every f with n. |
| `mod-1195` | Replace "f" with "o" | Replaces every f with o. |
| `mod-1196` | Replace "f" with "p" | Replaces every f with p. |
| `mod-1197` | Replace "f" with "q" | Replaces every f with q. |
| `mod-1198` | Replace "f" with "r" | Replaces every f with r. |
| `mod-1199` | Replace "f" with "s" | Replaces every f with s. |
| `mod-1200` | Replace "f" with "t" | Replaces every f with t. |
| `mod-1201` | Replace "f" with "u" | Replaces every f with u. |
| `mod-1202` | Replace "f" with "v" | Replaces every f with v. |
| `mod-1203` | Replace "f" with "w" | Replaces every f with w. |
| `mod-1204` | Replace "f" with "x" | Replaces every f with x. |
| `mod-1205` | Replace "f" with "y" | Replaces every f with y. |
| `mod-1206` | Replace "f" with "z" | Replaces every f with z. |
| `mod-1207` | Replace "f" with "A" | Replaces every f with A. |
| `mod-1208` | Replace "f" with "B" | Replaces every f with B. |
| `mod-1209` | Replace "f" with "C" | Replaces every f with C. |
| `mod-1210` | Replace "f" with "D" | Replaces every f with D. |
| `mod-1211` | Replace "f" with "E" | Replaces every f with E. |
| `mod-1212` | Replace "f" with "F" | Replaces every f with F. |
| `mod-1213` | Replace "f" with "G" | Replaces every f with G. |
| `mod-1214` | Replace "f" with "H" | Replaces every f with H. |
| `mod-1215` | Replace "f" with "I" | Replaces every f with I. |
| `mod-1216` | Replace "f" with "J" | Replaces every f with J. |
| `mod-1217` | Replace "f" with "K" | Replaces every f with K. |
| `mod-1218` | Replace "f" with "L" | Replaces every f with L. |
| `mod-1219` | Replace "f" with "M" | Replaces every f with M. |
| `mod-1220` | Replace "f" with "N" | Replaces every f with N. |
| `mod-1221` | Replace "f" with "O" | Replaces every f with O. |
| `mod-1222` | Replace "f" with "P" | Replaces every f with P. |
| `mod-1223` | Replace "f" with "Q" | Replaces every f with Q. |
| `mod-1224` | Replace "f" with "R" | Replaces every f with R. |
| `mod-1225` | Replace "f" with "S" | Replaces every f with S. |
| `mod-1226` | Replace "f" with "T" | Replaces every f with T. |
| `mod-1227` | Replace "f" with "U" | Replaces every f with U. |
| `mod-1228` | Replace "f" with "V" | Replaces every f with V. |
| `mod-1229` | Replace "f" with "W" | Replaces every f with W. |
| `mod-1230` | Replace "f" with "X" | Replaces every f with X. |
| `mod-1231` | Replace "f" with "Y" | Replaces every f with Y. |
| `mod-1232` | Replace "f" with "Z" | Replaces every f with Z. |
| `mod-1233` | Replace "f" with "0" | Replaces every f with 0. |
| `mod-1234` | Replace "f" with "1" | Replaces every f with 1. |
| `mod-1235` | Replace "f" with "2" | Replaces every f with 2. |
| `mod-1236` | Replace "f" with "3" | Replaces every f with 3. |
| `mod-1237` | Replace "f" with "4" | Replaces every f with 4. |
| `mod-1238` | Replace "f" with "5" | Replaces every f with 5. |
| `mod-1239` | Replace "f" with "6" | Replaces every f with 6. |
| `mod-1240` | Replace "f" with "7" | Replaces every f with 7. |
| `mod-1241` | Replace "f" with "8" | Replaces every f with 8. |
| `mod-1242` | Replace "f" with "9" | Replaces every f with 9. |
| `mod-1243` | Replace "g" with "a" | Replaces every g with a. |
| `mod-1244` | Replace "g" with "b" | Replaces every g with b. |
| `mod-1245` | Replace "g" with "c" | Replaces every g with c. |
| `mod-1246` | Replace "g" with "d" | Replaces every g with d. |
| `mod-1247` | Replace "g" with "e" | Replaces every g with e. |
| `mod-1248` | Replace "g" with "f" | Replaces every g with f. |
| `mod-1249` | Replace "g" with "h" | Replaces every g with h. |
| `mod-1250` | Replace "g" with "i" | Replaces every g with i. |
| `mod-1251` | Replace "g" with "j" | Replaces every g with j. |
| `mod-1252` | Replace "g" with "k" | Replaces every g with k. |
| `mod-1253` | Replace "g" with "l" | Replaces every g with l. |
| `mod-1254` | Replace "g" with "m" | Replaces every g with m. |
| `mod-1255` | Replace "g" with "n" | Replaces every g with n. |
| `mod-1256` | Replace "g" with "o" | Replaces every g with o. |
| `mod-1257` | Replace "g" with "p" | Replaces every g with p. |
| `mod-1258` | Replace "g" with "q" | Replaces every g with q. |
| `mod-1259` | Replace "g" with "r" | Replaces every g with r. |
| `mod-1260` | Replace "g" with "s" | Replaces every g with s. |
| `mod-1261` | Replace "g" with "t" | Replaces every g with t. |
| `mod-1262` | Replace "g" with "u" | Replaces every g with u. |
| `mod-1263` | Replace "g" with "v" | Replaces every g with v. |
| `mod-1264` | Replace "g" with "w" | Replaces every g with w. |
| `mod-1265` | Replace "g" with "x" | Replaces every g with x. |
| `mod-1266` | Replace "g" with "y" | Replaces every g with y. |
| `mod-1267` | Replace "g" with "z" | Replaces every g with z. |
| `mod-1268` | Replace "g" with "A" | Replaces every g with A. |
| `mod-1269` | Replace "g" with "B" | Replaces every g with B. |
| `mod-1270` | Replace "g" with "C" | Replaces every g with C. |
| `mod-1271` | Replace "g" with "D" | Replaces every g with D. |
| `mod-1272` | Replace "g" with "E" | Replaces every g with E. |
| `mod-1273` | Replace "g" with "F" | Replaces every g with F. |
| `mod-1274` | Replace "g" with "G" | Replaces every g with G. |
| `mod-1275` | Replace "g" with "H" | Replaces every g with H. |
| `mod-1276` | Replace "g" with "I" | Replaces every g with I. |
| `mod-1277` | Replace "g" with "J" | Replaces every g with J. |
| `mod-1278` | Replace "g" with "K" | Replaces every g with K. |
| `mod-1279` | Replace "g" with "L" | Replaces every g with L. |
| `mod-1280` | Replace "g" with "M" | Replaces every g with M. |
| `mod-1281` | Replace "g" with "N" | Replaces every g with N. |
| `mod-1282` | Replace "g" with "O" | Replaces every g with O. |
| `mod-1283` | Replace "g" with "P" | Replaces every g with P. |
| `mod-1284` | Replace "g" with "Q" | Replaces every g with Q. |
| `mod-1285` | Replace "g" with "R" | Replaces every g with R. |
| `mod-1286` | Replace "g" with "S" | Replaces every g with S. |
| `mod-1287` | Replace "g" with "T" | Replaces every g with T. |
| `mod-1288` | Replace "g" with "U" | Replaces every g with U. |
| `mod-1289` | Replace "g" with "V" | Replaces every g with V. |
| `mod-1290` | Replace "g" with "W" | Replaces every g with W. |
| `mod-1291` | Replace "g" with "X" | Replaces every g with X. |
| `mod-1292` | Replace "g" with "Y" | Replaces every g with Y. |
| `mod-1293` | Replace "g" with "Z" | Replaces every g with Z. |
| `mod-1294` | Replace "g" with "0" | Replaces every g with 0. |
| `mod-1295` | Replace "g" with "1" | Replaces every g with 1. |
| `mod-1296` | Replace "g" with "2" | Replaces every g with 2. |
| `mod-1297` | Replace "g" with "3" | Replaces every g with 3. |
| `mod-1298` | Replace "g" with "4" | Replaces every g with 4. |
| `mod-1299` | Replace "g" with "5" | Replaces every g with 5. |
| `mod-1300` | Replace "g" with "6" | Replaces every g with 6. |
| `mod-1301` | Replace "g" with "7" | Replaces every g with 7. |
| `mod-1302` | Replace "g" with "8" | Replaces every g with 8. |
| `mod-1303` | Replace "g" with "9" | Replaces every g with 9. |
| `mod-1304` | Replace "h" with "a" | Replaces every h with a. |
| `mod-1305` | Replace "h" with "b" | Replaces every h with b. |
| `mod-1306` | Replace "h" with "c" | Replaces every h with c. |
| `mod-1307` | Replace "h" with "d" | Replaces every h with d. |
| `mod-1308` | Replace "h" with "e" | Replaces every h with e. |
| `mod-1309` | Replace "h" with "f" | Replaces every h with f. |
| `mod-1310` | Replace "h" with "g" | Replaces every h with g. |
| `mod-1311` | Replace "h" with "i" | Replaces every h with i. |
| `mod-1312` | Replace "h" with "j" | Replaces every h with j. |
| `mod-1313` | Replace "h" with "k" | Replaces every h with k. |
| `mod-1314` | Replace "h" with "l" | Replaces every h with l. |
| `mod-1315` | Replace "h" with "m" | Replaces every h with m. |
| `mod-1316` | Replace "h" with "n" | Replaces every h with n. |
| `mod-1317` | Replace "h" with "o" | Replaces every h with o. |
| `mod-1318` | Replace "h" with "p" | Replaces every h with p. |
| `mod-1319` | Replace "h" with "q" | Replaces every h with q. |
| `mod-1320` | Replace "h" with "r" | Replaces every h with r. |
| `mod-1321` | Replace "h" with "s" | Replaces every h with s. |
| `mod-1322` | Replace "h" with "t" | Replaces every h with t. |
| `mod-1323` | Replace "h" with "u" | Replaces every h with u. |
| `mod-1324` | Replace "h" with "v" | Replaces every h with v. |
| `mod-1325` | Replace "h" with "w" | Replaces every h with w. |
| `mod-1326` | Replace "h" with "x" | Replaces every h with x. |
| `mod-1327` | Replace "h" with "y" | Replaces every h with y. |
| `mod-1328` | Replace "h" with "z" | Replaces every h with z. |
| `mod-1329` | Replace "h" with "A" | Replaces every h with A. |
| `mod-1330` | Replace "h" with "B" | Replaces every h with B. |
| `mod-1331` | Replace "h" with "C" | Replaces every h with C. |
| `mod-1332` | Replace "h" with "D" | Replaces every h with D. |
| `mod-1333` | Replace "h" with "E" | Replaces every h with E. |
| `mod-1334` | Replace "h" with "F" | Replaces every h with F. |
| `mod-1335` | Replace "h" with "G" | Replaces every h with G. |
| `mod-1336` | Replace "h" with "H" | Replaces every h with H. |
| `mod-1337` | Replace "h" with "I" | Replaces every h with I. |
| `mod-1338` | Replace "h" with "J" | Replaces every h with J. |
| `mod-1339` | Replace "h" with "K" | Replaces every h with K. |
| `mod-1340` | Replace "h" with "L" | Replaces every h with L. |
| `mod-1341` | Replace "h" with "M" | Replaces every h with M. |
| `mod-1342` | Replace "h" with "N" | Replaces every h with N. |
| `mod-1343` | Replace "h" with "O" | Replaces every h with O. |
| `mod-1344` | Replace "h" with "P" | Replaces every h with P. |
| `mod-1345` | Replace "h" with "Q" | Replaces every h with Q. |
| `mod-1346` | Replace "h" with "R" | Replaces every h with R. |
| `mod-1347` | Replace "h" with "S" | Replaces every h with S. |
| `mod-1348` | Replace "h" with "T" | Replaces every h with T. |
| `mod-1349` | Replace "h" with "U" | Replaces every h with U. |
| `mod-1350` | Replace "h" with "V" | Replaces every h with V. |
| `mod-1351` | Replace "h" with "W" | Replaces every h with W. |
| `mod-1352` | Replace "h" with "X" | Replaces every h with X. |
| `mod-1353` | Replace "h" with "Y" | Replaces every h with Y. |
| `mod-1354` | Replace "h" with "Z" | Replaces every h with Z. |
| `mod-1355` | Replace "h" with "0" | Replaces every h with 0. |
| `mod-1356` | Replace "h" with "1" | Replaces every h with 1. |
| `mod-1357` | Replace "h" with "2" | Replaces every h with 2. |
| `mod-1358` | Replace "h" with "3" | Replaces every h with 3. |
| `mod-1359` | Replace "h" with "4" | Replaces every h with 4. |
| `mod-1360` | Replace "h" with "5" | Replaces every h with 5. |
| `mod-1361` | Replace "h" with "6" | Replaces every h with 6. |
| `mod-1362` | Replace "h" with "7" | Replaces every h with 7. |
| `mod-1363` | Replace "h" with "8" | Replaces every h with 8. |
| `mod-1364` | Replace "h" with "9" | Replaces every h with 9. |
| `mod-1365` | Replace "i" with "a" | Replaces every i with a. |
| `mod-1366` | Replace "i" with "b" | Replaces every i with b. |
| `mod-1367` | Replace "i" with "c" | Replaces every i with c. |
| `mod-1368` | Replace "i" with "d" | Replaces every i with d. |
| `mod-1369` | Replace "i" with "e" | Replaces every i with e. |
| `mod-1370` | Replace "i" with "f" | Replaces every i with f. |
| `mod-1371` | Replace "i" with "g" | Replaces every i with g. |
| `mod-1372` | Replace "i" with "h" | Replaces every i with h. |
| `mod-1373` | Replace "i" with "j" | Replaces every i with j. |
| `mod-1374` | Replace "i" with "k" | Replaces every i with k. |
| `mod-1375` | Replace "i" with "l" | Replaces every i with l. |
| `mod-1376` | Replace "i" with "m" | Replaces every i with m. |
| `mod-1377` | Replace "i" with "n" | Replaces every i with n. |
| `mod-1378` | Replace "i" with "o" | Replaces every i with o. |
| `mod-1379` | Replace "i" with "p" | Replaces every i with p. |
| `mod-1380` | Replace "i" with "q" | Replaces every i with q. |
| `mod-1381` | Replace "i" with "r" | Replaces every i with r. |
| `mod-1382` | Replace "i" with "s" | Replaces every i with s. |
| `mod-1383` | Replace "i" with "t" | Replaces every i with t. |
| `mod-1384` | Replace "i" with "u" | Replaces every i with u. |
| `mod-1385` | Replace "i" with "v" | Replaces every i with v. |
| `mod-1386` | Replace "i" with "w" | Replaces every i with w. |
| `mod-1387` | Replace "i" with "x" | Replaces every i with x. |
| `mod-1388` | Replace "i" with "y" | Replaces every i with y. |
| `mod-1389` | Replace "i" with "z" | Replaces every i with z. |
| `mod-1390` | Replace "i" with "A" | Replaces every i with A. |
| `mod-1391` | Replace "i" with "B" | Replaces every i with B. |
| `mod-1392` | Replace "i" with "C" | Replaces every i with C. |
| `mod-1393` | Replace "i" with "D" | Replaces every i with D. |
| `mod-1394` | Replace "i" with "E" | Replaces every i with E. |
| `mod-1395` | Replace "i" with "F" | Replaces every i with F. |
| `mod-1396` | Replace "i" with "G" | Replaces every i with G. |
| `mod-1397` | Replace "i" with "H" | Replaces every i with H. |
| `mod-1398` | Replace "i" with "I" | Replaces every i with I. |
| `mod-1399` | Replace "i" with "J" | Replaces every i with J. |
| `mod-1400` | Replace "i" with "K" | Replaces every i with K. |
| `mod-1401` | Replace "i" with "L" | Replaces every i with L. |
| `mod-1402` | Replace "i" with "M" | Replaces every i with M. |
| `mod-1403` | Replace "i" with "N" | Replaces every i with N. |
| `mod-1404` | Replace "i" with "O" | Replaces every i with O. |
| `mod-1405` | Replace "i" with "P" | Replaces every i with P. |
| `mod-1406` | Replace "i" with "Q" | Replaces every i with Q. |
| `mod-1407` | Replace "i" with "R" | Replaces every i with R. |
| `mod-1408` | Replace "i" with "S" | Replaces every i with S. |
| `mod-1409` | Replace "i" with "T" | Replaces every i with T. |
| `mod-1410` | Replace "i" with "U" | Replaces every i with U. |
| `mod-1411` | Replace "i" with "V" | Replaces every i with V. |
| `mod-1412` | Replace "i" with "W" | Replaces every i with W. |
| `mod-1413` | Replace "i" with "X" | Replaces every i with X. |
| `mod-1414` | Replace "i" with "Y" | Replaces every i with Y. |
| `mod-1415` | Replace "i" with "Z" | Replaces every i with Z. |
| `mod-1416` | Replace "i" with "0" | Replaces every i with 0. |
| `mod-1417` | Replace "i" with "1" | Replaces every i with 1. |
| `mod-1418` | Replace "i" with "2" | Replaces every i with 2. |
| `mod-1419` | Replace "i" with "3" | Replaces every i with 3. |
| `mod-1420` | Replace "i" with "4" | Replaces every i with 4. |
| `mod-1421` | Replace "i" with "5" | Replaces every i with 5. |
| `mod-1422` | Replace "i" with "6" | Replaces every i with 6. |
| `mod-1423` | Replace "i" with "7" | Replaces every i with 7. |
| `mod-1424` | Replace "i" with "8" | Replaces every i with 8. |
| `mod-1425` | Replace "i" with "9" | Replaces every i with 9. |
| `mod-1426` | Replace "j" with "a" | Replaces every j with a. |
| `mod-1427` | Replace "j" with "b" | Replaces every j with b. |
| `mod-1428` | Replace "j" with "c" | Replaces every j with c. |
| `mod-1429` | Replace "j" with "d" | Replaces every j with d. |
| `mod-1430` | Replace "j" with "e" | Replaces every j with e. |
| `mod-1431` | Replace "j" with "f" | Replaces every j with f. |
| `mod-1432` | Replace "j" with "g" | Replaces every j with g. |
| `mod-1433` | Replace "j" with "h" | Replaces every j with h. |
| `mod-1434` | Replace "j" with "i" | Replaces every j with i. |
| `mod-1435` | Replace "j" with "k" | Replaces every j with k. |
| `mod-1436` | Replace "j" with "l" | Replaces every j with l. |
| `mod-1437` | Replace "j" with "m" | Replaces every j with m. |
| `mod-1438` | Replace "j" with "n" | Replaces every j with n. |
| `mod-1439` | Replace "j" with "o" | Replaces every j with o. |
| `mod-1440` | Replace "j" with "p" | Replaces every j with p. |
| `mod-1441` | Replace "j" with "q" | Replaces every j with q. |
| `mod-1442` | Replace "j" with "r" | Replaces every j with r. |
| `mod-1443` | Replace "j" with "s" | Replaces every j with s. |
| `mod-1444` | Replace "j" with "t" | Replaces every j with t. |
| `mod-1445` | Replace "j" with "u" | Replaces every j with u. |
| `mod-1446` | Replace "j" with "v" | Replaces every j with v. |
| `mod-1447` | Replace "j" with "w" | Replaces every j with w. |
| `mod-1448` | Replace "j" with "x" | Replaces every j with x. |
| `mod-1449` | Replace "j" with "y" | Replaces every j with y. |
| `mod-1450` | Replace "j" with "z" | Replaces every j with z. |
| `mod-1451` | Replace "j" with "A" | Replaces every j with A. |
| `mod-1452` | Replace "j" with "B" | Replaces every j with B. |
| `mod-1453` | Replace "j" with "C" | Replaces every j with C. |
| `mod-1454` | Replace "j" with "D" | Replaces every j with D. |
| `mod-1455` | Replace "j" with "E" | Replaces every j with E. |
| `mod-1456` | Replace "j" with "F" | Replaces every j with F. |
| `mod-1457` | Replace "j" with "G" | Replaces every j with G. |
| `mod-1458` | Replace "j" with "H" | Replaces every j with H. |
| `mod-1459` | Replace "j" with "I" | Replaces every j with I. |
| `mod-1460` | Replace "j" with "J" | Replaces every j with J. |
| `mod-1461` | Replace "j" with "K" | Replaces every j with K. |
| `mod-1462` | Replace "j" with "L" | Replaces every j with L. |
| `mod-1463` | Replace "j" with "M" | Replaces every j with M. |
| `mod-1464` | Replace "j" with "N" | Replaces every j with N. |
| `mod-1465` | Replace "j" with "O" | Replaces every j with O. |
| `mod-1466` | Replace "j" with "P" | Replaces every j with P. |
| `mod-1467` | Replace "j" with "Q" | Replaces every j with Q. |
| `mod-1468` | Replace "j" with "R" | Replaces every j with R. |
| `mod-1469` | Replace "j" with "S" | Replaces every j with S. |
| `mod-1470` | Replace "j" with "T" | Replaces every j with T. |
| `mod-1471` | Replace "j" with "U" | Replaces every j with U. |
| `mod-1472` | Replace "j" with "V" | Replaces every j with V. |
| `mod-1473` | Replace "j" with "W" | Replaces every j with W. |
| `mod-1474` | Replace "j" with "X" | Replaces every j with X. |
| `mod-1475` | Replace "j" with "Y" | Replaces every j with Y. |
| `mod-1476` | Replace "j" with "Z" | Replaces every j with Z. |
| `mod-1477` | Replace "j" with "0" | Replaces every j with 0. |
| `mod-1478` | Replace "j" with "1" | Replaces every j with 1. |
| `mod-1479` | Replace "j" with "2" | Replaces every j with 2. |
| `mod-1480` | Replace "j" with "3" | Replaces every j with 3. |
| `mod-1481` | Replace "j" with "4" | Replaces every j with 4. |
| `mod-1482` | Replace "j" with "5" | Replaces every j with 5. |
| `mod-1483` | Replace "j" with "6" | Replaces every j with 6. |
| `mod-1484` | Replace "j" with "7" | Replaces every j with 7. |
| `mod-1485` | Replace "j" with "8" | Replaces every j with 8. |
| `mod-1486` | Replace "j" with "9" | Replaces every j with 9. |
| `mod-1487` | Replace "k" with "a" | Replaces every k with a. |
| `mod-1488` | Replace "k" with "b" | Replaces every k with b. |
| `mod-1489` | Replace "k" with "c" | Replaces every k with c. |
| `mod-1490` | Replace "k" with "d" | Replaces every k with d. |
| `mod-1491` | Replace "k" with "e" | Replaces every k with e. |
| `mod-1492` | Replace "k" with "f" | Replaces every k with f. |
| `mod-1493` | Replace "k" with "g" | Replaces every k with g. |
| `mod-1494` | Replace "k" with "h" | Replaces every k with h. |
| `mod-1495` | Replace "k" with "i" | Replaces every k with i. |
| `mod-1496` | Replace "k" with "j" | Replaces every k with j. |
| `mod-1497` | Replace "k" with "l" | Replaces every k with l. |
| `mod-1498` | Replace "k" with "m" | Replaces every k with m. |
| `mod-1499` | Replace "k" with "n" | Replaces every k with n. |
| `mod-1500` | Replace "k" with "o" | Replaces every k with o. |
| `mod-1501` | Replace "k" with "p" | Replaces every k with p. |
| `mod-1502` | Replace "k" with "q" | Replaces every k with q. |
| `mod-1503` | Replace "k" with "r" | Replaces every k with r. |
| `mod-1504` | Replace "k" with "s" | Replaces every k with s. |
| `mod-1505` | Replace "k" with "t" | Replaces every k with t. |
| `mod-1506` | Replace "k" with "u" | Replaces every k with u. |
| `mod-1507` | Replace "k" with "v" | Replaces every k with v. |
| `mod-1508` | Replace "k" with "w" | Replaces every k with w. |
| `mod-1509` | Replace "k" with "x" | Replaces every k with x. |
| `mod-1510` | Replace "k" with "y" | Replaces every k with y. |
| `mod-1511` | Replace "k" with "z" | Replaces every k with z. |
| `mod-1512` | Replace "k" with "A" | Replaces every k with A. |
| `mod-1513` | Replace "k" with "B" | Replaces every k with B. |
| `mod-1514` | Replace "k" with "C" | Replaces every k with C. |
| `mod-1515` | Replace "k" with "D" | Replaces every k with D. |
| `mod-1516` | Replace "k" with "E" | Replaces every k with E. |
| `mod-1517` | Replace "k" with "F" | Replaces every k with F. |
| `mod-1518` | Replace "k" with "G" | Replaces every k with G. |
| `mod-1519` | Replace "k" with "H" | Replaces every k with H. |
| `mod-1520` | Replace "k" with "I" | Replaces every k with I. |
| `mod-1521` | Replace "k" with "J" | Replaces every k with J. |
| `mod-1522` | Replace "k" with "K" | Replaces every k with K. |
| `mod-1523` | Replace "k" with "L" | Replaces every k with L. |
| `mod-1524` | Replace "k" with "M" | Replaces every k with M. |
| `mod-1525` | Replace "k" with "N" | Replaces every k with N. |
| `mod-1526` | Replace "k" with "O" | Replaces every k with O. |
| `mod-1527` | Replace "k" with "P" | Replaces every k with P. |
| `mod-1528` | Replace "k" with "Q" | Replaces every k with Q. |
| `mod-1529` | Replace "k" with "R" | Replaces every k with R. |
| `mod-1530` | Replace "k" with "S" | Replaces every k with S. |
| `mod-1531` | Replace "k" with "T" | Replaces every k with T. |
| `mod-1532` | Replace "k" with "U" | Replaces every k with U. |
| `mod-1533` | Replace "k" with "V" | Replaces every k with V. |
| `mod-1534` | Replace "k" with "W" | Replaces every k with W. |
| `mod-1535` | Replace "k" with "X" | Replaces every k with X. |
| `mod-1536` | Replace "k" with "Y" | Replaces every k with Y. |
| `mod-1537` | Replace "k" with "Z" | Replaces every k with Z. |
| `mod-1538` | Replace "k" with "0" | Replaces every k with 0. |
| `mod-1539` | Replace "k" with "1" | Replaces every k with 1. |
| `mod-1540` | Replace "k" with "2" | Replaces every k with 2. |
| `mod-1541` | Replace "k" with "3" | Replaces every k with 3. |
| `mod-1542` | Replace "k" with "4" | Replaces every k with 4. |
| `mod-1543` | Replace "k" with "5" | Replaces every k with 5. |
| `mod-1544` | Replace "k" with "6" | Replaces every k with 6. |
| `mod-1545` | Replace "k" with "7" | Replaces every k with 7. |
| `mod-1546` | Replace "k" with "8" | Replaces every k with 8. |
| `mod-1547` | Replace "k" with "9" | Replaces every k with 9. |
| `mod-1548` | Replace "l" with "a" | Replaces every l with a. |
| `mod-1549` | Replace "l" with "b" | Replaces every l with b. |
| `mod-1550` | Replace "l" with "c" | Replaces every l with c. |
| `mod-1551` | Replace "l" with "d" | Replaces every l with d. |
| `mod-1552` | Replace "l" with "e" | Replaces every l with e. |
| `mod-1553` | Replace "l" with "f" | Replaces every l with f. |
| `mod-1554` | Replace "l" with "g" | Replaces every l with g. |
| `mod-1555` | Replace "l" with "h" | Replaces every l with h. |
| `mod-1556` | Replace "l" with "i" | Replaces every l with i. |
| `mod-1557` | Replace "l" with "j" | Replaces every l with j. |
| `mod-1558` | Replace "l" with "k" | Replaces every l with k. |
| `mod-1559` | Replace "l" with "m" | Replaces every l with m. |
| `mod-1560` | Replace "l" with "n" | Replaces every l with n. |
| `mod-1561` | Replace "l" with "o" | Replaces every l with o. |
| `mod-1562` | Replace "l" with "p" | Replaces every l with p. |
| `mod-1563` | Replace "l" with "q" | Replaces every l with q. |
| `mod-1564` | Replace "l" with "r" | Replaces every l with r. |
| `mod-1565` | Replace "l" with "s" | Replaces every l with s. |
| `mod-1566` | Replace "l" with "t" | Replaces every l with t. |
| `mod-1567` | Replace "l" with "u" | Replaces every l with u. |
| `mod-1568` | Replace "l" with "v" | Replaces every l with v. |
| `mod-1569` | Replace "l" with "w" | Replaces every l with w. |
| `mod-1570` | Replace "l" with "x" | Replaces every l with x. |
| `mod-1571` | Replace "l" with "y" | Replaces every l with y. |
| `mod-1572` | Replace "l" with "z" | Replaces every l with z. |
| `mod-1573` | Replace "l" with "A" | Replaces every l with A. |
| `mod-1574` | Replace "l" with "B" | Replaces every l with B. |
| `mod-1575` | Replace "l" with "C" | Replaces every l with C. |
| `mod-1576` | Replace "l" with "D" | Replaces every l with D. |
| `mod-1577` | Replace "l" with "E" | Replaces every l with E. |
| `mod-1578` | Replace "l" with "F" | Replaces every l with F. |
| `mod-1579` | Replace "l" with "G" | Replaces every l with G. |
| `mod-1580` | Replace "l" with "H" | Replaces every l with H. |
| `mod-1581` | Replace "l" with "I" | Replaces every l with I. |
| `mod-1582` | Replace "l" with "J" | Replaces every l with J. |
| `mod-1583` | Replace "l" with "K" | Replaces every l with K. |
| `mod-1584` | Replace "l" with "L" | Replaces every l with L. |
| `mod-1585` | Replace "l" with "M" | Replaces every l with M. |
| `mod-1586` | Replace "l" with "N" | Replaces every l with N. |
| `mod-1587` | Replace "l" with "O" | Replaces every l with O. |
| `mod-1588` | Replace "l" with "P" | Replaces every l with P. |
| `mod-1589` | Replace "l" with "Q" | Replaces every l with Q. |
| `mod-1590` | Replace "l" with "R" | Replaces every l with R. |
| `mod-1591` | Replace "l" with "S" | Replaces every l with S. |
| `mod-1592` | Replace "l" with "T" | Replaces every l with T. |
| `mod-1593` | Replace "l" with "U" | Replaces every l with U. |
| `mod-1594` | Replace "l" with "V" | Replaces every l with V. |
| `mod-1595` | Replace "l" with "W" | Replaces every l with W. |
| `mod-1596` | Replace "l" with "X" | Replaces every l with X. |
| `mod-1597` | Replace "l" with "Y" | Replaces every l with Y. |
| `mod-1598` | Replace "l" with "Z" | Replaces every l with Z. |
| `mod-1599` | Replace "l" with "0" | Replaces every l with 0. |
| `mod-1600` | Replace "l" with "1" | Replaces every l with 1. |
| `mod-1601` | Replace "l" with "2" | Replaces every l with 2. |
| `mod-1602` | Replace "l" with "3" | Replaces every l with 3. |
| `mod-1603` | Replace "l" with "4" | Replaces every l with 4. |
| `mod-1604` | Replace "l" with "5" | Replaces every l with 5. |
| `mod-1605` | Replace "l" with "6" | Replaces every l with 6. |
| `mod-1606` | Replace "l" with "7" | Replaces every l with 7. |
| `mod-1607` | Replace "l" with "8" | Replaces every l with 8. |
| `mod-1608` | Replace "l" with "9" | Replaces every l with 9. |
| `mod-1609` | Replace "m" with "a" | Replaces every m with a. |
| `mod-1610` | Replace "m" with "b" | Replaces every m with b. |
| `mod-1611` | Replace "m" with "c" | Replaces every m with c. |
| `mod-1612` | Replace "m" with "d" | Replaces every m with d. |
| `mod-1613` | Replace "m" with "e" | Replaces every m with e. |
| `mod-1614` | Replace "m" with "f" | Replaces every m with f. |
| `mod-1615` | Replace "m" with "g" | Replaces every m with g. |
| `mod-1616` | Replace "m" with "h" | Replaces every m with h. |
| `mod-1617` | Replace "m" with "i" | Replaces every m with i. |
| `mod-1618` | Replace "m" with "j" | Replaces every m with j. |
| `mod-1619` | Replace "m" with "k" | Replaces every m with k. |
| `mod-1620` | Replace "m" with "l" | Replaces every m with l. |
| `mod-1621` | Replace "m" with "n" | Replaces every m with n. |
| `mod-1622` | Replace "m" with "o" | Replaces every m with o. |
| `mod-1623` | Replace "m" with "p" | Replaces every m with p. |
| `mod-1624` | Replace "m" with "q" | Replaces every m with q. |
| `mod-1625` | Replace "m" with "r" | Replaces every m with r. |
| `mod-1626` | Replace "m" with "s" | Replaces every m with s. |
| `mod-1627` | Replace "m" with "t" | Replaces every m with t. |
| `mod-1628` | Replace "m" with "u" | Replaces every m with u. |
| `mod-1629` | Replace "m" with "v" | Replaces every m with v. |
| `mod-1630` | Replace "m" with "w" | Replaces every m with w. |
| `mod-1631` | Replace "m" with "x" | Replaces every m with x. |
| `mod-1632` | Replace "m" with "y" | Replaces every m with y. |
| `mod-1633` | Replace "m" with "z" | Replaces every m with z. |
| `mod-1634` | Replace "m" with "A" | Replaces every m with A. |
| `mod-1635` | Replace "m" with "B" | Replaces every m with B. |
| `mod-1636` | Replace "m" with "C" | Replaces every m with C. |
| `mod-1637` | Replace "m" with "D" | Replaces every m with D. |
| `mod-1638` | Replace "m" with "E" | Replaces every m with E. |
| `mod-1639` | Replace "m" with "F" | Replaces every m with F. |
| `mod-1640` | Replace "m" with "G" | Replaces every m with G. |
| `mod-1641` | Replace "m" with "H" | Replaces every m with H. |
| `mod-1642` | Replace "m" with "I" | Replaces every m with I. |
| `mod-1643` | Replace "m" with "J" | Replaces every m with J. |
| `mod-1644` | Replace "m" with "K" | Replaces every m with K. |
| `mod-1645` | Replace "m" with "L" | Replaces every m with L. |
| `mod-1646` | Replace "m" with "M" | Replaces every m with M. |
| `mod-1647` | Replace "m" with "N" | Replaces every m with N. |
| `mod-1648` | Replace "m" with "O" | Replaces every m with O. |
| `mod-1649` | Replace "m" with "P" | Replaces every m with P. |
| `mod-1650` | Replace "m" with "Q" | Replaces every m with Q. |
| `mod-1651` | Replace "m" with "R" | Replaces every m with R. |
| `mod-1652` | Replace "m" with "S" | Replaces every m with S. |
| `mod-1653` | Replace "m" with "T" | Replaces every m with T. |
| `mod-1654` | Replace "m" with "U" | Replaces every m with U. |
| `mod-1655` | Replace "m" with "V" | Replaces every m with V. |
| `mod-1656` | Replace "m" with "W" | Replaces every m with W. |
| `mod-1657` | Replace "m" with "X" | Replaces every m with X. |
| `mod-1658` | Replace "m" with "Y" | Replaces every m with Y. |
| `mod-1659` | Replace "m" with "Z" | Replaces every m with Z. |
| `mod-1660` | Replace "m" with "0" | Replaces every m with 0. |
| `mod-1661` | Replace "m" with "1" | Replaces every m with 1. |
| `mod-1662` | Replace "m" with "2" | Replaces every m with 2. |
| `mod-1663` | Replace "m" with "3" | Replaces every m with 3. |
| `mod-1664` | Replace "m" with "4" | Replaces every m with 4. |
| `mod-1665` | Replace "m" with "5" | Replaces every m with 5. |
| `mod-1666` | Replace "m" with "6" | Replaces every m with 6. |
| `mod-1667` | Replace "m" with "7" | Replaces every m with 7. |
| `mod-1668` | Replace "m" with "8" | Replaces every m with 8. |
| `mod-1669` | Replace "m" with "9" | Replaces every m with 9. |
| `mod-1670` | Replace "n" with "a" | Replaces every n with a. |
| `mod-1671` | Replace "n" with "b" | Replaces every n with b. |
| `mod-1672` | Replace "n" with "c" | Replaces every n with c. |
| `mod-1673` | Replace "n" with "d" | Replaces every n with d. |
| `mod-1674` | Replace "n" with "e" | Replaces every n with e. |
| `mod-1675` | Replace "n" with "f" | Replaces every n with f. |
| `mod-1676` | Replace "n" with "g" | Replaces every n with g. |
| `mod-1677` | Replace "n" with "h" | Replaces every n with h. |
| `mod-1678` | Replace "n" with "i" | Replaces every n with i. |
| `mod-1679` | Replace "n" with "j" | Replaces every n with j. |
| `mod-1680` | Replace "n" with "k" | Replaces every n with k. |
| `mod-1681` | Replace "n" with "l" | Replaces every n with l. |
| `mod-1682` | Replace "n" with "m" | Replaces every n with m. |
| `mod-1683` | Replace "n" with "o" | Replaces every n with o. |
| `mod-1684` | Replace "n" with "p" | Replaces every n with p. |
| `mod-1685` | Replace "n" with "q" | Replaces every n with q. |
| `mod-1686` | Replace "n" with "r" | Replaces every n with r. |
| `mod-1687` | Replace "n" with "s" | Replaces every n with s. |
| `mod-1688` | Replace "n" with "t" | Replaces every n with t. |
| `mod-1689` | Replace "n" with "u" | Replaces every n with u. |
| `mod-1690` | Replace "n" with "v" | Replaces every n with v. |
| `mod-1691` | Replace "n" with "w" | Replaces every n with w. |
| `mod-1692` | Replace "n" with "x" | Replaces every n with x. |
| `mod-1693` | Replace "n" with "y" | Replaces every n with y. |
| `mod-1694` | Replace "n" with "z" | Replaces every n with z. |
| `mod-1695` | Replace "n" with "A" | Replaces every n with A. |
| `mod-1696` | Replace "n" with "B" | Replaces every n with B. |
| `mod-1697` | Replace "n" with "C" | Replaces every n with C. |
| `mod-1698` | Replace "n" with "D" | Replaces every n with D. |
| `mod-1699` | Replace "n" with "E" | Replaces every n with E. |
| `mod-1700` | Replace "n" with "F" | Replaces every n with F. |
| `mod-1701` | Replace "n" with "G" | Replaces every n with G. |
| `mod-1702` | Replace "n" with "H" | Replaces every n with H. |
| `mod-1703` | Replace "n" with "I" | Replaces every n with I. |
| `mod-1704` | Replace "n" with "J" | Replaces every n with J. |
| `mod-1705` | Replace "n" with "K" | Replaces every n with K. |
| `mod-1706` | Replace "n" with "L" | Replaces every n with L. |
| `mod-1707` | Replace "n" with "M" | Replaces every n with M. |
| `mod-1708` | Replace "n" with "N" | Replaces every n with N. |
| `mod-1709` | Replace "n" with "O" | Replaces every n with O. |
| `mod-1710` | Replace "n" with "P" | Replaces every n with P. |
| `mod-1711` | Replace "n" with "Q" | Replaces every n with Q. |
| `mod-1712` | Replace "n" with "R" | Replaces every n with R. |
| `mod-1713` | Replace "n" with "S" | Replaces every n with S. |
| `mod-1714` | Replace "n" with "T" | Replaces every n with T. |
| `mod-1715` | Replace "n" with "U" | Replaces every n with U. |
| `mod-1716` | Replace "n" with "V" | Replaces every n with V. |
| `mod-1717` | Replace "n" with "W" | Replaces every n with W. |
| `mod-1718` | Replace "n" with "X" | Replaces every n with X. |
| `mod-1719` | Replace "n" with "Y" | Replaces every n with Y. |
| `mod-1720` | Replace "n" with "Z" | Replaces every n with Z. |
| `mod-1721` | Replace "n" with "0" | Replaces every n with 0. |
| `mod-1722` | Replace "n" with "1" | Replaces every n with 1. |
| `mod-1723` | Replace "n" with "2" | Replaces every n with 2. |
| `mod-1724` | Replace "n" with "3" | Replaces every n with 3. |
| `mod-1725` | Replace "n" with "4" | Replaces every n with 4. |
| `mod-1726` | Replace "n" with "5" | Replaces every n with 5. |
| `mod-1727` | Replace "n" with "6" | Replaces every n with 6. |
| `mod-1728` | Replace "n" with "7" | Replaces every n with 7. |
| `mod-1729` | Replace "n" with "8" | Replaces every n with 8. |
| `mod-1730` | Replace "n" with "9" | Replaces every n with 9. |
| `mod-1731` | Replace "o" with "a" | Replaces every o with a. |
| `mod-1732` | Replace "o" with "b" | Replaces every o with b. |
| `mod-1733` | Replace "o" with "c" | Replaces every o with c. |
| `mod-1734` | Replace "o" with "d" | Replaces every o with d. |
| `mod-1735` | Replace "o" with "e" | Replaces every o with e. |
| `mod-1736` | Replace "o" with "f" | Replaces every o with f. |
| `mod-1737` | Replace "o" with "g" | Replaces every o with g. |
| `mod-1738` | Replace "o" with "h" | Replaces every o with h. |
| `mod-1739` | Replace "o" with "i" | Replaces every o with i. |
| `mod-1740` | Replace "o" with "j" | Replaces every o with j. |
| `mod-1741` | Replace "o" with "k" | Replaces every o with k. |
| `mod-1742` | Replace "o" with "l" | Replaces every o with l. |
| `mod-1743` | Replace "o" with "m" | Replaces every o with m. |
| `mod-1744` | Replace "o" with "n" | Replaces every o with n. |
| `mod-1745` | Replace "o" with "p" | Replaces every o with p. |
| `mod-1746` | Replace "o" with "q" | Replaces every o with q. |
| `mod-1747` | Replace "o" with "r" | Replaces every o with r. |
| `mod-1748` | Replace "o" with "s" | Replaces every o with s. |
| `mod-1749` | Replace "o" with "t" | Replaces every o with t. |
| `mod-1750` | Replace "o" with "u" | Replaces every o with u. |
| `mod-1751` | Replace "o" with "v" | Replaces every o with v. |
| `mod-1752` | Replace "o" with "w" | Replaces every o with w. |
| `mod-1753` | Replace "o" with "x" | Replaces every o with x. |
| `mod-1754` | Replace "o" with "y" | Replaces every o with y. |
| `mod-1755` | Replace "o" with "z" | Replaces every o with z. |
| `mod-1756` | Replace "o" with "A" | Replaces every o with A. |
| `mod-1757` | Replace "o" with "B" | Replaces every o with B. |
| `mod-1758` | Replace "o" with "C" | Replaces every o with C. |
| `mod-1759` | Replace "o" with "D" | Replaces every o with D. |
| `mod-1760` | Replace "o" with "E" | Replaces every o with E. |
| `mod-1761` | Replace "o" with "F" | Replaces every o with F. |
| `mod-1762` | Replace "o" with "G" | Replaces every o with G. |
| `mod-1763` | Replace "o" with "H" | Replaces every o with H. |
| `mod-1764` | Replace "o" with "I" | Replaces every o with I. |
| `mod-1765` | Replace "o" with "J" | Replaces every o with J. |
| `mod-1766` | Replace "o" with "K" | Replaces every o with K. |
| `mod-1767` | Replace "o" with "L" | Replaces every o with L. |
| `mod-1768` | Replace "o" with "M" | Replaces every o with M. |
| `mod-1769` | Replace "o" with "N" | Replaces every o with N. |
| `mod-1770` | Replace "o" with "O" | Replaces every o with O. |
| `mod-1771` | Replace "o" with "P" | Replaces every o with P. |
| `mod-1772` | Replace "o" with "Q" | Replaces every o with Q. |
| `mod-1773` | Replace "o" with "R" | Replaces every o with R. |
| `mod-1774` | Replace "o" with "S" | Replaces every o with S. |
| `mod-1775` | Replace "o" with "T" | Replaces every o with T. |
| `mod-1776` | Replace "o" with "U" | Replaces every o with U. |
| `mod-1777` | Replace "o" with "V" | Replaces every o with V. |
| `mod-1778` | Replace "o" with "W" | Replaces every o with W. |
| `mod-1779` | Replace "o" with "X" | Replaces every o with X. |
| `mod-1780` | Replace "o" with "Y" | Replaces every o with Y. |
| `mod-1781` | Replace "o" with "Z" | Replaces every o with Z. |
| `mod-1782` | Replace "o" with "0" | Replaces every o with 0. |
| `mod-1783` | Replace "o" with "1" | Replaces every o with 1. |
| `mod-1784` | Replace "o" with "2" | Replaces every o with 2. |
| `mod-1785` | Replace "o" with "3" | Replaces every o with 3. |
| `mod-1786` | Replace "o" with "4" | Replaces every o with 4. |
| `mod-1787` | Replace "o" with "5" | Replaces every o with 5. |
| `mod-1788` | Replace "o" with "6" | Replaces every o with 6. |
| `mod-1789` | Replace "o" with "7" | Replaces every o with 7. |
| `mod-1790` | Replace "o" with "8" | Replaces every o with 8. |
| `mod-1791` | Replace "o" with "9" | Replaces every o with 9. |
| `mod-1792` | Replace "p" with "a" | Replaces every p with a. |
| `mod-1793` | Replace "p" with "b" | Replaces every p with b. |
| `mod-1794` | Replace "p" with "c" | Replaces every p with c. |
| `mod-1795` | Replace "p" with "d" | Replaces every p with d. |
| `mod-1796` | Replace "p" with "e" | Replaces every p with e. |
| `mod-1797` | Replace "p" with "f" | Replaces every p with f. |
| `mod-1798` | Replace "p" with "g" | Replaces every p with g. |
| `mod-1799` | Replace "p" with "h" | Replaces every p with h. |
| `mod-1800` | Replace "p" with "i" | Replaces every p with i. |
| `mod-1801` | Replace "p" with "j" | Replaces every p with j. |
| `mod-1802` | Replace "p" with "k" | Replaces every p with k. |
| `mod-1803` | Replace "p" with "l" | Replaces every p with l. |
| `mod-1804` | Replace "p" with "m" | Replaces every p with m. |
| `mod-1805` | Replace "p" with "n" | Replaces every p with n. |
| `mod-1806` | Replace "p" with "o" | Replaces every p with o. |
| `mod-1807` | Replace "p" with "q" | Replaces every p with q. |
| `mod-1808` | Replace "p" with "r" | Replaces every p with r. |
| `mod-1809` | Replace "p" with "s" | Replaces every p with s. |
| `mod-1810` | Replace "p" with "t" | Replaces every p with t. |
| `mod-1811` | Replace "p" with "u" | Replaces every p with u. |
| `mod-1812` | Replace "p" with "v" | Replaces every p with v. |
| `mod-1813` | Replace "p" with "w" | Replaces every p with w. |
| `mod-1814` | Replace "p" with "x" | Replaces every p with x. |
| `mod-1815` | Replace "p" with "y" | Replaces every p with y. |
| `mod-1816` | Replace "p" with "z" | Replaces every p with z. |
| `mod-1817` | Replace "p" with "A" | Replaces every p with A. |
| `mod-1818` | Replace "p" with "B" | Replaces every p with B. |
| `mod-1819` | Replace "p" with "C" | Replaces every p with C. |
| `mod-1820` | Replace "p" with "D" | Replaces every p with D. |
| `mod-1821` | Replace "p" with "E" | Replaces every p with E. |
| `mod-1822` | Replace "p" with "F" | Replaces every p with F. |
| `mod-1823` | Replace "p" with "G" | Replaces every p with G. |
| `mod-1824` | Replace "p" with "H" | Replaces every p with H. |
| `mod-1825` | Replace "p" with "I" | Replaces every p with I. |
| `mod-1826` | Replace "p" with "J" | Replaces every p with J. |
| `mod-1827` | Replace "p" with "K" | Replaces every p with K. |
| `mod-1828` | Replace "p" with "L" | Replaces every p with L. |
| `mod-1829` | Replace "p" with "M" | Replaces every p with M. |
| `mod-1830` | Replace "p" with "N" | Replaces every p with N. |
| `mod-1831` | Replace "p" with "O" | Replaces every p with O. |
| `mod-1832` | Replace "p" with "P" | Replaces every p with P. |
| `mod-1833` | Replace "p" with "Q" | Replaces every p with Q. |
| `mod-1834` | Replace "p" with "R" | Replaces every p with R. |
| `mod-1835` | Replace "p" with "S" | Replaces every p with S. |
| `mod-1836` | Replace "p" with "T" | Replaces every p with T. |
| `mod-1837` | Replace "p" with "U" | Replaces every p with U. |
| `mod-1838` | Replace "p" with "V" | Replaces every p with V. |
| `mod-1839` | Replace "p" with "W" | Replaces every p with W. |
| `mod-1840` | Replace "p" with "X" | Replaces every p with X. |
| `mod-1841` | Replace "p" with "Y" | Replaces every p with Y. |
| `mod-1842` | Replace "p" with "Z" | Replaces every p with Z. |
| `mod-1843` | Replace "p" with "0" | Replaces every p with 0. |
| `mod-1844` | Replace "p" with "1" | Replaces every p with 1. |
| `mod-1845` | Replace "p" with "2" | Replaces every p with 2. |
| `mod-1846` | Replace "p" with "3" | Replaces every p with 3. |
| `mod-1847` | Replace "p" with "4" | Replaces every p with 4. |
| `mod-1848` | Replace "p" with "5" | Replaces every p with 5. |
| `mod-1849` | Replace "p" with "6" | Replaces every p with 6. |
| `mod-1850` | Replace "p" with "7" | Replaces every p with 7. |
| `mod-1851` | Replace "p" with "8" | Replaces every p with 8. |
| `mod-1852` | Replace "p" with "9" | Replaces every p with 9. |
| `mod-1853` | Replace "q" with "a" | Replaces every q with a. |
| `mod-1854` | Replace "q" with "b" | Replaces every q with b. |
| `mod-1855` | Replace "q" with "c" | Replaces every q with c. |
| `mod-1856` | Replace "q" with "d" | Replaces every q with d. |
| `mod-1857` | Replace "q" with "e" | Replaces every q with e. |
| `mod-1858` | Replace "q" with "f" | Replaces every q with f. |
| `mod-1859` | Replace "q" with "g" | Replaces every q with g. |
| `mod-1860` | Replace "q" with "h" | Replaces every q with h. |
| `mod-1861` | Replace "q" with "i" | Replaces every q with i. |
| `mod-1862` | Replace "q" with "j" | Replaces every q with j. |
| `mod-1863` | Replace "q" with "k" | Replaces every q with k. |
| `mod-1864` | Replace "q" with "l" | Replaces every q with l. |
| `mod-1865` | Replace "q" with "m" | Replaces every q with m. |
| `mod-1866` | Replace "q" with "n" | Replaces every q with n. |
| `mod-1867` | Replace "q" with "o" | Replaces every q with o. |
| `mod-1868` | Replace "q" with "p" | Replaces every q with p. |
| `mod-1869` | Replace "q" with "r" | Replaces every q with r. |
| `mod-1870` | Replace "q" with "s" | Replaces every q with s. |
| `mod-1871` | Replace "q" with "t" | Replaces every q with t. |
| `mod-1872` | Replace "q" with "u" | Replaces every q with u. |
| `mod-1873` | Replace "q" with "v" | Replaces every q with v. |
| `mod-1874` | Replace "q" with "w" | Replaces every q with w. |
| `mod-1875` | Replace "q" with "x" | Replaces every q with x. |
| `mod-1876` | Replace "q" with "y" | Replaces every q with y. |
| `mod-1877` | Replace "q" with "z" | Replaces every q with z. |
| `mod-1878` | Replace "q" with "A" | Replaces every q with A. |
| `mod-1879` | Replace "q" with "B" | Replaces every q with B. |
| `mod-1880` | Replace "q" with "C" | Replaces every q with C. |
| `mod-1881` | Replace "q" with "D" | Replaces every q with D. |
| `mod-1882` | Replace "q" with "E" | Replaces every q with E. |
| `mod-1883` | Replace "q" with "F" | Replaces every q with F. |
| `mod-1884` | Replace "q" with "G" | Replaces every q with G. |
| `mod-1885` | Replace "q" with "H" | Replaces every q with H. |
| `mod-1886` | Replace "q" with "I" | Replaces every q with I. |
| `mod-1887` | Replace "q" with "J" | Replaces every q with J. |
| `mod-1888` | Replace "q" with "K" | Replaces every q with K. |
| `mod-1889` | Replace "q" with "L" | Replaces every q with L. |
| `mod-1890` | Replace "q" with "M" | Replaces every q with M. |
| `mod-1891` | Replace "q" with "N" | Replaces every q with N. |
| `mod-1892` | Replace "q" with "O" | Replaces every q with O. |
| `mod-1893` | Replace "q" with "P" | Replaces every q with P. |
| `mod-1894` | Replace "q" with "Q" | Replaces every q with Q. |
| `mod-1895` | Replace "q" with "R" | Replaces every q with R. |
| `mod-1896` | Replace "q" with "S" | Replaces every q with S. |
| `mod-1897` | Replace "q" with "T" | Replaces every q with T. |
| `mod-1898` | Replace "q" with "U" | Replaces every q with U. |
| `mod-1899` | Replace "q" with "V" | Replaces every q with V. |
| `mod-1900` | Replace "q" with "W" | Replaces every q with W. |
| `mod-1901` | Replace "q" with "X" | Replaces every q with X. |
| `mod-1902` | Replace "q" with "Y" | Replaces every q with Y. |
| `mod-1903` | Replace "q" with "Z" | Replaces every q with Z. |
| `mod-1904` | Replace "q" with "0" | Replaces every q with 0. |
| `mod-1905` | Replace "q" with "1" | Replaces every q with 1. |
| `mod-1906` | Replace "q" with "2" | Replaces every q with 2. |
| `mod-1907` | Replace "q" with "3" | Replaces every q with 3. |
| `mod-1908` | Replace "q" with "4" | Replaces every q with 4. |
| `mod-1909` | Replace "q" with "5" | Replaces every q with 5. |
| `mod-1910` | Replace "q" with "6" | Replaces every q with 6. |
| `mod-1911` | Replace "q" with "7" | Replaces every q with 7. |
| `mod-1912` | Replace "q" with "8" | Replaces every q with 8. |
| `mod-1913` | Replace "q" with "9" | Replaces every q with 9. |
| `mod-1914` | Replace "r" with "a" | Replaces every r with a. |
| `mod-1915` | Replace "r" with "b" | Replaces every r with b. |
| `mod-1916` | Replace "r" with "c" | Replaces every r with c. |
| `mod-1917` | Replace "r" with "d" | Replaces every r with d. |
| `mod-1918` | Replace "r" with "e" | Replaces every r with e. |
| `mod-1919` | Replace "r" with "f" | Replaces every r with f. |
| `mod-1920` | Replace "r" with "g" | Replaces every r with g. |
| `mod-1921` | Replace "r" with "h" | Replaces every r with h. |
| `mod-1922` | Replace "r" with "i" | Replaces every r with i. |
| `mod-1923` | Replace "r" with "j" | Replaces every r with j. |
| `mod-1924` | Replace "r" with "k" | Replaces every r with k. |
| `mod-1925` | Replace "r" with "l" | Replaces every r with l. |
| `mod-1926` | Replace "r" with "m" | Replaces every r with m. |
| `mod-1927` | Replace "r" with "n" | Replaces every r with n. |
| `mod-1928` | Replace "r" with "o" | Replaces every r with o. |
| `mod-1929` | Replace "r" with "p" | Replaces every r with p. |
| `mod-1930` | Replace "r" with "q" | Replaces every r with q. |
| `mod-1931` | Replace "r" with "s" | Replaces every r with s. |
| `mod-1932` | Replace "r" with "t" | Replaces every r with t. |
| `mod-1933` | Replace "r" with "u" | Replaces every r with u. |
| `mod-1934` | Replace "r" with "v" | Replaces every r with v. |
| `mod-1935` | Replace "r" with "w" | Replaces every r with w. |
| `mod-1936` | Replace "r" with "x" | Replaces every r with x. |
| `mod-1937` | Replace "r" with "y" | Replaces every r with y. |
| `mod-1938` | Replace "r" with "z" | Replaces every r with z. |
| `mod-1939` | Replace "r" with "A" | Replaces every r with A. |
| `mod-1940` | Replace "r" with "B" | Replaces every r with B. |
| `mod-1941` | Replace "r" with "C" | Replaces every r with C. |
| `mod-1942` | Replace "r" with "D" | Replaces every r with D. |
| `mod-1943` | Replace "r" with "E" | Replaces every r with E. |
| `mod-1944` | Replace "r" with "F" | Replaces every r with F. |
| `mod-1945` | Replace "r" with "G" | Replaces every r with G. |
| `mod-1946` | Replace "r" with "H" | Replaces every r with H. |
| `mod-1947` | Replace "r" with "I" | Replaces every r with I. |
| `mod-1948` | Replace "r" with "J" | Replaces every r with J. |
| `mod-1949` | Replace "r" with "K" | Replaces every r with K. |
| `mod-1950` | Replace "r" with "L" | Replaces every r with L. |
| `mod-1951` | Replace "r" with "M" | Replaces every r with M. |
| `mod-1952` | Replace "r" with "N" | Replaces every r with N. |
| `mod-1953` | Replace "r" with "O" | Replaces every r with O. |
| `mod-1954` | Replace "r" with "P" | Replaces every r with P. |
| `mod-1955` | Replace "r" with "Q" | Replaces every r with Q. |
| `mod-1956` | Replace "r" with "R" | Replaces every r with R. |
| `mod-1957` | Replace "r" with "S" | Replaces every r with S. |
| `mod-1958` | Replace "r" with "T" | Replaces every r with T. |
| `mod-1959` | Replace "r" with "U" | Replaces every r with U. |
| `mod-1960` | Replace "r" with "V" | Replaces every r with V. |
| `mod-1961` | Replace "r" with "W" | Replaces every r with W. |
| `mod-1962` | Replace "r" with "X" | Replaces every r with X. |
| `mod-1963` | Replace "r" with "Y" | Replaces every r with Y. |
| `mod-1964` | Replace "r" with "Z" | Replaces every r with Z. |
| `mod-1965` | Replace "r" with "0" | Replaces every r with 0. |
| `mod-1966` | Replace "r" with "1" | Replaces every r with 1. |
| `mod-1967` | Replace "r" with "2" | Replaces every r with 2. |
| `mod-1968` | Replace "r" with "3" | Replaces every r with 3. |
| `mod-1969` | Replace "r" with "4" | Replaces every r with 4. |
| `mod-1970` | Replace "r" with "5" | Replaces every r with 5. |
| `mod-1971` | Replace "r" with "6" | Replaces every r with 6. |
| `mod-1972` | Replace "r" with "7" | Replaces every r with 7. |
| `mod-1973` | Replace "r" with "8" | Replaces every r with 8. |
| `mod-1974` | Replace "r" with "9" | Replaces every r with 9. |
| `mod-1975` | Replace "s" with "a" | Replaces every s with a. |
| `mod-1976` | Replace "s" with "b" | Replaces every s with b. |
| `mod-1977` | Replace "s" with "c" | Replaces every s with c. |
| `mod-14658` | Replace "s" with "d" | Replaces every s with d. |
| `mod-1979` | Replace "s" with "e" | Replaces every s with e. |
| `mod-1980` | Replace "s" with "f" | Replaces every s with f. |
| `mod-1981` | Replace "s" with "g" | Replaces every s with g. |
| `mod-1982` | Replace "s" with "h" | Replaces every s with h. |
| `mod-1983` | Replace "s" with "i" | Replaces every s with i. |
| `mod-1984` | Replace "s" with "j" | Replaces every s with j. |
| `mod-1985` | Replace "s" with "k" | Replaces every s with k. |
| `mod-1986` | Replace "s" with "l" | Replaces every s with l. |
| `mod-1987` | Replace "s" with "m" | Replaces every s with m. |
| `mod-1988` | Replace "s" with "n" | Replaces every s with n. |
| `mod-1989` | Replace "s" with "o" | Replaces every s with o. |
| `mod-1990` | Replace "s" with "p" | Replaces every s with p. |
| `mod-1991` | Replace "s" with "q" | Replaces every s with q. |
| `mod-1992` | Replace "s" with "r" | Replaces every s with r. |
| `mod-1993` | Replace "s" with "t" | Replaces every s with t. |
| `mod-1994` | Replace "s" with "u" | Replaces every s with u. |
| `mod-1995` | Replace "s" with "v" | Replaces every s with v. |
| `mod-1996` | Replace "s" with "w" | Replaces every s with w. |
| `mod-1997` | Replace "s" with "x" | Replaces every s with x. |
| `mod-1998` | Replace "s" with "y" | Replaces every s with y. |
| `mod-1999` | Replace "s" with "z" | Replaces every s with z. |
| `mod-2000` | Replace "s" with "A" | Replaces every s with A. |
| `mod-2001` | Replace "s" with "B" | Replaces every s with B. |
| `mod-2002` | Replace "s" with "C" | Replaces every s with C. |
| `mod-2003` | Replace "s" with "D" | Replaces every s with D. |
| `mod-2004` | Replace "s" with "E" | Replaces every s with E. |
| `mod-2005` | Replace "s" with "F" | Replaces every s with F. |
| `mod-2006` | Replace "s" with "G" | Replaces every s with G. |
| `mod-2007` | Replace "s" with "H" | Replaces every s with H. |
| `mod-2008` | Replace "s" with "I" | Replaces every s with I. |
| `mod-2009` | Replace "s" with "J" | Replaces every s with J. |
| `mod-2010` | Replace "s" with "K" | Replaces every s with K. |
| `mod-2011` | Replace "s" with "L" | Replaces every s with L. |
| `mod-2012` | Replace "s" with "M" | Replaces every s with M. |
| `mod-2013` | Replace "s" with "N" | Replaces every s with N. |
| `mod-2014` | Replace "s" with "O" | Replaces every s with O. |
| `mod-2015` | Replace "s" with "P" | Replaces every s with P. |
| `mod-2016` | Replace "s" with "Q" | Replaces every s with Q. |
| `mod-2017` | Replace "s" with "R" | Replaces every s with R. |
| `mod-2018` | Replace "s" with "S" | Replaces every s with S. |
| `mod-2019` | Replace "s" with "T" | Replaces every s with T. |
| `mod-2020` | Replace "s" with "U" | Replaces every s with U. |
| `mod-2021` | Replace "s" with "V" | Replaces every s with V. |
| `mod-2022` | Replace "s" with "W" | Replaces every s with W. |
| `mod-2023` | Replace "s" with "X" | Replaces every s with X. |
| `mod-2024` | Replace "s" with "Y" | Replaces every s with Y. |
| `mod-2025` | Replace "s" with "Z" | Replaces every s with Z. |
| `mod-2026` | Replace "s" with "0" | Replaces every s with 0. |
| `mod-2027` | Replace "s" with "1" | Replaces every s with 1. |
| `mod-2028` | Replace "s" with "2" | Replaces every s with 2. |
| `mod-2029` | Replace "s" with "3" | Replaces every s with 3. |
| `mod-2030` | Replace "s" with "4" | Replaces every s with 4. |
| `mod-2031` | Replace "s" with "5" | Replaces every s with 5. |
| `mod-2032` | Replace "s" with "6" | Replaces every s with 6. |
| `mod-2033` | Replace "s" with "7" | Replaces every s with 7. |
| `mod-2034` | Replace "s" with "8" | Replaces every s with 8. |
| `mod-2035` | Replace "s" with "9" | Replaces every s with 9. |
| `mod-2036` | Replace "t" with "a" | Replaces every t with a. |
| `mod-2037` | Replace "t" with "b" | Replaces every t with b. |
| `mod-2038` | Replace "t" with "c" | Replaces every t with c. |
| `mod-2039` | Replace "t" with "d" | Replaces every t with d. |
| `mod-2040` | Replace "t" with "e" | Replaces every t with e. |
| `mod-2041` | Replace "t" with "f" | Replaces every t with f. |
| `mod-2042` | Replace "t" with "g" | Replaces every t with g. |
| `mod-2043` | Replace "t" with "h" | Replaces every t with h. |
| `mod-2044` | Replace "t" with "i" | Replaces every t with i. |
| `mod-2045` | Replace "t" with "j" | Replaces every t with j. |
| `mod-2046` | Replace "t" with "k" | Replaces every t with k. |
| `mod-2047` | Replace "t" with "l" | Replaces every t with l. |
| `mod-2048` | Replace "t" with "m" | Replaces every t with m. |
| `mod-2049` | Replace "t" with "n" | Replaces every t with n. |
| `mod-2050` | Replace "t" with "o" | Replaces every t with o. |
| `mod-2051` | Replace "t" with "p" | Replaces every t with p. |
| `mod-2052` | Replace "t" with "q" | Replaces every t with q. |
| `mod-2053` | Replace "t" with "r" | Replaces every t with r. |
| `mod-2054` | Replace "t" with "s" | Replaces every t with s. |
| `mod-2055` | Replace "t" with "u" | Replaces every t with u. |
| `mod-2056` | Replace "t" with "v" | Replaces every t with v. |
| `mod-2057` | Replace "t" with "w" | Replaces every t with w. |
| `mod-2058` | Replace "t" with "x" | Replaces every t with x. |
| `mod-2059` | Replace "t" with "y" | Replaces every t with y. |
| `mod-2060` | Replace "t" with "z" | Replaces every t with z. |
| `mod-2061` | Replace "t" with "A" | Replaces every t with A. |
| `mod-2062` | Replace "t" with "B" | Replaces every t with B. |
| `mod-2063` | Replace "t" with "C" | Replaces every t with C. |
| `mod-2064` | Replace "t" with "D" | Replaces every t with D. |
| `mod-2065` | Replace "t" with "E" | Replaces every t with E. |
| `mod-2066` | Replace "t" with "F" | Replaces every t with F. |
| `mod-2067` | Replace "t" with "G" | Replaces every t with G. |
| `mod-2068` | Replace "t" with "H" | Replaces every t with H. |
| `mod-2069` | Replace "t" with "I" | Replaces every t with I. |
| `mod-2070` | Replace "t" with "J" | Replaces every t with J. |
| `mod-2071` | Replace "t" with "K" | Replaces every t with K. |
| `mod-2072` | Replace "t" with "L" | Replaces every t with L. |
| `mod-2073` | Replace "t" with "M" | Replaces every t with M. |
| `mod-2074` | Replace "t" with "N" | Replaces every t with N. |
| `mod-2075` | Replace "t" with "O" | Replaces every t with O. |
| `mod-2076` | Replace "t" with "P" | Replaces every t with P. |
| `mod-2077` | Replace "t" with "Q" | Replaces every t with Q. |
| `mod-2078` | Replace "t" with "R" | Replaces every t with R. |
| `mod-2079` | Replace "t" with "S" | Replaces every t with S. |
| `mod-2080` | Replace "t" with "T" | Replaces every t with T. |
| `mod-2081` | Replace "t" with "U" | Replaces every t with U. |
| `mod-2082` | Replace "t" with "V" | Replaces every t with V. |
| `mod-2083` | Replace "t" with "W" | Replaces every t with W. |
| `mod-2084` | Replace "t" with "X" | Replaces every t with X. |
| `mod-2085` | Replace "t" with "Y" | Replaces every t with Y. |
| `mod-2086` | Replace "t" with "Z" | Replaces every t with Z. |
| `mod-2087` | Replace "t" with "0" | Replaces every t with 0. |
| `mod-2088` | Replace "t" with "1" | Replaces every t with 1. |
| `mod-2089` | Replace "t" with "2" | Replaces every t with 2. |
| `mod-2090` | Replace "t" with "3" | Replaces every t with 3. |
| `mod-2091` | Replace "t" with "4" | Replaces every t with 4. |
| `mod-2092` | Replace "t" with "5" | Replaces every t with 5. |
| `mod-2093` | Replace "t" with "6" | Replaces every t with 6. |
| `mod-2094` | Replace "t" with "7" | Replaces every t with 7. |
| `mod-2095` | Replace "t" with "8" | Replaces every t with 8. |
| `mod-2096` | Replace "t" with "9" | Replaces every t with 9. |
| `mod-2097` | Replace "u" with "a" | Replaces every u with a. |
| `mod-2098` | Replace "u" with "b" | Replaces every u with b. |
| `mod-2099` | Replace "u" with "c" | Replaces every u with c. |
| `mod-2100` | Replace "u" with "d" | Replaces every u with d. |
| `mod-2101` | Replace "u" with "e" | Replaces every u with e. |
| `mod-2102` | Replace "u" with "f" | Replaces every u with f. |
| `mod-2103` | Replace "u" with "g" | Replaces every u with g. |
| `mod-2104` | Replace "u" with "h" | Replaces every u with h. |
| `mod-2105` | Replace "u" with "i" | Replaces every u with i. |
| `mod-2106` | Replace "u" with "j" | Replaces every u with j. |
| `mod-2107` | Replace "u" with "k" | Replaces every u with k. |
| `mod-2108` | Replace "u" with "l" | Replaces every u with l. |
| `mod-2109` | Replace "u" with "m" | Replaces every u with m. |
| `mod-2110` | Replace "u" with "n" | Replaces every u with n. |
| `mod-2111` | Replace "u" with "o" | Replaces every u with o. |
| `mod-2112` | Replace "u" with "p" | Replaces every u with p. |
| `mod-2113` | Replace "u" with "q" | Replaces every u with q. |
| `mod-2114` | Replace "u" with "r" | Replaces every u with r. |
| `mod-2115` | Replace "u" with "s" | Replaces every u with s. |
| `mod-2116` | Replace "u" with "t" | Replaces every u with t. |
| `mod-2117` | Replace "u" with "v" | Replaces every u with v. |
| `mod-2118` | Replace "u" with "w" | Replaces every u with w. |
| `mod-2119` | Replace "u" with "x" | Replaces every u with x. |
| `mod-2120` | Replace "u" with "y" | Replaces every u with y. |
| `mod-2121` | Replace "u" with "z" | Replaces every u with z. |
| `mod-2122` | Replace "u" with "A" | Replaces every u with A. |
| `mod-2123` | Replace "u" with "B" | Replaces every u with B. |
| `mod-2124` | Replace "u" with "C" | Replaces every u with C. |
| `mod-2125` | Replace "u" with "D" | Replaces every u with D. |
| `mod-2126` | Replace "u" with "E" | Replaces every u with E. |
| `mod-2127` | Replace "u" with "F" | Replaces every u with F. |
| `mod-2128` | Replace "u" with "G" | Replaces every u with G. |
| `mod-2129` | Replace "u" with "H" | Replaces every u with H. |
| `mod-2130` | Replace "u" with "I" | Replaces every u with I. |
| `mod-2131` | Replace "u" with "J" | Replaces every u with J. |
| `mod-2132` | Replace "u" with "K" | Replaces every u with K. |
| `mod-2133` | Replace "u" with "L" | Replaces every u with L. |
| `mod-2134` | Replace "u" with "M" | Replaces every u with M. |
| `mod-2135` | Replace "u" with "N" | Replaces every u with N. |
| `mod-2136` | Replace "u" with "O" | Replaces every u with O. |
| `mod-2137` | Replace "u" with "P" | Replaces every u with P. |
| `mod-2138` | Replace "u" with "Q" | Replaces every u with Q. |
| `mod-2139` | Replace "u" with "R" | Replaces every u with R. |
| `mod-2140` | Replace "u" with "S" | Replaces every u with S. |
| `mod-2141` | Replace "u" with "T" | Replaces every u with T. |
| `mod-2142` | Replace "u" with "U" | Replaces every u with U. |
| `mod-2143` | Replace "u" with "V" | Replaces every u with V. |
| `mod-2144` | Replace "u" with "W" | Replaces every u with W. |
| `mod-2145` | Replace "u" with "X" | Replaces every u with X. |
| `mod-2146` | Replace "u" with "Y" | Replaces every u with Y. |
| `mod-2147` | Replace "u" with "Z" | Replaces every u with Z. |
| `mod-2148` | Replace "u" with "0" | Replaces every u with 0. |
| `mod-2149` | Replace "u" with "1" | Replaces every u with 1. |
| `mod-2150` | Replace "u" with "2" | Replaces every u with 2. |
| `mod-2151` | Replace "u" with "3" | Replaces every u with 3. |
| `mod-2152` | Replace "u" with "4" | Replaces every u with 4. |
| `mod-2153` | Replace "u" with "5" | Replaces every u with 5. |
| `mod-2154` | Replace "u" with "6" | Replaces every u with 6. |
| `mod-2155` | Replace "u" with "7" | Replaces every u with 7. |
| `mod-2156` | Replace "u" with "8" | Replaces every u with 8. |
| `mod-2157` | Replace "u" with "9" | Replaces every u with 9. |
| `mod-2158` | Replace "v" with "a" | Replaces every v with a. |
| `mod-2159` | Replace "v" with "b" | Replaces every v with b. |
| `mod-2160` | Replace "v" with "c" | Replaces every v with c. |
| `mod-2161` | Replace "v" with "d" | Replaces every v with d. |
| `mod-2162` | Replace "v" with "e" | Replaces every v with e. |
| `mod-2163` | Replace "v" with "f" | Replaces every v with f. |
| `mod-2164` | Replace "v" with "g" | Replaces every v with g. |
| `mod-2165` | Replace "v" with "h" | Replaces every v with h. |
| `mod-2166` | Replace "v" with "i" | Replaces every v with i. |
| `mod-2167` | Replace "v" with "j" | Replaces every v with j. |
| `mod-2168` | Replace "v" with "k" | Replaces every v with k. |
| `mod-2169` | Replace "v" with "l" | Replaces every v with l. |
| `mod-2170` | Replace "v" with "m" | Replaces every v with m. |
| `mod-2171` | Replace "v" with "n" | Replaces every v with n. |
| `mod-2172` | Replace "v" with "o" | Replaces every v with o. |
| `mod-2173` | Replace "v" with "p" | Replaces every v with p. |
| `mod-2174` | Replace "v" with "q" | Replaces every v with q. |
| `mod-2175` | Replace "v" with "r" | Replaces every v with r. |
| `mod-2176` | Replace "v" with "s" | Replaces every v with s. |
| `mod-2177` | Replace "v" with "t" | Replaces every v with t. |
| `mod-2178` | Replace "v" with "u" | Replaces every v with u. |
| `mod-2179` | Replace "v" with "w" | Replaces every v with w. |
| `mod-2180` | Replace "v" with "x" | Replaces every v with x. |
| `mod-2181` | Replace "v" with "y" | Replaces every v with y. |
| `mod-2182` | Replace "v" with "z" | Replaces every v with z. |
| `mod-2183` | Replace "v" with "A" | Replaces every v with A. |
| `mod-2184` | Replace "v" with "B" | Replaces every v with B. |
| `mod-2185` | Replace "v" with "C" | Replaces every v with C. |
| `mod-2186` | Replace "v" with "D" | Replaces every v with D. |
| `mod-2187` | Replace "v" with "E" | Replaces every v with E. |
| `mod-2188` | Replace "v" with "F" | Replaces every v with F. |
| `mod-2189` | Replace "v" with "G" | Replaces every v with G. |
| `mod-2190` | Replace "v" with "H" | Replaces every v with H. |
| `mod-2191` | Replace "v" with "I" | Replaces every v with I. |
| `mod-2192` | Replace "v" with "J" | Replaces every v with J. |
| `mod-2193` | Replace "v" with "K" | Replaces every v with K. |
| `mod-2194` | Replace "v" with "L" | Replaces every v with L. |
| `mod-2195` | Replace "v" with "M" | Replaces every v with M. |
| `mod-2196` | Replace "v" with "N" | Replaces every v with N. |
| `mod-2197` | Replace "v" with "O" | Replaces every v with O. |
| `mod-2198` | Replace "v" with "P" | Replaces every v with P. |
| `mod-2199` | Replace "v" with "Q" | Replaces every v with Q. |
| `mod-2200` | Replace "v" with "R" | Replaces every v with R. |
| `mod-2201` | Replace "v" with "S" | Replaces every v with S. |
| `mod-2202` | Replace "v" with "T" | Replaces every v with T. |
| `mod-2203` | Replace "v" with "U" | Replaces every v with U. |
| `mod-2204` | Replace "v" with "V" | Replaces every v with V. |
| `mod-2205` | Replace "v" with "W" | Replaces every v with W. |
| `mod-2206` | Replace "v" with "X" | Replaces every v with X. |
| `mod-2207` | Replace "v" with "Y" | Replaces every v with Y. |
| `mod-2208` | Replace "v" with "Z" | Replaces every v with Z. |
| `mod-2209` | Replace "v" with "0" | Replaces every v with 0. |
| `mod-2210` | Replace "v" with "1" | Replaces every v with 1. |
| `mod-2211` | Replace "v" with "2" | Replaces every v with 2. |
| `mod-2212` | Replace "v" with "3" | Replaces every v with 3. |
| `mod-2213` | Replace "v" with "4" | Replaces every v with 4. |
| `mod-2214` | Replace "v" with "5" | Replaces every v with 5. |
| `mod-2215` | Replace "v" with "6" | Replaces every v with 6. |
| `mod-2216` | Replace "v" with "7" | Replaces every v with 7. |
| `mod-2217` | Replace "v" with "8" | Replaces every v with 8. |
| `mod-2218` | Replace "v" with "9" | Replaces every v with 9. |
| `mod-2219` | Replace "w" with "a" | Replaces every w with a. |
| `mod-2220` | Replace "w" with "b" | Replaces every w with b. |
| `mod-2221` | Replace "w" with "c" | Replaces every w with c. |
| `mod-2222` | Replace "w" with "d" | Replaces every w with d. |
| `mod-2223` | Replace "w" with "e" | Replaces every w with e. |
| `mod-2224` | Replace "w" with "f" | Replaces every w with f. |
| `mod-2225` | Replace "w" with "g" | Replaces every w with g. |
| `mod-2226` | Replace "w" with "h" | Replaces every w with h. |
| `mod-2227` | Replace "w" with "i" | Replaces every w with i. |
| `mod-2228` | Replace "w" with "j" | Replaces every w with j. |
| `mod-2229` | Replace "w" with "k" | Replaces every w with k. |
| `mod-2230` | Replace "w" with "l" | Replaces every w with l. |
| `mod-2231` | Replace "w" with "m" | Replaces every w with m. |
| `mod-2232` | Replace "w" with "n" | Replaces every w with n. |
| `mod-2233` | Replace "w" with "o" | Replaces every w with o. |
| `mod-2234` | Replace "w" with "p" | Replaces every w with p. |
| `mod-2235` | Replace "w" with "q" | Replaces every w with q. |
| `mod-2236` | Replace "w" with "r" | Replaces every w with r. |
| `mod-2237` | Replace "w" with "s" | Replaces every w with s. |
| `mod-2238` | Replace "w" with "t" | Replaces every w with t. |
| `mod-2239` | Replace "w" with "u" | Replaces every w with u. |
| `mod-2240` | Replace "w" with "v" | Replaces every w with v. |
| `mod-2241` | Replace "w" with "x" | Replaces every w with x. |
| `mod-2242` | Replace "w" with "y" | Replaces every w with y. |
| `mod-2243` | Replace "w" with "z" | Replaces every w with z. |
| `mod-2244` | Replace "w" with "A" | Replaces every w with A. |
| `mod-2245` | Replace "w" with "B" | Replaces every w with B. |
| `mod-2246` | Replace "w" with "C" | Replaces every w with C. |
| `mod-2247` | Replace "w" with "D" | Replaces every w with D. |
| `mod-2248` | Replace "w" with "E" | Replaces every w with E. |
| `mod-2249` | Replace "w" with "F" | Replaces every w with F. |
| `mod-2250` | Replace "w" with "G" | Replaces every w with G. |
| `mod-2251` | Replace "w" with "H" | Replaces every w with H. |
| `mod-2252` | Replace "w" with "I" | Replaces every w with I. |
| `mod-2253` | Replace "w" with "J" | Replaces every w with J. |
| `mod-2254` | Replace "w" with "K" | Replaces every w with K. |
| `mod-2255` | Replace "w" with "L" | Replaces every w with L. |
| `mod-2256` | Replace "w" with "M" | Replaces every w with M. |
| `mod-2257` | Replace "w" with "N" | Replaces every w with N. |
| `mod-2258` | Replace "w" with "O" | Replaces every w with O. |
| `mod-2259` | Replace "w" with "P" | Replaces every w with P. |
| `mod-2260` | Replace "w" with "Q" | Replaces every w with Q. |
| `mod-2261` | Replace "w" with "R" | Replaces every w with R. |
| `mod-2262` | Replace "w" with "S" | Replaces every w with S. |
| `mod-2263` | Replace "w" with "T" | Replaces every w with T. |
| `mod-2264` | Replace "w" with "U" | Replaces every w with U. |
| `mod-2265` | Replace "w" with "V" | Replaces every w with V. |
| `mod-2266` | Replace "w" with "W" | Replaces every w with W. |
| `mod-2267` | Replace "w" with "X" | Replaces every w with X. |
| `mod-2268` | Replace "w" with "Y" | Replaces every w with Y. |
| `mod-2269` | Replace "w" with "Z" | Replaces every w with Z. |
| `mod-2270` | Replace "w" with "0" | Replaces every w with 0. |
| `mod-2271` | Replace "w" with "1" | Replaces every w with 1. |
| `mod-2272` | Replace "w" with "2" | Replaces every w with 2. |
| `mod-2273` | Replace "w" with "3" | Replaces every w with 3. |
| `mod-2274` | Replace "w" with "4" | Replaces every w with 4. |
| `mod-2275` | Replace "w" with "5" | Replaces every w with 5. |
| `mod-2276` | Replace "w" with "6" | Replaces every w with 6. |
| `mod-2277` | Replace "w" with "7" | Replaces every w with 7. |
| `mod-2278` | Replace "w" with "8" | Replaces every w with 8. |
| `mod-2279` | Replace "w" with "9" | Replaces every w with 9. |
| `mod-2280` | Replace "x" with "a" | Replaces every x with a. |
| `mod-2281` | Replace "x" with "b" | Replaces every x with b. |
| `mod-2282` | Replace "x" with "c" | Replaces every x with c. |
| `mod-2283` | Replace "x" with "d" | Replaces every x with d. |
| `mod-2284` | Replace "x" with "e" | Replaces every x with e. |
| `mod-2285` | Replace "x" with "f" | Replaces every x with f. |
| `mod-2286` | Replace "x" with "g" | Replaces every x with g. |
| `mod-2287` | Replace "x" with "h" | Replaces every x with h. |
| `mod-2288` | Replace "x" with "i" | Replaces every x with i. |
| `mod-2289` | Replace "x" with "j" | Replaces every x with j. |
| `mod-2290` | Replace "x" with "k" | Replaces every x with k. |
| `mod-2291` | Replace "x" with "l" | Replaces every x with l. |
| `mod-2292` | Replace "x" with "m" | Replaces every x with m. |
| `mod-2293` | Replace "x" with "n" | Replaces every x with n. |
| `mod-2294` | Replace "x" with "o" | Replaces every x with o. |
| `mod-2295` | Replace "x" with "p" | Replaces every x with p. |
| `mod-2296` | Replace "x" with "q" | Replaces every x with q. |
| `mod-2297` | Replace "x" with "r" | Replaces every x with r. |
| `mod-2298` | Replace "x" with "s" | Replaces every x with s. |
| `mod-2299` | Replace "x" with "t" | Replaces every x with t. |
| `mod-2300` | Replace "x" with "u" | Replaces every x with u. |
| `mod-2301` | Replace "x" with "v" | Replaces every x with v. |
| `mod-2302` | Replace "x" with "w" | Replaces every x with w. |
| `mod-2303` | Replace "x" with "y" | Replaces every x with y. |
| `mod-2304` | Replace "x" with "z" | Replaces every x with z. |
| `mod-2305` | Replace "x" with "A" | Replaces every x with A. |
| `mod-2306` | Replace "x" with "B" | Replaces every x with B. |
| `mod-2307` | Replace "x" with "C" | Replaces every x with C. |
| `mod-2308` | Replace "x" with "D" | Replaces every x with D. |
| `mod-2309` | Replace "x" with "E" | Replaces every x with E. |
| `mod-2310` | Replace "x" with "F" | Replaces every x with F. |
| `mod-2311` | Replace "x" with "G" | Replaces every x with G. |
| `mod-2312` | Replace "x" with "H" | Replaces every x with H. |
| `mod-2313` | Replace "x" with "I" | Replaces every x with I. |
| `mod-2314` | Replace "x" with "J" | Replaces every x with J. |
| `mod-2315` | Replace "x" with "K" | Replaces every x with K. |
| `mod-2316` | Replace "x" with "L" | Replaces every x with L. |
| `mod-2317` | Replace "x" with "M" | Replaces every x with M. |
| `mod-2318` | Replace "x" with "N" | Replaces every x with N. |
| `mod-2319` | Replace "x" with "O" | Replaces every x with O. |
| `mod-2320` | Replace "x" with "P" | Replaces every x with P. |
| `mod-2321` | Replace "x" with "Q" | Replaces every x with Q. |
| `mod-2322` | Replace "x" with "R" | Replaces every x with R. |
| `mod-2323` | Replace "x" with "S" | Replaces every x with S. |
| `mod-2324` | Replace "x" with "T" | Replaces every x with T. |
| `mod-2325` | Replace "x" with "U" | Replaces every x with U. |
| `mod-2326` | Replace "x" with "V" | Replaces every x with V. |
| `mod-2327` | Replace "x" with "W" | Replaces every x with W. |
| `mod-2328` | Replace "x" with "X" | Replaces every x with X. |
| `mod-2329` | Replace "x" with "Y" | Replaces every x with Y. |
| `mod-2330` | Replace "x" with "Z" | Replaces every x with Z. |
| `mod-2331` | Replace "x" with "0" | Replaces every x with 0. |
| `mod-2332` | Replace "x" with "1" | Replaces every x with 1. |
| `mod-2333` | Replace "x" with "2" | Replaces every x with 2. |
| `mod-2334` | Replace "x" with "3" | Replaces every x with 3. |
| `mod-2335` | Replace "x" with "4" | Replaces every x with 4. |
| `mod-2336` | Replace "x" with "5" | Replaces every x with 5. |
| `mod-2337` | Replace "x" with "6" | Replaces every x with 6. |
| `mod-2338` | Replace "x" with "7" | Replaces every x with 7. |
| `mod-2339` | Replace "x" with "8" | Replaces every x with 8. |
| `mod-2340` | Replace "x" with "9" | Replaces every x with 9. |
| `mod-2341` | Replace "y" with "a" | Replaces every y with a. |
| `mod-2342` | Replace "y" with "b" | Replaces every y with b. |
| `mod-2343` | Replace "y" with "c" | Replaces every y with c. |
| `mod-2344` | Replace "y" with "d" | Replaces every y with d. |
| `mod-2345` | Replace "y" with "e" | Replaces every y with e. |
| `mod-2346` | Replace "y" with "f" | Replaces every y with f. |
| `mod-2347` | Replace "y" with "g" | Replaces every y with g. |
| `mod-2348` | Replace "y" with "h" | Replaces every y with h. |
| `mod-2349` | Replace "y" with "i" | Replaces every y with i. |
| `mod-2350` | Replace "y" with "j" | Replaces every y with j. |
| `mod-2351` | Replace "y" with "k" | Replaces every y with k. |
| `mod-2352` | Replace "y" with "l" | Replaces every y with l. |
| `mod-2353` | Replace "y" with "m" | Replaces every y with m. |
| `mod-2354` | Replace "y" with "n" | Replaces every y with n. |
| `mod-2355` | Replace "y" with "o" | Replaces every y with o. |
| `mod-2356` | Replace "y" with "p" | Replaces every y with p. |
| `mod-2357` | Replace "y" with "q" | Replaces every y with q. |
| `mod-2358` | Replace "y" with "r" | Replaces every y with r. |
| `mod-2359` | Replace "y" with "s" | Replaces every y with s. |
| `mod-2360` | Replace "y" with "t" | Replaces every y with t. |
| `mod-2361` | Replace "y" with "u" | Replaces every y with u. |
| `mod-2362` | Replace "y" with "v" | Replaces every y with v. |
| `mod-2363` | Replace "y" with "w" | Replaces every y with w. |
| `mod-2364` | Replace "y" with "x" | Replaces every y with x. |
| `mod-2365` | Replace "y" with "z" | Replaces every y with z. |
| `mod-2366` | Replace "y" with "A" | Replaces every y with A. |
| `mod-2367` | Replace "y" with "B" | Replaces every y with B. |
| `mod-2368` | Replace "y" with "C" | Replaces every y with C. |
| `mod-2369` | Replace "y" with "D" | Replaces every y with D. |
| `mod-2370` | Replace "y" with "E" | Replaces every y with E. |
| `mod-2371` | Replace "y" with "F" | Replaces every y with F. |
| `mod-2372` | Replace "y" with "G" | Replaces every y with G. |
| `mod-2373` | Replace "y" with "H" | Replaces every y with H. |
| `mod-2374` | Replace "y" with "I" | Replaces every y with I. |
| `mod-2375` | Replace "y" with "J" | Replaces every y with J. |
| `mod-2376` | Replace "y" with "K" | Replaces every y with K. |
| `mod-2377` | Replace "y" with "L" | Replaces every y with L. |
| `mod-2378` | Replace "y" with "M" | Replaces every y with M. |
| `mod-2379` | Replace "y" with "N" | Replaces every y with N. |
| `mod-2380` | Replace "y" with "O" | Replaces every y with O. |
| `mod-2381` | Replace "y" with "P" | Replaces every y with P. |
| `mod-2382` | Replace "y" with "Q" | Replaces every y with Q. |
| `mod-2383` | Replace "y" with "R" | Replaces every y with R. |
| `mod-2384` | Replace "y" with "S" | Replaces every y with S. |
| `mod-2385` | Replace "y" with "T" | Replaces every y with T. |
| `mod-2386` | Replace "y" with "U" | Replaces every y with U. |
| `mod-2387` | Replace "y" with "V" | Replaces every y with V. |
| `mod-2388` | Replace "y" with "W" | Replaces every y with W. |
| `mod-2389` | Replace "y" with "X" | Replaces every y with X. |
| `mod-2390` | Replace "y" with "Y" | Replaces every y with Y. |
| `mod-2391` | Replace "y" with "Z" | Replaces every y with Z. |
| `mod-2392` | Replace "y" with "0" | Replaces every y with 0. |
| `mod-2393` | Replace "y" with "1" | Replaces every y with 1. |
| `mod-2394` | Replace "y" with "2" | Replaces every y with 2. |
| `mod-2395` | Replace "y" with "3" | Replaces every y with 3. |
| `mod-2396` | Replace "y" with "4" | Replaces every y with 4. |
| `mod-2397` | Replace "y" with "5" | Replaces every y with 5. |
| `mod-2398` | Replace "y" with "6" | Replaces every y with 6. |
| `mod-2399` | Replace "y" with "7" | Replaces every y with 7. |
| `mod-2400` | Replace "y" with "8" | Replaces every y with 8. |
| `mod-2401` | Replace "y" with "9" | Replaces every y with 9. |
| `mod-2402` | Replace "z" with "a" | Replaces every z with a. |
| `mod-2403` | Replace "z" with "b" | Replaces every z with b. |
| `mod-2404` | Replace "z" with "c" | Replaces every z with c. |
| `mod-2405` | Replace "z" with "d" | Replaces every z with d. |
| `mod-2406` | Replace "z" with "e" | Replaces every z with e. |
| `mod-2407` | Replace "z" with "f" | Replaces every z with f. |
| `mod-2408` | Replace "z" with "g" | Replaces every z with g. |
| `mod-2409` | Replace "z" with "h" | Replaces every z with h. |
| `mod-2410` | Replace "z" with "i" | Replaces every z with i. |
| `mod-2411` | Replace "z" with "j" | Replaces every z with j. |
| `mod-2412` | Replace "z" with "k" | Replaces every z with k. |
| `mod-2413` | Replace "z" with "l" | Replaces every z with l. |
| `mod-2414` | Replace "z" with "m" | Replaces every z with m. |
| `mod-2415` | Replace "z" with "n" | Replaces every z with n. |
| `mod-2416` | Replace "z" with "o" | Replaces every z with o. |
| `mod-2417` | Replace "z" with "p" | Replaces every z with p. |
| `mod-2418` | Replace "z" with "q" | Replaces every z with q. |
| `mod-2419` | Replace "z" with "r" | Replaces every z with r. |
| `mod-2420` | Replace "z" with "s" | Replaces every z with s. |
| `mod-2421` | Replace "z" with "t" | Replaces every z with t. |
| `mod-2422` | Replace "z" with "u" | Replaces every z with u. |
| `mod-2423` | Replace "z" with "v" | Replaces every z with v. |
| `mod-2424` | Replace "z" with "w" | Replaces every z with w. |
| `mod-2425` | Replace "z" with "x" | Replaces every z with x. |
| `mod-2426` | Replace "z" with "y" | Replaces every z with y. |
| `mod-2427` | Replace "z" with "A" | Replaces every z with A. |
| `mod-2428` | Replace "z" with "B" | Replaces every z with B. |
| `mod-2429` | Replace "z" with "C" | Replaces every z with C. |
| `mod-2430` | Replace "z" with "D" | Replaces every z with D. |
| `mod-2431` | Replace "z" with "E" | Replaces every z with E. |
| `mod-2432` | Replace "z" with "F" | Replaces every z with F. |
| `mod-2433` | Replace "z" with "G" | Replaces every z with G. |
| `mod-2434` | Replace "z" with "H" | Replaces every z with H. |
| `mod-2435` | Replace "z" with "I" | Replaces every z with I. |
| `mod-2436` | Replace "z" with "J" | Replaces every z with J. |
| `mod-2437` | Replace "z" with "K" | Replaces every z with K. |
| `mod-2438` | Replace "z" with "L" | Replaces every z with L. |
| `mod-2439` | Replace "z" with "M" | Replaces every z with M. |
| `mod-2440` | Replace "z" with "N" | Replaces every z with N. |
| `mod-2441` | Replace "z" with "O" | Replaces every z with O. |
| `mod-2442` | Replace "z" with "P" | Replaces every z with P. |
| `mod-2443` | Replace "z" with "Q" | Replaces every z with Q. |
| `mod-2444` | Replace "z" with "R" | Replaces every z with R. |
| `mod-2445` | Replace "z" with "S" | Replaces every z with S. |
| `mod-2446` | Replace "z" with "T" | Replaces every z with T. |
| `mod-2447` | Replace "z" with "U" | Replaces every z with U. |
| `mod-2448` | Replace "z" with "V" | Replaces every z with V. |
| `mod-2449` | Replace "z" with "W" | Replaces every z with W. |
| `mod-2450` | Replace "z" with "X" | Replaces every z with X. |
| `mod-2451` | Replace "z" with "Y" | Replaces every z with Y. |
| `mod-2452` | Replace "z" with "Z" | Replaces every z with Z. |
| `mod-2453` | Replace "z" with "0" | Replaces every z with 0. |
| `mod-2454` | Replace "z" with "1" | Replaces every z with 1. |
| `mod-2455` | Replace "z" with "2" | Replaces every z with 2. |
| `mod-2456` | Replace "z" with "3" | Replaces every z with 3. |
| `mod-2457` | Replace "z" with "4" | Replaces every z with 4. |
| `mod-2458` | Replace "z" with "5" | Replaces every z with 5. |
| `mod-2459` | Replace "z" with "6" | Replaces every z with 6. |
| `mod-2460` | Replace "z" with "7" | Replaces every z with 7. |
| `mod-2461` | Replace "z" with "8" | Replaces every z with 8. |
| `mod-2462` | Replace "z" with "9" | Replaces every z with 9. |
| `mod-2463` | Replace "A" with "a" | Replaces every A with a. |
| `mod-2464` | Replace "A" with "b" | Replaces every A with b. |
| `mod-2465` | Replace "A" with "c" | Replaces every A with c. |
| `mod-2466` | Replace "A" with "d" | Replaces every A with d. |
| `mod-2467` | Replace "A" with "e" | Replaces every A with e. |
| `mod-2468` | Replace "A" with "f" | Replaces every A with f. |
| `mod-2469` | Replace "A" with "g" | Replaces every A with g. |
| `mod-2470` | Replace "A" with "h" | Replaces every A with h. |
| `mod-2471` | Replace "A" with "i" | Replaces every A with i. |
| `mod-2472` | Replace "A" with "j" | Replaces every A with j. |
| `mod-2473` | Replace "A" with "k" | Replaces every A with k. |
| `mod-2474` | Replace "A" with "l" | Replaces every A with l. |
| `mod-2475` | Replace "A" with "m" | Replaces every A with m. |
| `mod-2476` | Replace "A" with "n" | Replaces every A with n. |
| `mod-2477` | Replace "A" with "o" | Replaces every A with o. |
| `mod-2478` | Replace "A" with "p" | Replaces every A with p. |
| `mod-2479` | Replace "A" with "q" | Replaces every A with q. |
| `mod-2480` | Replace "A" with "r" | Replaces every A with r. |
| `mod-2481` | Replace "A" with "s" | Replaces every A with s. |
| `mod-2482` | Replace "A" with "t" | Replaces every A with t. |
| `mod-2483` | Replace "A" with "u" | Replaces every A with u. |
| `mod-2484` | Replace "A" with "v" | Replaces every A with v. |
| `mod-2485` | Replace "A" with "w" | Replaces every A with w. |
| `mod-2486` | Replace "A" with "x" | Replaces every A with x. |
| `mod-2487` | Replace "A" with "y" | Replaces every A with y. |
| `mod-2488` | Replace "A" with "z" | Replaces every A with z. |
| `mod-2489` | Replace "A" with "B" | Replaces every A with B. |
| `mod-2490` | Replace "A" with "C" | Replaces every A with C. |
| `mod-2491` | Replace "A" with "D" | Replaces every A with D. |
| `mod-2492` | Replace "A" with "E" | Replaces every A with E. |
| `mod-2493` | Replace "A" with "F" | Replaces every A with F. |
| `mod-2494` | Replace "A" with "G" | Replaces every A with G. |
| `mod-2495` | Replace "A" with "H" | Replaces every A with H. |
| `mod-2496` | Replace "A" with "I" | Replaces every A with I. |
| `mod-2497` | Replace "A" with "J" | Replaces every A with J. |
| `mod-2498` | Replace "A" with "K" | Replaces every A with K. |
| `mod-2499` | Replace "A" with "L" | Replaces every A with L. |
| `mod-2500` | Replace "A" with "M" | Replaces every A with M. |
| `mod-2501` | Replace "A" with "N" | Replaces every A with N. |
| `mod-2502` | Replace "A" with "O" | Replaces every A with O. |
| `mod-2503` | Replace "A" with "P" | Replaces every A with P. |
| `mod-2504` | Replace "A" with "Q" | Replaces every A with Q. |
| `mod-2505` | Replace "A" with "R" | Replaces every A with R. |
| `mod-2506` | Replace "A" with "S" | Replaces every A with S. |
| `mod-2507` | Replace "A" with "T" | Replaces every A with T. |
| `mod-2508` | Replace "A" with "U" | Replaces every A with U. |
| `mod-2509` | Replace "A" with "V" | Replaces every A with V. |
| `mod-2510` | Replace "A" with "W" | Replaces every A with W. |
| `mod-2511` | Replace "A" with "X" | Replaces every A with X. |
| `mod-2512` | Replace "A" with "Y" | Replaces every A with Y. |
| `mod-2513` | Replace "A" with "Z" | Replaces every A with Z. |
| `mod-2514` | Replace "A" with "0" | Replaces every A with 0. |
| `mod-2515` | Replace "A" with "1" | Replaces every A with 1. |
| `mod-2516` | Replace "A" with "2" | Replaces every A with 2. |
| `mod-2517` | Replace "A" with "3" | Replaces every A with 3. |
| `mod-2518` | Replace "A" with "4" | Replaces every A with 4. |
| `mod-2519` | Replace "A" with "5" | Replaces every A with 5. |
| `mod-2520` | Replace "A" with "6" | Replaces every A with 6. |
| `mod-2521` | Replace "A" with "7" | Replaces every A with 7. |
| `mod-2522` | Replace "A" with "8" | Replaces every A with 8. |
| `mod-2523` | Replace "A" with "9" | Replaces every A with 9. |
| `mod-2524` | Replace "B" with "a" | Replaces every B with a. |
| `mod-2525` | Replace "B" with "b" | Replaces every B with b. |
| `mod-2526` | Replace "B" with "c" | Replaces every B with c. |
| `mod-2527` | Replace "B" with "d" | Replaces every B with d. |
| `mod-2528` | Replace "B" with "e" | Replaces every B with e. |
| `mod-2529` | Replace "B" with "f" | Replaces every B with f. |
| `mod-2530` | Replace "B" with "g" | Replaces every B with g. |
| `mod-2531` | Replace "B" with "h" | Replaces every B with h. |
| `mod-2532` | Replace "B" with "i" | Replaces every B with i. |
| `mod-2533` | Replace "B" with "j" | Replaces every B with j. |
| `mod-2534` | Replace "B" with "k" | Replaces every B with k. |
| `mod-2535` | Replace "B" with "l" | Replaces every B with l. |
| `mod-2536` | Replace "B" with "m" | Replaces every B with m. |
| `mod-2537` | Replace "B" with "n" | Replaces every B with n. |
| `mod-2538` | Replace "B" with "o" | Replaces every B with o. |
| `mod-2539` | Replace "B" with "p" | Replaces every B with p. |
| `mod-2540` | Replace "B" with "q" | Replaces every B with q. |
| `mod-2541` | Replace "B" with "r" | Replaces every B with r. |
| `mod-2542` | Replace "B" with "s" | Replaces every B with s. |
| `mod-2543` | Replace "B" with "t" | Replaces every B with t. |
| `mod-2544` | Replace "B" with "u" | Replaces every B with u. |
| `mod-2545` | Replace "B" with "v" | Replaces every B with v. |
| `mod-2546` | Replace "B" with "w" | Replaces every B with w. |
| `mod-2547` | Replace "B" with "x" | Replaces every B with x. |
| `mod-2548` | Replace "B" with "y" | Replaces every B with y. |
| `mod-2549` | Replace "B" with "z" | Replaces every B with z. |
| `mod-2550` | Replace "B" with "A" | Replaces every B with A. |
| `mod-2551` | Replace "B" with "C" | Replaces every B with C. |
| `mod-2552` | Replace "B" with "D" | Replaces every B with D. |
| `mod-2553` | Replace "B" with "E" | Replaces every B with E. |
| `mod-2554` | Replace "B" with "F" | Replaces every B with F. |
| `mod-2555` | Replace "B" with "G" | Replaces every B with G. |
| `mod-2556` | Replace "B" with "H" | Replaces every B with H. |
| `mod-2557` | Replace "B" with "I" | Replaces every B with I. |
| `mod-2558` | Replace "B" with "J" | Replaces every B with J. |
| `mod-2559` | Replace "B" with "K" | Replaces every B with K. |
| `mod-2560` | Replace "B" with "L" | Replaces every B with L. |
| `mod-2561` | Replace "B" with "M" | Replaces every B with M. |
| `mod-2562` | Replace "B" with "N" | Replaces every B with N. |
| `mod-2563` | Replace "B" with "O" | Replaces every B with O. |
| `mod-2564` | Replace "B" with "P" | Replaces every B with P. |
| `mod-2565` | Replace "B" with "Q" | Replaces every B with Q. |
| `mod-2566` | Replace "B" with "R" | Replaces every B with R. |
| `mod-2567` | Replace "B" with "S" | Replaces every B with S. |
| `mod-2568` | Replace "B" with "T" | Replaces every B with T. |
| `mod-2569` | Replace "B" with "U" | Replaces every B with U. |
| `mod-2570` | Replace "B" with "V" | Replaces every B with V. |
| `mod-2571` | Replace "B" with "W" | Replaces every B with W. |
| `mod-2572` | Replace "B" with "X" | Replaces every B with X. |
| `mod-2573` | Replace "B" with "Y" | Replaces every B with Y. |
| `mod-2574` | Replace "B" with "Z" | Replaces every B with Z. |
| `mod-2575` | Replace "B" with "0" | Replaces every B with 0. |
| `mod-2576` | Replace "B" with "1" | Replaces every B with 1. |
| `mod-2577` | Replace "B" with "2" | Replaces every B with 2. |
| `mod-2578` | Replace "B" with "3" | Replaces every B with 3. |
| `mod-2579` | Replace "B" with "4" | Replaces every B with 4. |
| `mod-2580` | Replace "B" with "5" | Replaces every B with 5. |
| `mod-2581` | Replace "B" with "6" | Replaces every B with 6. |
| `mod-2582` | Replace "B" with "7" | Replaces every B with 7. |
| `mod-2583` | Replace "B" with "8" | Replaces every B with 8. |
| `mod-2584` | Replace "B" with "9" | Replaces every B with 9. |
| `mod-2585` | Replace "C" with "a" | Replaces every C with a. |
| `mod-2586` | Replace "C" with "b" | Replaces every C with b. |
| `mod-2587` | Replace "C" with "c" | Replaces every C with c. |
| `mod-2588` | Replace "C" with "d" | Replaces every C with d. |
| `mod-2589` | Replace "C" with "e" | Replaces every C with e. |
| `mod-2590` | Replace "C" with "f" | Replaces every C with f. |
| `mod-2591` | Replace "C" with "g" | Replaces every C with g. |
| `mod-2592` | Replace "C" with "h" | Replaces every C with h. |
| `mod-2593` | Replace "C" with "i" | Replaces every C with i. |
| `mod-2594` | Replace "C" with "j" | Replaces every C with j. |
| `mod-2595` | Replace "C" with "k" | Replaces every C with k. |
| `mod-2596` | Replace "C" with "l" | Replaces every C with l. |
| `mod-2597` | Replace "C" with "m" | Replaces every C with m. |
| `mod-2598` | Replace "C" with "n" | Replaces every C with n. |
| `mod-2599` | Replace "C" with "o" | Replaces every C with o. |
| `mod-2600` | Replace "C" with "p" | Replaces every C with p. |
| `mod-2601` | Replace "C" with "q" | Replaces every C with q. |
| `mod-2602` | Replace "C" with "r" | Replaces every C with r. |
| `mod-2603` | Replace "C" with "s" | Replaces every C with s. |
| `mod-2604` | Replace "C" with "t" | Replaces every C with t. |
| `mod-2605` | Replace "C" with "u" | Replaces every C with u. |
| `mod-2606` | Replace "C" with "v" | Replaces every C with v. |
| `mod-2607` | Replace "C" with "w" | Replaces every C with w. |
| `mod-2608` | Replace "C" with "x" | Replaces every C with x. |
| `mod-2609` | Replace "C" with "y" | Replaces every C with y. |
| `mod-2610` | Replace "C" with "z" | Replaces every C with z. |
| `mod-2611` | Replace "C" with "A" | Replaces every C with A. |
| `mod-2612` | Replace "C" with "B" | Replaces every C with B. |
| `mod-2613` | Replace "C" with "D" | Replaces every C with D. |
| `mod-2614` | Replace "C" with "E" | Replaces every C with E. |
| `mod-2615` | Replace "C" with "F" | Replaces every C with F. |
| `mod-2616` | Replace "C" with "G" | Replaces every C with G. |
| `mod-2617` | Replace "C" with "H" | Replaces every C with H. |
| `mod-2618` | Replace "C" with "I" | Replaces every C with I. |
| `mod-2619` | Replace "C" with "J" | Replaces every C with J. |
| `mod-2620` | Replace "C" with "K" | Replaces every C with K. |
| `mod-2621` | Replace "C" with "L" | Replaces every C with L. |
| `mod-2622` | Replace "C" with "M" | Replaces every C with M. |
| `mod-2623` | Replace "C" with "N" | Replaces every C with N. |
| `mod-2624` | Replace "C" with "O" | Replaces every C with O. |
| `mod-2625` | Replace "C" with "P" | Replaces every C with P. |
| `mod-2626` | Replace "C" with "Q" | Replaces every C with Q. |
| `mod-2627` | Replace "C" with "R" | Replaces every C with R. |
| `mod-2628` | Replace "C" with "S" | Replaces every C with S. |
| `mod-2629` | Replace "C" with "T" | Replaces every C with T. |
| `mod-2630` | Replace "C" with "U" | Replaces every C with U. |
| `mod-2631` | Replace "C" with "V" | Replaces every C with V. |
| `mod-2632` | Replace "C" with "W" | Replaces every C with W. |
| `mod-2633` | Replace "C" with "X" | Replaces every C with X. |
| `mod-2634` | Replace "C" with "Y" | Replaces every C with Y. |
| `mod-2635` | Replace "C" with "Z" | Replaces every C with Z. |
| `mod-2636` | Replace "C" with "0" | Replaces every C with 0. |
| `mod-2637` | Replace "C" with "1" | Replaces every C with 1. |
| `mod-2638` | Replace "C" with "2" | Replaces every C with 2. |
| `mod-2639` | Replace "C" with "3" | Replaces every C with 3. |
| `mod-2640` | Replace "C" with "4" | Replaces every C with 4. |
| `mod-2641` | Replace "C" with "5" | Replaces every C with 5. |
| `mod-2642` | Replace "C" with "6" | Replaces every C with 6. |
| `mod-2643` | Replace "C" with "7" | Replaces every C with 7. |
| `mod-2644` | Replace "C" with "8" | Replaces every C with 8. |
| `mod-2645` | Replace "C" with "9" | Replaces every C with 9. |
| `mod-2646` | Replace "D" with "a" | Replaces every D with a. |
| `mod-2647` | Replace "D" with "b" | Replaces every D with b. |
| `mod-2648` | Replace "D" with "c" | Replaces every D with c. |
| `mod-2649` | Replace "D" with "d" | Replaces every D with d. |
| `mod-2650` | Replace "D" with "e" | Replaces every D with e. |
| `mod-2651` | Replace "D" with "f" | Replaces every D with f. |
| `mod-2652` | Replace "D" with "g" | Replaces every D with g. |
| `mod-2653` | Replace "D" with "h" | Replaces every D with h. |
| `mod-2654` | Replace "D" with "i" | Replaces every D with i. |
| `mod-2655` | Replace "D" with "j" | Replaces every D with j. |
| `mod-2656` | Replace "D" with "k" | Replaces every D with k. |
| `mod-2657` | Replace "D" with "l" | Replaces every D with l. |
| `mod-2658` | Replace "D" with "m" | Replaces every D with m. |
| `mod-2659` | Replace "D" with "n" | Replaces every D with n. |
| `mod-2660` | Replace "D" with "o" | Replaces every D with o. |
| `mod-2661` | Replace "D" with "p" | Replaces every D with p. |
| `mod-2662` | Replace "D" with "q" | Replaces every D with q. |
| `mod-2663` | Replace "D" with "r" | Replaces every D with r. |
| `mod-2664` | Replace "D" with "s" | Replaces every D with s. |
| `mod-2665` | Replace "D" with "t" | Replaces every D with t. |
| `mod-2666` | Replace "D" with "u" | Replaces every D with u. |
| `mod-2667` | Replace "D" with "v" | Replaces every D with v. |
| `mod-2668` | Replace "D" with "w" | Replaces every D with w. |
| `mod-2669` | Replace "D" with "x" | Replaces every D with x. |
| `mod-2670` | Replace "D" with "y" | Replaces every D with y. |
| `mod-2671` | Replace "D" with "z" | Replaces every D with z. |
| `mod-2672` | Replace "D" with "A" | Replaces every D with A. |
| `mod-2673` | Replace "D" with "B" | Replaces every D with B. |
| `mod-2674` | Replace "D" with "C" | Replaces every D with C. |
| `mod-2675` | Replace "D" with "E" | Replaces every D with E. |
| `mod-2676` | Replace "D" with "F" | Replaces every D with F. |
| `mod-2677` | Replace "D" with "G" | Replaces every D with G. |
| `mod-2678` | Replace "D" with "H" | Replaces every D with H. |
| `mod-2679` | Replace "D" with "I" | Replaces every D with I. |
| `mod-2680` | Replace "D" with "J" | Replaces every D with J. |
| `mod-2681` | Replace "D" with "K" | Replaces every D with K. |
| `mod-2682` | Replace "D" with "L" | Replaces every D with L. |
| `mod-2683` | Replace "D" with "M" | Replaces every D with M. |
| `mod-2684` | Replace "D" with "N" | Replaces every D with N. |
| `mod-2685` | Replace "D" with "O" | Replaces every D with O. |
| `mod-2686` | Replace "D" with "P" | Replaces every D with P. |
| `mod-2687` | Replace "D" with "Q" | Replaces every D with Q. |
| `mod-2688` | Replace "D" with "R" | Replaces every D with R. |
| `mod-2689` | Replace "D" with "S" | Replaces every D with S. |
| `mod-2690` | Replace "D" with "T" | Replaces every D with T. |
| `mod-2691` | Replace "D" with "U" | Replaces every D with U. |
| `mod-2692` | Replace "D" with "V" | Replaces every D with V. |
| `mod-2693` | Replace "D" with "W" | Replaces every D with W. |
| `mod-2694` | Replace "D" with "X" | Replaces every D with X. |
| `mod-2695` | Replace "D" with "Y" | Replaces every D with Y. |
| `mod-2696` | Replace "D" with "Z" | Replaces every D with Z. |
| `mod-2697` | Replace "D" with "0" | Replaces every D with 0. |
| `mod-2698` | Replace "D" with "1" | Replaces every D with 1. |
| `mod-2699` | Replace "D" with "2" | Replaces every D with 2. |
| `mod-2700` | Replace "D" with "3" | Replaces every D with 3. |
| `mod-2701` | Replace "D" with "4" | Replaces every D with 4. |
| `mod-2702` | Replace "D" with "5" | Replaces every D with 5. |
| `mod-2703` | Replace "D" with "6" | Replaces every D with 6. |
| `mod-2704` | Replace "D" with "7" | Replaces every D with 7. |
| `mod-2705` | Replace "D" with "8" | Replaces every D with 8. |
| `mod-2706` | Replace "D" with "9" | Replaces every D with 9. |
| `mod-2707` | Replace "E" with "a" | Replaces every E with a. |
| `mod-2708` | Replace "E" with "b" | Replaces every E with b. |
| `mod-2709` | Replace "E" with "c" | Replaces every E with c. |
| `mod-2710` | Replace "E" with "d" | Replaces every E with d. |
| `mod-2711` | Replace "E" with "e" | Replaces every E with e. |
| `mod-2712` | Replace "E" with "f" | Replaces every E with f. |
| `mod-2713` | Replace "E" with "g" | Replaces every E with g. |
| `mod-2714` | Replace "E" with "h" | Replaces every E with h. |
| `mod-2715` | Replace "E" with "i" | Replaces every E with i. |
| `mod-2716` | Replace "E" with "j" | Replaces every E with j. |
| `mod-2717` | Replace "E" with "k" | Replaces every E with k. |
| `mod-2718` | Replace "E" with "l" | Replaces every E with l. |
| `mod-2719` | Replace "E" with "m" | Replaces every E with m. |
| `mod-2720` | Replace "E" with "n" | Replaces every E with n. |
| `mod-2721` | Replace "E" with "o" | Replaces every E with o. |
| `mod-2722` | Replace "E" with "p" | Replaces every E with p. |
| `mod-2723` | Replace "E" with "q" | Replaces every E with q. |
| `mod-2724` | Replace "E" with "r" | Replaces every E with r. |
| `mod-2725` | Replace "E" with "s" | Replaces every E with s. |
| `mod-2726` | Replace "E" with "t" | Replaces every E with t. |
| `mod-2727` | Replace "E" with "u" | Replaces every E with u. |
| `mod-2728` | Replace "E" with "v" | Replaces every E with v. |
| `mod-2729` | Replace "E" with "w" | Replaces every E with w. |
| `mod-2730` | Replace "E" with "x" | Replaces every E with x. |
| `mod-2731` | Replace "E" with "y" | Replaces every E with y. |
| `mod-2732` | Replace "E" with "z" | Replaces every E with z. |
| `mod-2733` | Replace "E" with "A" | Replaces every E with A. |
| `mod-2734` | Replace "E" with "B" | Replaces every E with B. |
| `mod-2735` | Replace "E" with "C" | Replaces every E with C. |
| `mod-2736` | Replace "E" with "D" | Replaces every E with D. |
| `mod-2737` | Replace "E" with "F" | Replaces every E with F. |
| `mod-2738` | Replace "E" with "G" | Replaces every E with G. |
| `mod-2739` | Replace "E" with "H" | Replaces every E with H. |
| `mod-2740` | Replace "E" with "I" | Replaces every E with I. |
| `mod-2741` | Replace "E" with "J" | Replaces every E with J. |
| `mod-2742` | Replace "E" with "K" | Replaces every E with K. |
| `mod-2743` | Replace "E" with "L" | Replaces every E with L. |
| `mod-2744` | Replace "E" with "M" | Replaces every E with M. |
| `mod-2745` | Replace "E" with "N" | Replaces every E with N. |
| `mod-2746` | Replace "E" with "O" | Replaces every E with O. |
| `mod-2747` | Replace "E" with "P" | Replaces every E with P. |
| `mod-2748` | Replace "E" with "Q" | Replaces every E with Q. |
| `mod-2749` | Replace "E" with "R" | Replaces every E with R. |
| `mod-2750` | Replace "E" with "S" | Replaces every E with S. |
| `mod-2751` | Replace "E" with "T" | Replaces every E with T. |
| `mod-2752` | Replace "E" with "U" | Replaces every E with U. |
| `mod-2753` | Replace "E" with "V" | Replaces every E with V. |
| `mod-2754` | Replace "E" with "W" | Replaces every E with W. |
| `mod-2755` | Replace "E" with "X" | Replaces every E with X. |
| `mod-2756` | Replace "E" with "Y" | Replaces every E with Y. |
| `mod-2757` | Replace "E" with "Z" | Replaces every E with Z. |
| `mod-2758` | Replace "E" with "0" | Replaces every E with 0. |
| `mod-2759` | Replace "E" with "1" | Replaces every E with 1. |
| `mod-2760` | Replace "E" with "2" | Replaces every E with 2. |
| `mod-2761` | Replace "E" with "3" | Replaces every E with 3. |
| `mod-2762` | Replace "E" with "4" | Replaces every E with 4. |
| `mod-2763` | Replace "E" with "5" | Replaces every E with 5. |
| `mod-2764` | Replace "E" with "6" | Replaces every E with 6. |
| `mod-2765` | Replace "E" with "7" | Replaces every E with 7. |
| `mod-2766` | Replace "E" with "8" | Replaces every E with 8. |
| `mod-2767` | Replace "E" with "9" | Replaces every E with 9. |
| `mod-2768` | Replace "F" with "a" | Replaces every F with a. |
| `mod-2769` | Replace "F" with "b" | Replaces every F with b. |
| `mod-2770` | Replace "F" with "c" | Replaces every F with c. |
| `mod-2771` | Replace "F" with "d" | Replaces every F with d. |
| `mod-2772` | Replace "F" with "e" | Replaces every F with e. |
| `mod-2773` | Replace "F" with "f" | Replaces every F with f. |
| `mod-2774` | Replace "F" with "g" | Replaces every F with g. |
| `mod-2775` | Replace "F" with "h" | Replaces every F with h. |
| `mod-2776` | Replace "F" with "i" | Replaces every F with i. |
| `mod-2777` | Replace "F" with "j" | Replaces every F with j. |
| `mod-2778` | Replace "F" with "k" | Replaces every F with k. |
| `mod-2779` | Replace "F" with "l" | Replaces every F with l. |
| `mod-2780` | Replace "F" with "m" | Replaces every F with m. |
| `mod-2781` | Replace "F" with "n" | Replaces every F with n. |
| `mod-2782` | Replace "F" with "o" | Replaces every F with o. |
| `mod-2783` | Replace "F" with "p" | Replaces every F with p. |
| `mod-2784` | Replace "F" with "q" | Replaces every F with q. |
| `mod-2785` | Replace "F" with "r" | Replaces every F with r. |
| `mod-2786` | Replace "F" with "s" | Replaces every F with s. |
| `mod-2787` | Replace "F" with "t" | Replaces every F with t. |
| `mod-2788` | Replace "F" with "u" | Replaces every F with u. |
| `mod-2789` | Replace "F" with "v" | Replaces every F with v. |
| `mod-2790` | Replace "F" with "w" | Replaces every F with w. |
| `mod-2791` | Replace "F" with "x" | Replaces every F with x. |
| `mod-2792` | Replace "F" with "y" | Replaces every F with y. |
| `mod-2793` | Replace "F" with "z" | Replaces every F with z. |
| `mod-2794` | Replace "F" with "A" | Replaces every F with A. |
| `mod-2795` | Replace "F" with "B" | Replaces every F with B. |
| `mod-2796` | Replace "F" with "C" | Replaces every F with C. |
| `mod-2797` | Replace "F" with "D" | Replaces every F with D. |
| `mod-2798` | Replace "F" with "E" | Replaces every F with E. |
| `mod-2799` | Replace "F" with "G" | Replaces every F with G. |
| `mod-2800` | Replace "F" with "H" | Replaces every F with H. |
| `mod-2801` | Replace "F" with "I" | Replaces every F with I. |
| `mod-2802` | Replace "F" with "J" | Replaces every F with J. |
| `mod-2803` | Replace "F" with "K" | Replaces every F with K. |
| `mod-2804` | Replace "F" with "L" | Replaces every F with L. |
| `mod-2805` | Replace "F" with "M" | Replaces every F with M. |
| `mod-2806` | Replace "F" with "N" | Replaces every F with N. |
| `mod-2807` | Replace "F" with "O" | Replaces every F with O. |
| `mod-2808` | Replace "F" with "P" | Replaces every F with P. |
| `mod-2809` | Replace "F" with "Q" | Replaces every F with Q. |
| `mod-2810` | Replace "F" with "R" | Replaces every F with R. |
| `mod-2811` | Replace "F" with "S" | Replaces every F with S. |
| `mod-2812` | Replace "F" with "T" | Replaces every F with T. |
| `mod-2813` | Replace "F" with "U" | Replaces every F with U. |
| `mod-2814` | Replace "F" with "V" | Replaces every F with V. |
| `mod-2815` | Replace "F" with "W" | Replaces every F with W. |
| `mod-2816` | Replace "F" with "X" | Replaces every F with X. |
| `mod-2817` | Replace "F" with "Y" | Replaces every F with Y. |
| `mod-2818` | Replace "F" with "Z" | Replaces every F with Z. |
| `mod-2819` | Replace "F" with "0" | Replaces every F with 0. |
| `mod-2820` | Replace "F" with "1" | Replaces every F with 1. |
| `mod-2821` | Replace "F" with "2" | Replaces every F with 2. |
| `mod-2822` | Replace "F" with "3" | Replaces every F with 3. |
| `mod-2823` | Replace "F" with "4" | Replaces every F with 4. |
| `mod-2824` | Replace "F" with "5" | Replaces every F with 5. |
| `mod-2825` | Replace "F" with "6" | Replaces every F with 6. |
| `mod-2826` | Replace "F" with "7" | Replaces every F with 7. |
| `mod-2827` | Replace "F" with "8" | Replaces every F with 8. |
| `mod-2828` | Replace "F" with "9" | Replaces every F with 9. |
| `mod-2829` | Replace "G" with "a" | Replaces every G with a. |
| `mod-2830` | Replace "G" with "b" | Replaces every G with b. |
| `mod-2831` | Replace "G" with "c" | Replaces every G with c. |
| `mod-2832` | Replace "G" with "d" | Replaces every G with d. |
| `mod-2833` | Replace "G" with "e" | Replaces every G with e. |
| `mod-2834` | Replace "G" with "f" | Replaces every G with f. |
| `mod-2835` | Replace "G" with "g" | Replaces every G with g. |
| `mod-2836` | Replace "G" with "h" | Replaces every G with h. |
| `mod-2837` | Replace "G" with "i" | Replaces every G with i. |
| `mod-2838` | Replace "G" with "j" | Replaces every G with j. |
| `mod-2839` | Replace "G" with "k" | Replaces every G with k. |
| `mod-2840` | Replace "G" with "l" | Replaces every G with l. |
| `mod-2841` | Replace "G" with "m" | Replaces every G with m. |
| `mod-2842` | Replace "G" with "n" | Replaces every G with n. |
| `mod-2843` | Replace "G" with "o" | Replaces every G with o. |
| `mod-2844` | Replace "G" with "p" | Replaces every G with p. |
| `mod-2845` | Replace "G" with "q" | Replaces every G with q. |
| `mod-2846` | Replace "G" with "r" | Replaces every G with r. |
| `mod-2847` | Replace "G" with "s" | Replaces every G with s. |
| `mod-2848` | Replace "G" with "t" | Replaces every G with t. |
| `mod-2849` | Replace "G" with "u" | Replaces every G with u. |
| `mod-2850` | Replace "G" with "v" | Replaces every G with v. |
| `mod-2851` | Replace "G" with "w" | Replaces every G with w. |
| `mod-2852` | Replace "G" with "x" | Replaces every G with x. |
| `mod-2853` | Replace "G" with "y" | Replaces every G with y. |
| `mod-2854` | Replace "G" with "z" | Replaces every G with z. |
| `mod-2855` | Replace "G" with "A" | Replaces every G with A. |
| `mod-2856` | Replace "G" with "B" | Replaces every G with B. |
| `mod-2857` | Replace "G" with "C" | Replaces every G with C. |
| `mod-2858` | Replace "G" with "D" | Replaces every G with D. |
| `mod-2859` | Replace "G" with "E" | Replaces every G with E. |
| `mod-2860` | Replace "G" with "F" | Replaces every G with F. |
| `mod-2861` | Replace "G" with "H" | Replaces every G with H. |
| `mod-2862` | Replace "G" with "I" | Replaces every G with I. |
| `mod-2863` | Replace "G" with "J" | Replaces every G with J. |
| `mod-2864` | Replace "G" with "K" | Replaces every G with K. |
| `mod-2865` | Replace "G" with "L" | Replaces every G with L. |
| `mod-2866` | Replace "G" with "M" | Replaces every G with M. |
| `mod-2867` | Replace "G" with "N" | Replaces every G with N. |
| `mod-2868` | Replace "G" with "O" | Replaces every G with O. |
| `mod-2869` | Replace "G" with "P" | Replaces every G with P. |
| `mod-2870` | Replace "G" with "Q" | Replaces every G with Q. |
| `mod-2871` | Replace "G" with "R" | Replaces every G with R. |
| `mod-2872` | Replace "G" with "S" | Replaces every G with S. |
| `mod-2873` | Replace "G" with "T" | Replaces every G with T. |
| `mod-2874` | Replace "G" with "U" | Replaces every G with U. |
| `mod-2875` | Replace "G" with "V" | Replaces every G with V. |
| `mod-2876` | Replace "G" with "W" | Replaces every G with W. |
| `mod-2877` | Replace "G" with "X" | Replaces every G with X. |
| `mod-2878` | Replace "G" with "Y" | Replaces every G with Y. |
| `mod-2879` | Replace "G" with "Z" | Replaces every G with Z. |
| `mod-2880` | Replace "G" with "0" | Replaces every G with 0. |
| `mod-2881` | Replace "G" with "1" | Replaces every G with 1. |
| `mod-2882` | Replace "G" with "2" | Replaces every G with 2. |
| `mod-2883` | Replace "G" with "3" | Replaces every G with 3. |
| `mod-2884` | Replace "G" with "4" | Replaces every G with 4. |
| `mod-2885` | Replace "G" with "5" | Replaces every G with 5. |
| `mod-2886` | Replace "G" with "6" | Replaces every G with 6. |
| `mod-2887` | Replace "G" with "7" | Replaces every G with 7. |
| `mod-2888` | Replace "G" with "8" | Replaces every G with 8. |
| `mod-2889` | Replace "G" with "9" | Replaces every G with 9. |
| `mod-2890` | Replace "H" with "a" | Replaces every H with a. |
| `mod-2891` | Replace "H" with "b" | Replaces every H with b. |
| `mod-2892` | Replace "H" with "c" | Replaces every H with c. |
| `mod-2893` | Replace "H" with "d" | Replaces every H with d. |
| `mod-2894` | Replace "H" with "e" | Replaces every H with e. |
| `mod-2895` | Replace "H" with "f" | Replaces every H with f. |
| `mod-2896` | Replace "H" with "g" | Replaces every H with g. |
| `mod-2897` | Replace "H" with "h" | Replaces every H with h. |
| `mod-2898` | Replace "H" with "i" | Replaces every H with i. |
| `mod-2899` | Replace "H" with "j" | Replaces every H with j. |
| `mod-2900` | Replace "H" with "k" | Replaces every H with k. |
| `mod-2901` | Replace "H" with "l" | Replaces every H with l. |
| `mod-2902` | Replace "H" with "m" | Replaces every H with m. |
| `mod-2903` | Replace "H" with "n" | Replaces every H with n. |
| `mod-2904` | Replace "H" with "o" | Replaces every H with o. |
| `mod-2905` | Replace "H" with "p" | Replaces every H with p. |
| `mod-2906` | Replace "H" with "q" | Replaces every H with q. |
| `mod-2907` | Replace "H" with "r" | Replaces every H with r. |
| `mod-2908` | Replace "H" with "s" | Replaces every H with s. |
| `mod-2909` | Replace "H" with "t" | Replaces every H with t. |
| `mod-2910` | Replace "H" with "u" | Replaces every H with u. |
| `mod-2911` | Replace "H" with "v" | Replaces every H with v. |
| `mod-2912` | Replace "H" with "w" | Replaces every H with w. |
| `mod-2913` | Replace "H" with "x" | Replaces every H with x. |
| `mod-2914` | Replace "H" with "y" | Replaces every H with y. |
| `mod-2915` | Replace "H" with "z" | Replaces every H with z. |
| `mod-2916` | Replace "H" with "A" | Replaces every H with A. |
| `mod-2917` | Replace "H" with "B" | Replaces every H with B. |
| `mod-2918` | Replace "H" with "C" | Replaces every H with C. |
| `mod-2919` | Replace "H" with "D" | Replaces every H with D. |
| `mod-2920` | Replace "H" with "E" | Replaces every H with E. |
| `mod-2921` | Replace "H" with "F" | Replaces every H with F. |
| `mod-2922` | Replace "H" with "G" | Replaces every H with G. |
| `mod-2923` | Replace "H" with "I" | Replaces every H with I. |
| `mod-2924` | Replace "H" with "J" | Replaces every H with J. |
| `mod-2925` | Replace "H" with "K" | Replaces every H with K. |
| `mod-2926` | Replace "H" with "L" | Replaces every H with L. |
| `mod-2927` | Replace "H" with "M" | Replaces every H with M. |
| `mod-2928` | Replace "H" with "N" | Replaces every H with N. |
| `mod-2929` | Replace "H" with "O" | Replaces every H with O. |
| `mod-2930` | Replace "H" with "P" | Replaces every H with P. |
| `mod-2931` | Replace "H" with "Q" | Replaces every H with Q. |
| `mod-2932` | Replace "H" with "R" | Replaces every H with R. |
| `mod-2933` | Replace "H" with "S" | Replaces every H with S. |
| `mod-2934` | Replace "H" with "T" | Replaces every H with T. |
| `mod-2935` | Replace "H" with "U" | Replaces every H with U. |
| `mod-2936` | Replace "H" with "V" | Replaces every H with V. |
| `mod-2937` | Replace "H" with "W" | Replaces every H with W. |
| `mod-2938` | Replace "H" with "X" | Replaces every H with X. |
| `mod-2939` | Replace "H" with "Y" | Replaces every H with Y. |
| `mod-2940` | Replace "H" with "Z" | Replaces every H with Z. |
| `mod-2941` | Replace "H" with "0" | Replaces every H with 0. |
| `mod-2942` | Replace "H" with "1" | Replaces every H with 1. |
| `mod-2943` | Replace "H" with "2" | Replaces every H with 2. |
| `mod-2944` | Replace "H" with "3" | Replaces every H with 3. |
| `mod-2945` | Replace "H" with "4" | Replaces every H with 4. |
| `mod-2946` | Replace "H" with "5" | Replaces every H with 5. |
| `mod-2947` | Replace "H" with "6" | Replaces every H with 6. |
| `mod-2948` | Replace "H" with "7" | Replaces every H with 7. |
| `mod-2949` | Replace "H" with "8" | Replaces every H with 8. |
| `mod-2950` | Replace "H" with "9" | Replaces every H with 9. |
| `mod-2951` | Replace "I" with "a" | Replaces every I with a. |
| `mod-2952` | Replace "I" with "b" | Replaces every I with b. |
| `mod-2953` | Replace "I" with "c" | Replaces every I with c. |
| `mod-2954` | Replace "I" with "d" | Replaces every I with d. |
| `mod-2955` | Replace "I" with "e" | Replaces every I with e. |
| `mod-2956` | Replace "I" with "f" | Replaces every I with f. |
| `mod-2957` | Replace "I" with "g" | Replaces every I with g. |
| `mod-2958` | Replace "I" with "h" | Replaces every I with h. |
| `mod-2959` | Replace "I" with "i" | Replaces every I with i. |
| `mod-2960` | Replace "I" with "j" | Replaces every I with j. |
| `mod-2961` | Replace "I" with "k" | Replaces every I with k. |
| `mod-2962` | Replace "I" with "l" | Replaces every I with l. |
| `mod-2963` | Replace "I" with "m" | Replaces every I with m. |
| `mod-2964` | Replace "I" with "n" | Replaces every I with n. |
| `mod-2965` | Replace "I" with "o" | Replaces every I with o. |
| `mod-2966` | Replace "I" with "p" | Replaces every I with p. |
| `mod-2967` | Replace "I" with "q" | Replaces every I with q. |
| `mod-2968` | Replace "I" with "r" | Replaces every I with r. |
| `mod-2969` | Replace "I" with "s" | Replaces every I with s. |
| `mod-2970` | Replace "I" with "t" | Replaces every I with t. |
| `mod-2971` | Replace "I" with "u" | Replaces every I with u. |
| `mod-2972` | Replace "I" with "v" | Replaces every I with v. |
| `mod-2973` | Replace "I" with "w" | Replaces every I with w. |
| `mod-2974` | Replace "I" with "x" | Replaces every I with x. |
| `mod-2975` | Replace "I" with "y" | Replaces every I with y. |
| `mod-2976` | Replace "I" with "z" | Replaces every I with z. |
| `mod-2977` | Replace "I" with "A" | Replaces every I with A. |
| `mod-24658` | Replace "I" with "B" | Replaces every I with B. |
| `mod-2979` | Replace "I" with "C" | Replaces every I with C. |
| `mod-2980` | Replace "I" with "D" | Replaces every I with D. |
| `mod-2981` | Replace "I" with "E" | Replaces every I with E. |
| `mod-2982` | Replace "I" with "F" | Replaces every I with F. |
| `mod-2983` | Replace "I" with "G" | Replaces every I with G. |
| `mod-2984` | Replace "I" with "H" | Replaces every I with H. |
| `mod-2985` | Replace "I" with "J" | Replaces every I with J. |
| `mod-2986` | Replace "I" with "K" | Replaces every I with K. |
| `mod-2987` | Replace "I" with "L" | Replaces every I with L. |
| `mod-2988` | Replace "I" with "M" | Replaces every I with M. |
| `mod-2989` | Replace "I" with "N" | Replaces every I with N. |
| `mod-2990` | Replace "I" with "O" | Replaces every I with O. |
| `mod-2991` | Replace "I" with "P" | Replaces every I with P. |
| `mod-2992` | Replace "I" with "Q" | Replaces every I with Q. |
| `mod-2993` | Replace "I" with "R" | Replaces every I with R. |
| `mod-2994` | Replace "I" with "S" | Replaces every I with S. |
| `mod-2995` | Replace "I" with "T" | Replaces every I with T. |
| `mod-2996` | Replace "I" with "U" | Replaces every I with U. |
| `mod-2997` | Replace "I" with "V" | Replaces every I with V. |
| `mod-2998` | Replace "I" with "W" | Replaces every I with W. |
| `mod-2999` | Replace "I" with "X" | Replaces every I with X. |
| `mod-3000` | Replace "I" with "Y" | Replaces every I with Y. |
| `mod-3001` | Replace "I" with "Z" | Replaces every I with Z. |
| `mod-3002` | Replace "I" with "0" | Replaces every I with 0. |
| `mod-3003` | Replace "I" with "1" | Replaces every I with 1. |
| `mod-3004` | Replace "I" with "2" | Replaces every I with 2. |
| `mod-3005` | Replace "I" with "3" | Replaces every I with 3. |
| `mod-3006` | Replace "I" with "4" | Replaces every I with 4. |
| `mod-3007` | Replace "I" with "5" | Replaces every I with 5. |
| `mod-3008` | Replace "I" with "6" | Replaces every I with 6. |
| `mod-3009` | Replace "I" with "7" | Replaces every I with 7. |
| `mod-3010` | Replace "I" with "8" | Replaces every I with 8. |
| `mod-3011` | Replace "I" with "9" | Replaces every I with 9. |
| `mod-3012` | Replace "J" with "a" | Replaces every J with a. |
| `mod-3013` | Replace "J" with "b" | Replaces every J with b. |
| `mod-3014` | Replace "J" with "c" | Replaces every J with c. |
| `mod-3015` | Replace "J" with "d" | Replaces every J with d. |
| `mod-3016` | Replace "J" with "e" | Replaces every J with e. |
| `mod-3017` | Replace "J" with "f" | Replaces every J with f. |
| `mod-3018` | Replace "J" with "g" | Replaces every J with g. |
| `mod-3019` | Replace "J" with "h" | Replaces every J with h. |
| `mod-3020` | Replace "J" with "i" | Replaces every J with i. |
| `mod-3021` | Replace "J" with "j" | Replaces every J with j. |
| `mod-3022` | Replace "J" with "k" | Replaces every J with k. |
| `mod-3023` | Replace "J" with "l" | Replaces every J with l. |
| `mod-3024` | Replace "J" with "m" | Replaces every J with m. |
| `mod-3025` | Replace "J" with "n" | Replaces every J with n. |
| `mod-3026` | Replace "J" with "o" | Replaces every J with o. |
| `mod-3027` | Replace "J" with "p" | Replaces every J with p. |
| `mod-3028` | Replace "J" with "q" | Replaces every J with q. |
| `mod-3029` | Replace "J" with "r" | Replaces every J with r. |
| `mod-3030` | Replace "J" with "s" | Replaces every J with s. |
| `mod-3031` | Replace "J" with "t" | Replaces every J with t. |
| `mod-3032` | Replace "J" with "u" | Replaces every J with u. |
| `mod-3033` | Replace "J" with "v" | Replaces every J with v. |
| `mod-3034` | Replace "J" with "w" | Replaces every J with w. |
| `mod-3035` | Replace "J" with "x" | Replaces every J with x. |
| `mod-3036` | Replace "J" with "y" | Replaces every J with y. |
| `mod-3037` | Replace "J" with "z" | Replaces every J with z. |
| `mod-3038` | Replace "J" with "A" | Replaces every J with A. |
| `mod-3039` | Replace "J" with "B" | Replaces every J with B. |
| `mod-3040` | Replace "J" with "C" | Replaces every J with C. |
| `mod-3041` | Replace "J" with "D" | Replaces every J with D. |
| `mod-3042` | Replace "J" with "E" | Replaces every J with E. |
| `mod-3043` | Replace "J" with "F" | Replaces every J with F. |
| `mod-3044` | Replace "J" with "G" | Replaces every J with G. |
| `mod-3045` | Replace "J" with "H" | Replaces every J with H. |
| `mod-3046` | Replace "J" with "I" | Replaces every J with I. |
| `mod-3047` | Replace "J" with "K" | Replaces every J with K. |
| `mod-3048` | Replace "J" with "L" | Replaces every J with L. |
| `mod-3049` | Replace "J" with "M" | Replaces every J with M. |
| `mod-3050` | Replace "J" with "N" | Replaces every J with N. |
| `mod-3051` | Replace "J" with "O" | Replaces every J with O. |
| `mod-3052` | Replace "J" with "P" | Replaces every J with P. |
| `mod-3053` | Replace "J" with "Q" | Replaces every J with Q. |
| `mod-3054` | Replace "J" with "R" | Replaces every J with R. |
| `mod-3055` | Replace "J" with "S" | Replaces every J with S. |
| `mod-3056` | Replace "J" with "T" | Replaces every J with T. |
| `mod-3057` | Replace "J" with "U" | Replaces every J with U. |
| `mod-3058` | Replace "J" with "V" | Replaces every J with V. |
| `mod-3059` | Replace "J" with "W" | Replaces every J with W. |
| `mod-3060` | Replace "J" with "X" | Replaces every J with X. |
| `mod-3061` | Replace "J" with "Y" | Replaces every J with Y. |
| `mod-3062` | Replace "J" with "Z" | Replaces every J with Z. |
| `mod-3063` | Replace "J" with "0" | Replaces every J with 0. |
| `mod-3064` | Replace "J" with "1" | Replaces every J with 1. |
| `mod-3065` | Replace "J" with "2" | Replaces every J with 2. |
| `mod-3066` | Replace "J" with "3" | Replaces every J with 3. |
| `mod-3067` | Replace "J" with "4" | Replaces every J with 4. |
| `mod-3068` | Replace "J" with "5" | Replaces every J with 5. |
| `mod-3069` | Replace "J" with "6" | Replaces every J with 6. |
| `mod-3070` | Replace "J" with "7" | Replaces every J with 7. |
| `mod-3071` | Replace "J" with "8" | Replaces every J with 8. |
| `mod-3072` | Replace "J" with "9" | Replaces every J with 9. |
| `mod-3073` | Replace "K" with "a" | Replaces every K with a. |
| `mod-3074` | Replace "K" with "b" | Replaces every K with b. |
| `mod-3075` | Replace "K" with "c" | Replaces every K with c. |
| `mod-3076` | Replace "K" with "d" | Replaces every K with d. |
| `mod-3077` | Replace "K" with "e" | Replaces every K with e. |
| `mod-3078` | Replace "K" with "f" | Replaces every K with f. |
| `mod-3079` | Replace "K" with "g" | Replaces every K with g. |
| `mod-3080` | Replace "K" with "h" | Replaces every K with h. |
| `mod-3081` | Replace "K" with "i" | Replaces every K with i. |
| `mod-3082` | Replace "K" with "j" | Replaces every K with j. |
| `mod-3083` | Replace "K" with "k" | Replaces every K with k. |
| `mod-3084` | Replace "K" with "l" | Replaces every K with l. |
| `mod-3085` | Replace "K" with "m" | Replaces every K with m. |
| `mod-3086` | Replace "K" with "n" | Replaces every K with n. |
| `mod-3087` | Replace "K" with "o" | Replaces every K with o. |
| `mod-3088` | Replace "K" with "p" | Replaces every K with p. |
| `mod-3089` | Replace "K" with "q" | Replaces every K with q. |
| `mod-3090` | Replace "K" with "r" | Replaces every K with r. |
| `mod-3091` | Replace "K" with "s" | Replaces every K with s. |
| `mod-3092` | Replace "K" with "t" | Replaces every K with t. |
| `mod-3093` | Replace "K" with "u" | Replaces every K with u. |
| `mod-3094` | Replace "K" with "v" | Replaces every K with v. |
| `mod-3095` | Replace "K" with "w" | Replaces every K with w. |
| `mod-3096` | Replace "K" with "x" | Replaces every K with x. |
| `mod-3097` | Replace "K" with "y" | Replaces every K with y. |
| `mod-3098` | Replace "K" with "z" | Replaces every K with z. |
| `mod-3099` | Replace "K" with "A" | Replaces every K with A. |
| `mod-3100` | Replace "K" with "B" | Replaces every K with B. |
| `mod-3101` | Replace "K" with "C" | Replaces every K with C. |
| `mod-3102` | Replace "K" with "D" | Replaces every K with D. |
| `mod-3103` | Replace "K" with "E" | Replaces every K with E. |
| `mod-3104` | Replace "K" with "F" | Replaces every K with F. |
| `mod-3105` | Replace "K" with "G" | Replaces every K with G. |
| `mod-3106` | Replace "K" with "H" | Replaces every K with H. |
| `mod-3107` | Replace "K" with "I" | Replaces every K with I. |
| `mod-3108` | Replace "K" with "J" | Replaces every K with J. |
| `mod-3109` | Replace "K" with "L" | Replaces every K with L. |
| `mod-3110` | Replace "K" with "M" | Replaces every K with M. |
| `mod-3111` | Replace "K" with "N" | Replaces every K with N. |
| `mod-3112` | Replace "K" with "O" | Replaces every K with O. |
| `mod-3113` | Replace "K" with "P" | Replaces every K with P. |
| `mod-3114` | Replace "K" with "Q" | Replaces every K with Q. |
| `mod-3115` | Replace "K" with "R" | Replaces every K with R. |
| `mod-3116` | Replace "K" with "S" | Replaces every K with S. |
| `mod-3117` | Replace "K" with "T" | Replaces every K with T. |
| `mod-3118` | Replace "K" with "U" | Replaces every K with U. |
| `mod-3119` | Replace "K" with "V" | Replaces every K with V. |
| `mod-3120` | Replace "K" with "W" | Replaces every K with W. |
| `mod-3121` | Replace "K" with "X" | Replaces every K with X. |
| `mod-3122` | Replace "K" with "Y" | Replaces every K with Y. |
| `mod-3123` | Replace "K" with "Z" | Replaces every K with Z. |
| `mod-3124` | Replace "K" with "0" | Replaces every K with 0. |
| `mod-3125` | Replace "K" with "1" | Replaces every K with 1. |
| `mod-3126` | Replace "K" with "2" | Replaces every K with 2. |
| `mod-3127` | Replace "K" with "3" | Replaces every K with 3. |
| `mod-3128` | Replace "K" with "4" | Replaces every K with 4. |
| `mod-3129` | Replace "K" with "5" | Replaces every K with 5. |
| `mod-3130` | Replace "K" with "6" | Replaces every K with 6. |
| `mod-3131` | Replace "K" with "7" | Replaces every K with 7. |
| `mod-3132` | Replace "K" with "8" | Replaces every K with 8. |
| `mod-3133` | Replace "K" with "9" | Replaces every K with 9. |
| `mod-3134` | Replace "L" with "a" | Replaces every L with a. |
| `mod-3135` | Replace "L" with "b" | Replaces every L with b. |
| `mod-3136` | Replace "L" with "c" | Replaces every L with c. |
| `mod-3137` | Replace "L" with "d" | Replaces every L with d. |
| `mod-3138` | Replace "L" with "e" | Replaces every L with e. |
| `mod-3139` | Replace "L" with "f" | Replaces every L with f. |
| `mod-3140` | Replace "L" with "g" | Replaces every L with g. |
| `mod-3141` | Replace "L" with "h" | Replaces every L with h. |
| `mod-3142` | Replace "L" with "i" | Replaces every L with i. |
| `mod-3143` | Replace "L" with "j" | Replaces every L with j. |
| `mod-3144` | Replace "L" with "k" | Replaces every L with k. |
| `mod-3145` | Replace "L" with "l" | Replaces every L with l. |
| `mod-3146` | Replace "L" with "m" | Replaces every L with m. |
| `mod-3147` | Replace "L" with "n" | Replaces every L with n. |
| `mod-3148` | Replace "L" with "o" | Replaces every L with o. |
| `mod-3149` | Replace "L" with "p" | Replaces every L with p. |
| `mod-3150` | Replace "L" with "q" | Replaces every L with q. |
| `mod-3151` | Replace "L" with "r" | Replaces every L with r. |
| `mod-3152` | Replace "L" with "s" | Replaces every L with s. |
| `mod-3153` | Replace "L" with "t" | Replaces every L with t. |
| `mod-3154` | Replace "L" with "u" | Replaces every L with u. |
| `mod-3155` | Replace "L" with "v" | Replaces every L with v. |
| `mod-3156` | Replace "L" with "w" | Replaces every L with w. |
| `mod-3157` | Replace "L" with "x" | Replaces every L with x. |
| `mod-3158` | Replace "L" with "y" | Replaces every L with y. |
| `mod-3159` | Replace "L" with "z" | Replaces every L with z. |
| `mod-3160` | Replace "L" with "A" | Replaces every L with A. |
| `mod-3161` | Replace "L" with "B" | Replaces every L with B. |
| `mod-3162` | Replace "L" with "C" | Replaces every L with C. |
| `mod-3163` | Replace "L" with "D" | Replaces every L with D. |
| `mod-3164` | Replace "L" with "E" | Replaces every L with E. |
| `mod-3165` | Replace "L" with "F" | Replaces every L with F. |
| `mod-3166` | Replace "L" with "G" | Replaces every L with G. |
| `mod-3167` | Replace "L" with "H" | Replaces every L with H. |
| `mod-3168` | Replace "L" with "I" | Replaces every L with I. |
| `mod-3169` | Replace "L" with "J" | Replaces every L with J. |
| `mod-3170` | Replace "L" with "K" | Replaces every L with K. |
| `mod-3171` | Replace "L" with "M" | Replaces every L with M. |
| `mod-3172` | Replace "L" with "N" | Replaces every L with N. |
| `mod-3173` | Replace "L" with "O" | Replaces every L with O. |
| `mod-3174` | Replace "L" with "P" | Replaces every L with P. |
| `mod-3175` | Replace "L" with "Q" | Replaces every L with Q. |
| `mod-3176` | Replace "L" with "R" | Replaces every L with R. |
| `mod-3177` | Replace "L" with "S" | Replaces every L with S. |
| `mod-3178` | Replace "L" with "T" | Replaces every L with T. |
| `mod-3179` | Replace "L" with "U" | Replaces every L with U. |
| `mod-3180` | Replace "L" with "V" | Replaces every L with V. |
| `mod-3181` | Replace "L" with "W" | Replaces every L with W. |
| `mod-3182` | Replace "L" with "X" | Replaces every L with X. |
| `mod-3183` | Replace "L" with "Y" | Replaces every L with Y. |
| `mod-3184` | Replace "L" with "Z" | Replaces every L with Z. |
| `mod-3185` | Replace "L" with "0" | Replaces every L with 0. |
| `mod-3186` | Replace "L" with "1" | Replaces every L with 1. |
| `mod-3187` | Replace "L" with "2" | Replaces every L with 2. |
| `mod-3188` | Replace "L" with "3" | Replaces every L with 3. |
| `mod-3189` | Replace "L" with "4" | Replaces every L with 4. |
| `mod-3190` | Replace "L" with "5" | Replaces every L with 5. |
| `mod-3191` | Replace "L" with "6" | Replaces every L with 6. |
| `mod-3192` | Replace "L" with "7" | Replaces every L with 7. |
| `mod-3193` | Replace "L" with "8" | Replaces every L with 8. |
| `mod-3194` | Replace "L" with "9" | Replaces every L with 9. |
| `mod-3195` | Replace "M" with "a" | Replaces every M with a. |
| `mod-3196` | Replace "M" with "b" | Replaces every M with b. |
| `mod-3197` | Replace "M" with "c" | Replaces every M with c. |
| `mod-3198` | Replace "M" with "d" | Replaces every M with d. |
| `mod-3199` | Replace "M" with "e" | Replaces every M with e. |
| `mod-3200` | Replace "M" with "f" | Replaces every M with f. |
| `mod-3201` | Replace "M" with "g" | Replaces every M with g. |
| `mod-3202` | Replace "M" with "h" | Replaces every M with h. |
| `mod-3203` | Replace "M" with "i" | Replaces every M with i. |
| `mod-3204` | Replace "M" with "j" | Replaces every M with j. |
| `mod-3205` | Replace "M" with "k" | Replaces every M with k. |
| `mod-3206` | Replace "M" with "l" | Replaces every M with l. |
| `mod-3207` | Replace "M" with "m" | Replaces every M with m. |
| `mod-3208` | Replace "M" with "n" | Replaces every M with n. |
| `mod-3209` | Replace "M" with "o" | Replaces every M with o. |
| `mod-3210` | Replace "M" with "p" | Replaces every M with p. |
| `mod-3211` | Replace "M" with "q" | Replaces every M with q. |
| `mod-3212` | Replace "M" with "r" | Replaces every M with r. |
| `mod-3213` | Replace "M" with "s" | Replaces every M with s. |
| `mod-3214` | Replace "M" with "t" | Replaces every M with t. |
| `mod-3215` | Replace "M" with "u" | Replaces every M with u. |
| `mod-3216` | Replace "M" with "v" | Replaces every M with v. |
| `mod-3217` | Replace "M" with "w" | Replaces every M with w. |
| `mod-3218` | Replace "M" with "x" | Replaces every M with x. |
| `mod-3219` | Replace "M" with "y" | Replaces every M with y. |
| `mod-3220` | Replace "M" with "z" | Replaces every M with z. |
| `mod-3221` | Replace "M" with "A" | Replaces every M with A. |
| `mod-3222` | Replace "M" with "B" | Replaces every M with B. |
| `mod-3223` | Replace "M" with "C" | Replaces every M with C. |
| `mod-3224` | Replace "M" with "D" | Replaces every M with D. |
| `mod-3225` | Replace "M" with "E" | Replaces every M with E. |
| `mod-3226` | Replace "M" with "F" | Replaces every M with F. |
| `mod-3227` | Replace "M" with "G" | Replaces every M with G. |
| `mod-3228` | Replace "M" with "H" | Replaces every M with H. |
| `mod-3229` | Replace "M" with "I" | Replaces every M with I. |
| `mod-3230` | Replace "M" with "J" | Replaces every M with J. |
| `mod-3231` | Replace "M" with "K" | Replaces every M with K. |
| `mod-3232` | Replace "M" with "L" | Replaces every M with L. |
| `mod-3233` | Replace "M" with "N" | Replaces every M with N. |
| `mod-3234` | Replace "M" with "O" | Replaces every M with O. |
| `mod-3235` | Replace "M" with "P" | Replaces every M with P. |
| `mod-3236` | Replace "M" with "Q" | Replaces every M with Q. |
| `mod-3237` | Replace "M" with "R" | Replaces every M with R. |
| `mod-3238` | Replace "M" with "S" | Replaces every M with S. |
| `mod-3239` | Replace "M" with "T" | Replaces every M with T. |
| `mod-3240` | Replace "M" with "U" | Replaces every M with U. |
| `mod-3241` | Replace "M" with "V" | Replaces every M with V. |
| `mod-3242` | Replace "M" with "W" | Replaces every M with W. |
| `mod-3243` | Replace "M" with "X" | Replaces every M with X. |
| `mod-3244` | Replace "M" with "Y" | Replaces every M with Y. |
| `mod-3245` | Replace "M" with "Z" | Replaces every M with Z. |
| `mod-3246` | Replace "M" with "0" | Replaces every M with 0. |
| `mod-3247` | Replace "M" with "1" | Replaces every M with 1. |
| `mod-3248` | Replace "M" with "2" | Replaces every M with 2. |
| `mod-3249` | Replace "M" with "3" | Replaces every M with 3. |
| `mod-3250` | Replace "M" with "4" | Replaces every M with 4. |
| `mod-3251` | Replace "M" with "5" | Replaces every M with 5. |
| `mod-3252` | Replace "M" with "6" | Replaces every M with 6. |
| `mod-3253` | Replace "M" with "7" | Replaces every M with 7. |
| `mod-3254` | Replace "M" with "8" | Replaces every M with 8. |
| `mod-3255` | Replace "M" with "9" | Replaces every M with 9. |
| `mod-3256` | Replace "N" with "a" | Replaces every N with a. |
| `mod-3257` | Replace "N" with "b" | Replaces every N with b. |
| `mod-3258` | Replace "N" with "c" | Replaces every N with c. |
| `mod-3259` | Replace "N" with "d" | Replaces every N with d. |
| `mod-3260` | Replace "N" with "e" | Replaces every N with e. |
| `mod-3261` | Replace "N" with "f" | Replaces every N with f. |
| `mod-3262` | Replace "N" with "g" | Replaces every N with g. |
| `mod-3263` | Replace "N" with "h" | Replaces every N with h. |
| `mod-3264` | Replace "N" with "i" | Replaces every N with i. |
| `mod-3265` | Replace "N" with "j" | Replaces every N with j. |
| `mod-3266` | Replace "N" with "k" | Replaces every N with k. |
| `mod-3267` | Replace "N" with "l" | Replaces every N with l. |
| `mod-3268` | Replace "N" with "m" | Replaces every N with m. |
| `mod-3269` | Replace "N" with "n" | Replaces every N with n. |
| `mod-3270` | Replace "N" with "o" | Replaces every N with o. |
| `mod-3271` | Replace "N" with "p" | Replaces every N with p. |
| `mod-3272` | Replace "N" with "q" | Replaces every N with q. |
| `mod-3273` | Replace "N" with "r" | Replaces every N with r. |
| `mod-3274` | Replace "N" with "s" | Replaces every N with s. |
| `mod-3275` | Replace "N" with "t" | Replaces every N with t. |
| `mod-3276` | Replace "N" with "u" | Replaces every N with u. |
| `mod-3277` | Replace "N" with "v" | Replaces every N with v. |
| `mod-3278` | Replace "N" with "w" | Replaces every N with w. |
| `mod-3279` | Replace "N" with "x" | Replaces every N with x. |
| `mod-3280` | Replace "N" with "y" | Replaces every N with y. |
| `mod-3281` | Replace "N" with "z" | Replaces every N with z. |
| `mod-3282` | Replace "N" with "A" | Replaces every N with A. |
| `mod-3283` | Replace "N" with "B" | Replaces every N with B. |
| `mod-3284` | Replace "N" with "C" | Replaces every N with C. |
| `mod-3285` | Replace "N" with "D" | Replaces every N with D. |
| `mod-3286` | Replace "N" with "E" | Replaces every N with E. |
| `mod-3287` | Replace "N" with "F" | Replaces every N with F. |
| `mod-3288` | Replace "N" with "G" | Replaces every N with G. |
| `mod-3289` | Replace "N" with "H" | Replaces every N with H. |
| `mod-3290` | Replace "N" with "I" | Replaces every N with I. |
| `mod-3291` | Replace "N" with "J" | Replaces every N with J. |
| `mod-3292` | Replace "N" with "K" | Replaces every N with K. |
| `mod-3293` | Replace "N" with "L" | Replaces every N with L. |
| `mod-3294` | Replace "N" with "M" | Replaces every N with M. |
| `mod-3295` | Replace "N" with "O" | Replaces every N with O. |
| `mod-3296` | Replace "N" with "P" | Replaces every N with P. |
| `mod-3297` | Replace "N" with "Q" | Replaces every N with Q. |
| `mod-3298` | Replace "N" with "R" | Replaces every N with R. |
| `mod-3299` | Replace "N" with "S" | Replaces every N with S. |
| `mod-3300` | Replace "N" with "T" | Replaces every N with T. |
| `mod-3301` | Replace "N" with "U" | Replaces every N with U. |
| `mod-3302` | Replace "N" with "V" | Replaces every N with V. |
| `mod-3303` | Replace "N" with "W" | Replaces every N with W. |
| `mod-3304` | Replace "N" with "X" | Replaces every N with X. |
| `mod-3305` | Replace "N" with "Y" | Replaces every N with Y. |
| `mod-3306` | Replace "N" with "Z" | Replaces every N with Z. |
| `mod-3307` | Replace "N" with "0" | Replaces every N with 0. |
| `mod-3308` | Replace "N" with "1" | Replaces every N with 1. |
| `mod-3309` | Replace "N" with "2" | Replaces every N with 2. |
| `mod-3310` | Replace "N" with "3" | Replaces every N with 3. |
| `mod-3311` | Replace "N" with "4" | Replaces every N with 4. |
| `mod-3312` | Replace "N" with "5" | Replaces every N with 5. |
| `mod-3313` | Replace "N" with "6" | Replaces every N with 6. |
| `mod-3314` | Replace "N" with "7" | Replaces every N with 7. |
| `mod-3315` | Replace "N" with "8" | Replaces every N with 8. |
| `mod-3316` | Replace "N" with "9" | Replaces every N with 9. |
| `mod-3317` | Replace "O" with "a" | Replaces every O with a. |
| `mod-3318` | Replace "O" with "b" | Replaces every O with b. |
| `mod-3319` | Replace "O" with "c" | Replaces every O with c. |
| `mod-3320` | Replace "O" with "d" | Replaces every O with d. |
| `mod-3321` | Replace "O" with "e" | Replaces every O with e. |
| `mod-3322` | Replace "O" with "f" | Replaces every O with f. |
| `mod-3323` | Replace "O" with "g" | Replaces every O with g. |
| `mod-3324` | Replace "O" with "h" | Replaces every O with h. |
| `mod-3325` | Replace "O" with "i" | Replaces every O with i. |
| `mod-3326` | Replace "O" with "j" | Replaces every O with j. |
| `mod-3327` | Replace "O" with "k" | Replaces every O with k. |
| `mod-3328` | Replace "O" with "l" | Replaces every O with l. |
| `mod-3329` | Replace "O" with "m" | Replaces every O with m. |
| `mod-3330` | Replace "O" with "n" | Replaces every O with n. |
| `mod-3331` | Replace "O" with "o" | Replaces every O with o. |
| `mod-3332` | Replace "O" with "p" | Replaces every O with p. |
| `mod-3333` | Replace "O" with "q" | Replaces every O with q. |
| `mod-3334` | Replace "O" with "r" | Replaces every O with r. |
| `mod-3335` | Replace "O" with "s" | Replaces every O with s. |
| `mod-3336` | Replace "O" with "t" | Replaces every O with t. |
| `mod-3337` | Replace "O" with "u" | Replaces every O with u. |
| `mod-3338` | Replace "O" with "v" | Replaces every O with v. |
| `mod-3339` | Replace "O" with "w" | Replaces every O with w. |
| `mod-3340` | Replace "O" with "x" | Replaces every O with x. |
| `mod-3341` | Replace "O" with "y" | Replaces every O with y. |
| `mod-3342` | Replace "O" with "z" | Replaces every O with z. |
| `mod-3343` | Replace "O" with "A" | Replaces every O with A. |
| `mod-3344` | Replace "O" with "B" | Replaces every O with B. |
| `mod-3345` | Replace "O" with "C" | Replaces every O with C. |
| `mod-3346` | Replace "O" with "D" | Replaces every O with D. |
| `mod-3347` | Replace "O" with "E" | Replaces every O with E. |
| `mod-3348` | Replace "O" with "F" | Replaces every O with F. |
| `mod-3349` | Replace "O" with "G" | Replaces every O with G. |
| `mod-3350` | Replace "O" with "H" | Replaces every O with H. |
| `mod-3351` | Replace "O" with "I" | Replaces every O with I. |
| `mod-3352` | Replace "O" with "J" | Replaces every O with J. |
| `mod-3353` | Replace "O" with "K" | Replaces every O with K. |
| `mod-3354` | Replace "O" with "L" | Replaces every O with L. |
| `mod-3355` | Replace "O" with "M" | Replaces every O with M. |
| `mod-3356` | Replace "O" with "N" | Replaces every O with N. |
| `mod-3357` | Replace "O" with "P" | Replaces every O with P. |
| `mod-3358` | Replace "O" with "Q" | Replaces every O with Q. |
| `mod-3359` | Replace "O" with "R" | Replaces every O with R. |
| `mod-3360` | Replace "O" with "S" | Replaces every O with S. |
| `mod-3361` | Replace "O" with "T" | Replaces every O with T. |
| `mod-3362` | Replace "O" with "U" | Replaces every O with U. |
| `mod-3363` | Replace "O" with "V" | Replaces every O with V. |
| `mod-3364` | Replace "O" with "W" | Replaces every O with W. |
| `mod-3365` | Replace "O" with "X" | Replaces every O with X. |
| `mod-3366` | Replace "O" with "Y" | Replaces every O with Y. |
| `mod-3367` | Replace "O" with "Z" | Replaces every O with Z. |
| `mod-3368` | Replace "O" with "0" | Replaces every O with 0. |
| `mod-3369` | Replace "O" with "1" | Replaces every O with 1. |
| `mod-3370` | Replace "O" with "2" | Replaces every O with 2. |
| `mod-3371` | Replace "O" with "3" | Replaces every O with 3. |
| `mod-3372` | Replace "O" with "4" | Replaces every O with 4. |
| `mod-3373` | Replace "O" with "5" | Replaces every O with 5. |
| `mod-3374` | Replace "O" with "6" | Replaces every O with 6. |
| `mod-3375` | Replace "O" with "7" | Replaces every O with 7. |
| `mod-3376` | Replace "O" with "8" | Replaces every O with 8. |
| `mod-3377` | Replace "O" with "9" | Replaces every O with 9. |
| `mod-3378` | Replace "P" with "a" | Replaces every P with a. |
| `mod-3379` | Replace "P" with "b" | Replaces every P with b. |
| `mod-3380` | Replace "P" with "c" | Replaces every P with c. |
| `mod-3381` | Replace "P" with "d" | Replaces every P with d. |
| `mod-3382` | Replace "P" with "e" | Replaces every P with e. |
| `mod-3383` | Replace "P" with "f" | Replaces every P with f. |
| `mod-3384` | Replace "P" with "g" | Replaces every P with g. |
| `mod-3385` | Replace "P" with "h" | Replaces every P with h. |
| `mod-3386` | Replace "P" with "i" | Replaces every P with i. |
| `mod-3387` | Replace "P" with "j" | Replaces every P with j. |
| `mod-3388` | Replace "P" with "k" | Replaces every P with k. |
| `mod-3389` | Replace "P" with "l" | Replaces every P with l. |
| `mod-3390` | Replace "P" with "m" | Replaces every P with m. |
| `mod-3391` | Replace "P" with "n" | Replaces every P with n. |
| `mod-3392` | Replace "P" with "o" | Replaces every P with o. |
| `mod-3393` | Replace "P" with "p" | Replaces every P with p. |
| `mod-3394` | Replace "P" with "q" | Replaces every P with q. |
| `mod-3395` | Replace "P" with "r" | Replaces every P with r. |
| `mod-3396` | Replace "P" with "s" | Replaces every P with s. |
| `mod-3397` | Replace "P" with "t" | Replaces every P with t. |
| `mod-3398` | Replace "P" with "u" | Replaces every P with u. |
| `mod-3399` | Replace "P" with "v" | Replaces every P with v. |
| `mod-3400` | Replace "P" with "w" | Replaces every P with w. |
| `mod-3401` | Replace "P" with "x" | Replaces every P with x. |
| `mod-3402` | Replace "P" with "y" | Replaces every P with y. |
| `mod-3403` | Replace "P" with "z" | Replaces every P with z. |
| `mod-3404` | Replace "P" with "A" | Replaces every P with A. |
| `mod-3405` | Replace "P" with "B" | Replaces every P with B. |
| `mod-3406` | Replace "P" with "C" | Replaces every P with C. |
| `mod-3407` | Replace "P" with "D" | Replaces every P with D. |
| `mod-3408` | Replace "P" with "E" | Replaces every P with E. |
| `mod-3409` | Replace "P" with "F" | Replaces every P with F. |
| `mod-3410` | Replace "P" with "G" | Replaces every P with G. |
| `mod-3411` | Replace "P" with "H" | Replaces every P with H. |
| `mod-3412` | Replace "P" with "I" | Replaces every P with I. |
| `mod-3413` | Replace "P" with "J" | Replaces every P with J. |
| `mod-3414` | Replace "P" with "K" | Replaces every P with K. |
| `mod-3415` | Replace "P" with "L" | Replaces every P with L. |
| `mod-3416` | Replace "P" with "M" | Replaces every P with M. |
| `mod-3417` | Replace "P" with "N" | Replaces every P with N. |
| `mod-3418` | Replace "P" with "O" | Replaces every P with O. |
| `mod-3419` | Replace "P" with "Q" | Replaces every P with Q. |
| `mod-3420` | Replace "P" with "R" | Replaces every P with R. |
| `mod-3421` | Replace "P" with "S" | Replaces every P with S. |
| `mod-3422` | Replace "P" with "T" | Replaces every P with T. |
| `mod-3423` | Replace "P" with "U" | Replaces every P with U. |
| `mod-3424` | Replace "P" with "V" | Replaces every P with V. |
| `mod-3425` | Replace "P" with "W" | Replaces every P with W. |
| `mod-3426` | Replace "P" with "X" | Replaces every P with X. |
| `mod-3427` | Replace "P" with "Y" | Replaces every P with Y. |
| `mod-3428` | Replace "P" with "Z" | Replaces every P with Z. |
| `mod-3429` | Replace "P" with "0" | Replaces every P with 0. |
| `mod-3430` | Replace "P" with "1" | Replaces every P with 1. |
| `mod-3431` | Replace "P" with "2" | Replaces every P with 2. |
| `mod-3432` | Replace "P" with "3" | Replaces every P with 3. |
| `mod-3433` | Replace "P" with "4" | Replaces every P with 4. |
| `mod-3434` | Replace "P" with "5" | Replaces every P with 5. |
| `mod-3435` | Replace "P" with "6" | Replaces every P with 6. |
| `mod-3436` | Replace "P" with "7" | Replaces every P with 7. |
| `mod-3437` | Replace "P" with "8" | Replaces every P with 8. |
| `mod-3438` | Replace "P" with "9" | Replaces every P with 9. |
| `mod-3439` | Replace "Q" with "a" | Replaces every Q with a. |
| `mod-3440` | Replace "Q" with "b" | Replaces every Q with b. |
| `mod-3441` | Replace "Q" with "c" | Replaces every Q with c. |
| `mod-3442` | Replace "Q" with "d" | Replaces every Q with d. |
| `mod-3443` | Replace "Q" with "e" | Replaces every Q with e. |
| `mod-3444` | Replace "Q" with "f" | Replaces every Q with f. |
| `mod-3445` | Replace "Q" with "g" | Replaces every Q with g. |
| `mod-3446` | Replace "Q" with "h" | Replaces every Q with h. |
| `mod-3447` | Replace "Q" with "i" | Replaces every Q with i. |
| `mod-3448` | Replace "Q" with "j" | Replaces every Q with j. |
| `mod-3449` | Replace "Q" with "k" | Replaces every Q with k. |
| `mod-3450` | Replace "Q" with "l" | Replaces every Q with l. |
| `mod-3451` | Replace "Q" with "m" | Replaces every Q with m. |
| `mod-3452` | Replace "Q" with "n" | Replaces every Q with n. |
| `mod-3453` | Replace "Q" with "o" | Replaces every Q with o. |
| `mod-3454` | Replace "Q" with "p" | Replaces every Q with p. |
| `mod-3455` | Replace "Q" with "q" | Replaces every Q with q. |
| `mod-3456` | Replace "Q" with "r" | Replaces every Q with r. |
| `mod-3457` | Replace "Q" with "s" | Replaces every Q with s. |
| `mod-3458` | Replace "Q" with "t" | Replaces every Q with t. |
| `mod-3459` | Replace "Q" with "u" | Replaces every Q with u. |
| `mod-3460` | Replace "Q" with "v" | Replaces every Q with v. |
| `mod-3461` | Replace "Q" with "w" | Replaces every Q with w. |
| `mod-3462` | Replace "Q" with "x" | Replaces every Q with x. |
| `mod-3463` | Replace "Q" with "y" | Replaces every Q with y. |
| `mod-3464` | Replace "Q" with "z" | Replaces every Q with z. |
| `mod-3465` | Replace "Q" with "A" | Replaces every Q with A. |
| `mod-3466` | Replace "Q" with "B" | Replaces every Q with B. |
| `mod-3467` | Replace "Q" with "C" | Replaces every Q with C. |
| `mod-3468` | Replace "Q" with "D" | Replaces every Q with D. |
| `mod-3469` | Replace "Q" with "E" | Replaces every Q with E. |
| `mod-3470` | Replace "Q" with "F" | Replaces every Q with F. |
| `mod-3471` | Replace "Q" with "G" | Replaces every Q with G. |
| `mod-3472` | Replace "Q" with "H" | Replaces every Q with H. |
| `mod-3473` | Replace "Q" with "I" | Replaces every Q with I. |
| `mod-3474` | Replace "Q" with "J" | Replaces every Q with J. |
| `mod-3475` | Replace "Q" with "K" | Replaces every Q with K. |
| `mod-3476` | Replace "Q" with "L" | Replaces every Q with L. |
| `mod-3477` | Replace "Q" with "M" | Replaces every Q with M. |
| `mod-3478` | Replace "Q" with "N" | Replaces every Q with N. |
| `mod-3479` | Replace "Q" with "O" | Replaces every Q with O. |
| `mod-3480` | Replace "Q" with "P" | Replaces every Q with P. |
| `mod-3481` | Replace "Q" with "R" | Replaces every Q with R. |
| `mod-3482` | Replace "Q" with "S" | Replaces every Q with S. |
| `mod-3483` | Replace "Q" with "T" | Replaces every Q with T. |
| `mod-3484` | Replace "Q" with "U" | Replaces every Q with U. |
| `mod-3485` | Replace "Q" with "V" | Replaces every Q with V. |
| `mod-3486` | Replace "Q" with "W" | Replaces every Q with W. |
| `mod-3487` | Replace "Q" with "X" | Replaces every Q with X. |
| `mod-3488` | Replace "Q" with "Y" | Replaces every Q with Y. |
| `mod-3489` | Replace "Q" with "Z" | Replaces every Q with Z. |
| `mod-3490` | Replace "Q" with "0" | Replaces every Q with 0. |
| `mod-3491` | Replace "Q" with "1" | Replaces every Q with 1. |
| `mod-3492` | Replace "Q" with "2" | Replaces every Q with 2. |
| `mod-3493` | Replace "Q" with "3" | Replaces every Q with 3. |
| `mod-3494` | Replace "Q" with "4" | Replaces every Q with 4. |
| `mod-3495` | Replace "Q" with "5" | Replaces every Q with 5. |
| `mod-3496` | Replace "Q" with "6" | Replaces every Q with 6. |
| `mod-3497` | Replace "Q" with "7" | Replaces every Q with 7. |
| `mod-3498` | Replace "Q" with "8" | Replaces every Q with 8. |
| `mod-3499` | Replace "Q" with "9" | Replaces every Q with 9. |
| `mod-3500` | Replace "R" with "a" | Replaces every R with a. |
| `mod-3501` | Replace "R" with "b" | Replaces every R with b. |
| `mod-3502` | Replace "R" with "c" | Replaces every R with c. |
| `mod-3503` | Replace "R" with "d" | Replaces every R with d. |
| `mod-3504` | Replace "R" with "e" | Replaces every R with e. |
| `mod-3505` | Replace "R" with "f" | Replaces every R with f. |
| `mod-3506` | Replace "R" with "g" | Replaces every R with g. |
| `mod-3507` | Replace "R" with "h" | Replaces every R with h. |
| `mod-3508` | Replace "R" with "i" | Replaces every R with i. |
| `mod-3509` | Replace "R" with "j" | Replaces every R with j. |
| `mod-3510` | Replace "R" with "k" | Replaces every R with k. |
| `mod-3511` | Replace "R" with "l" | Replaces every R with l. |
| `mod-3512` | Replace "R" with "m" | Replaces every R with m. |
| `mod-3513` | Replace "R" with "n" | Replaces every R with n. |
| `mod-3514` | Replace "R" with "o" | Replaces every R with o. |
| `mod-3515` | Replace "R" with "p" | Replaces every R with p. |
| `mod-3516` | Replace "R" with "q" | Replaces every R with q. |
| `mod-3517` | Replace "R" with "r" | Replaces every R with r. |
| `mod-3518` | Replace "R" with "s" | Replaces every R with s. |
| `mod-3519` | Replace "R" with "t" | Replaces every R with t. |
| `mod-3520` | Replace "R" with "u" | Replaces every R with u. |
| `mod-3521` | Replace "R" with "v" | Replaces every R with v. |
| `mod-3522` | Replace "R" with "w" | Replaces every R with w. |
| `mod-3523` | Replace "R" with "x" | Replaces every R with x. |
| `mod-3524` | Replace "R" with "y" | Replaces every R with y. |
| `mod-3525` | Replace "R" with "z" | Replaces every R with z. |
| `mod-3526` | Replace "R" with "A" | Replaces every R with A. |
| `mod-3527` | Replace "R" with "B" | Replaces every R with B. |
| `mod-3528` | Replace "R" with "C" | Replaces every R with C. |
| `mod-3529` | Replace "R" with "D" | Replaces every R with D. |
| `mod-3530` | Replace "R" with "E" | Replaces every R with E. |
| `mod-3531` | Replace "R" with "F" | Replaces every R with F. |
| `mod-3532` | Replace "R" with "G" | Replaces every R with G. |
| `mod-3533` | Replace "R" with "H" | Replaces every R with H. |
| `mod-3534` | Replace "R" with "I" | Replaces every R with I. |
| `mod-3535` | Replace "R" with "J" | Replaces every R with J. |
| `mod-3536` | Replace "R" with "K" | Replaces every R with K. |
| `mod-3537` | Replace "R" with "L" | Replaces every R with L. |
| `mod-3538` | Replace "R" with "M" | Replaces every R with M. |
| `mod-3539` | Replace "R" with "N" | Replaces every R with N. |
| `mod-3540` | Replace "R" with "O" | Replaces every R with O. |
| `mod-3541` | Replace "R" with "P" | Replaces every R with P. |
| `mod-3542` | Replace "R" with "Q" | Replaces every R with Q. |
| `mod-3543` | Replace "R" with "S" | Replaces every R with S. |
| `mod-3544` | Replace "R" with "T" | Replaces every R with T. |
| `mod-3545` | Replace "R" with "U" | Replaces every R with U. |
| `mod-3546` | Replace "R" with "V" | Replaces every R with V. |
| `mod-3547` | Replace "R" with "W" | Replaces every R with W. |
| `mod-3548` | Replace "R" with "X" | Replaces every R with X. |
| `mod-3549` | Replace "R" with "Y" | Replaces every R with Y. |
| `mod-3550` | Replace "R" with "Z" | Replaces every R with Z. |
| `mod-3551` | Replace "R" with "0" | Replaces every R with 0. |
| `mod-3552` | Replace "R" with "1" | Replaces every R with 1. |
| `mod-3553` | Replace "R" with "2" | Replaces every R with 2. |
| `mod-3554` | Replace "R" with "3" | Replaces every R with 3. |
| `mod-3555` | Replace "R" with "4" | Replaces every R with 4. |
| `mod-3556` | Replace "R" with "5" | Replaces every R with 5. |
| `mod-3557` | Replace "R" with "6" | Replaces every R with 6. |
| `mod-3558` | Replace "R" with "7" | Replaces every R with 7. |
| `mod-3559` | Replace "R" with "8" | Replaces every R with 8. |
| `mod-3560` | Replace "R" with "9" | Replaces every R with 9. |
| `mod-3561` | Replace "S" with "a" | Replaces every S with a. |
| `mod-3562` | Replace "S" with "b" | Replaces every S with b. |
| `mod-3563` | Replace "S" with "c" | Replaces every S with c. |
| `mod-3564` | Replace "S" with "d" | Replaces every S with d. |
| `mod-3565` | Replace "S" with "e" | Replaces every S with e. |
| `mod-3566` | Replace "S" with "f" | Replaces every S with f. |
| `mod-3567` | Replace "S" with "g" | Replaces every S with g. |
| `mod-3568` | Replace "S" with "h" | Replaces every S with h. |
| `mod-3569` | Replace "S" with "i" | Replaces every S with i. |
| `mod-3570` | Replace "S" with "j" | Replaces every S with j. |
| `mod-3571` | Replace "S" with "k" | Replaces every S with k. |
| `mod-3572` | Replace "S" with "l" | Replaces every S with l. |
| `mod-3573` | Replace "S" with "m" | Replaces every S with m. |
| `mod-3574` | Replace "S" with "n" | Replaces every S with n. |
| `mod-3575` | Replace "S" with "o" | Replaces every S with o. |
| `mod-3576` | Replace "S" with "p" | Replaces every S with p. |
| `mod-3577` | Replace "S" with "q" | Replaces every S with q. |
| `mod-3578` | Replace "S" with "r" | Replaces every S with r. |
| `mod-3579` | Replace "S" with "s" | Replaces every S with s. |
| `mod-3580` | Replace "S" with "t" | Replaces every S with t. |
| `mod-3581` | Replace "S" with "u" | Replaces every S with u. |
| `mod-3582` | Replace "S" with "v" | Replaces every S with v. |
| `mod-3583` | Replace "S" with "w" | Replaces every S with w. |
| `mod-3584` | Replace "S" with "x" | Replaces every S with x. |
| `mod-3585` | Replace "S" with "y" | Replaces every S with y. |
| `mod-3586` | Replace "S" with "z" | Replaces every S with z. |
| `mod-3587` | Replace "S" with "A" | Replaces every S with A. |
| `mod-3588` | Replace "S" with "B" | Replaces every S with B. |
| `mod-3589` | Replace "S" with "C" | Replaces every S with C. |
| `mod-3590` | Replace "S" with "D" | Replaces every S with D. |
| `mod-3591` | Replace "S" with "E" | Replaces every S with E. |
| `mod-3592` | Replace "S" with "F" | Replaces every S with F. |
| `mod-3593` | Replace "S" with "G" | Replaces every S with G. |
| `mod-3594` | Replace "S" with "H" | Replaces every S with H. |
| `mod-3595` | Replace "S" with "I" | Replaces every S with I. |
| `mod-3596` | Replace "S" with "J" | Replaces every S with J. |
| `mod-3597` | Replace "S" with "K" | Replaces every S with K. |
| `mod-3598` | Replace "S" with "L" | Replaces every S with L. |
| `mod-3599` | Replace "S" with "M" | Replaces every S with M. |
| `mod-3600` | Replace "S" with "N" | Replaces every S with N. |
| `mod-3601` | Replace "S" with "O" | Replaces every S with O. |
| `mod-3602` | Replace "S" with "P" | Replaces every S with P. |
| `mod-3603` | Replace "S" with "Q" | Replaces every S with Q. |
| `mod-3604` | Replace "S" with "R" | Replaces every S with R. |
| `mod-3605` | Replace "S" with "T" | Replaces every S with T. |
| `mod-3606` | Replace "S" with "U" | Replaces every S with U. |
| `mod-3607` | Replace "S" with "V" | Replaces every S with V. |
| `mod-3608` | Replace "S" with "W" | Replaces every S with W. |
| `mod-3609` | Replace "S" with "X" | Replaces every S with X. |
| `mod-3610` | Replace "S" with "Y" | Replaces every S with Y. |
| `mod-3611` | Replace "S" with "Z" | Replaces every S with Z. |
| `mod-3612` | Replace "S" with "0" | Replaces every S with 0. |
| `mod-3613` | Replace "S" with "1" | Replaces every S with 1. |
| `mod-3614` | Replace "S" with "2" | Replaces every S with 2. |
| `mod-3615` | Replace "S" with "3" | Replaces every S with 3. |
| `mod-3616` | Replace "S" with "4" | Replaces every S with 4. |
| `mod-3617` | Replace "S" with "5" | Replaces every S with 5. |
| `mod-3618` | Replace "S" with "6" | Replaces every S with 6. |
| `mod-3619` | Replace "S" with "7" | Replaces every S with 7. |
| `mod-3620` | Replace "S" with "8" | Replaces every S with 8. |
| `mod-3621` | Replace "S" with "9" | Replaces every S with 9. |
| `mod-3622` | Replace "T" with "a" | Replaces every T with a. |
| `mod-3623` | Replace "T" with "b" | Replaces every T with b. |
| `mod-3624` | Replace "T" with "c" | Replaces every T with c. |
| `mod-3625` | Replace "T" with "d" | Replaces every T with d. |
| `mod-3626` | Replace "T" with "e" | Replaces every T with e. |
| `mod-3627` | Replace "T" with "f" | Replaces every T with f. |
| `mod-3628` | Replace "T" with "g" | Replaces every T with g. |
| `mod-3629` | Replace "T" with "h" | Replaces every T with h. |
| `mod-3630` | Replace "T" with "i" | Replaces every T with i. |
| `mod-3631` | Replace "T" with "j" | Replaces every T with j. |
| `mod-3632` | Replace "T" with "k" | Replaces every T with k. |
| `mod-3633` | Replace "T" with "l" | Replaces every T with l. |
| `mod-3634` | Replace "T" with "m" | Replaces every T with m. |
| `mod-3635` | Replace "T" with "n" | Replaces every T with n. |
| `mod-3636` | Replace "T" with "o" | Replaces every T with o. |
| `mod-3637` | Replace "T" with "p" | Replaces every T with p. |
| `mod-3638` | Replace "T" with "q" | Replaces every T with q. |
| `mod-3639` | Replace "T" with "r" | Replaces every T with r. |
| `mod-3640` | Replace "T" with "s" | Replaces every T with s. |
| `mod-3641` | Replace "T" with "t" | Replaces every T with t. |
| `mod-3642` | Replace "T" with "u" | Replaces every T with u. |
| `mod-3643` | Replace "T" with "v" | Replaces every T with v. |
| `mod-3644` | Replace "T" with "w" | Replaces every T with w. |
| `mod-3645` | Replace "T" with "x" | Replaces every T with x. |
| `mod-3646` | Replace "T" with "y" | Replaces every T with y. |
| `mod-3647` | Replace "T" with "z" | Replaces every T with z. |
| `mod-3648` | Replace "T" with "A" | Replaces every T with A. |
| `mod-3649` | Replace "T" with "B" | Replaces every T with B. |
| `mod-3650` | Replace "T" with "C" | Replaces every T with C. |
| `mod-3651` | Replace "T" with "D" | Replaces every T with D. |
| `mod-3652` | Replace "T" with "E" | Replaces every T with E. |
| `mod-3653` | Replace "T" with "F" | Replaces every T with F. |
| `mod-3654` | Replace "T" with "G" | Replaces every T with G. |
| `mod-3655` | Replace "T" with "H" | Replaces every T with H. |
| `mod-3656` | Replace "T" with "I" | Replaces every T with I. |
| `mod-3657` | Replace "T" with "J" | Replaces every T with J. |
| `mod-3658` | Replace "T" with "K" | Replaces every T with K. |
| `mod-3659` | Replace "T" with "L" | Replaces every T with L. |
| `mod-3660` | Replace "T" with "M" | Replaces every T with M. |
| `mod-3661` | Replace "T" with "N" | Replaces every T with N. |
| `mod-3662` | Replace "T" with "O" | Replaces every T with O. |
| `mod-3663` | Replace "T" with "P" | Replaces every T with P. |
| `mod-3664` | Replace "T" with "Q" | Replaces every T with Q. |
| `mod-3665` | Replace "T" with "R" | Replaces every T with R. |
| `mod-3666` | Replace "T" with "S" | Replaces every T with S. |
| `mod-3667` | Replace "T" with "U" | Replaces every T with U. |
| `mod-3668` | Replace "T" with "V" | Replaces every T with V. |
| `mod-3669` | Replace "T" with "W" | Replaces every T with W. |
| `mod-3670` | Replace "T" with "X" | Replaces every T with X. |
| `mod-3671` | Replace "T" with "Y" | Replaces every T with Y. |
| `mod-3672` | Replace "T" with "Z" | Replaces every T with Z. |
| `mod-3673` | Replace "T" with "0" | Replaces every T with 0. |
| `mod-3674` | Replace "T" with "1" | Replaces every T with 1. |
| `mod-3675` | Replace "T" with "2" | Replaces every T with 2. |
| `mod-3676` | Replace "T" with "3" | Replaces every T with 3. |
| `mod-3677` | Replace "T" with "4" | Replaces every T with 4. |
| `mod-3678` | Replace "T" with "5" | Replaces every T with 5. |
| `mod-3679` | Replace "T" with "6" | Replaces every T with 6. |
| `mod-3680` | Replace "T" with "7" | Replaces every T with 7. |
| `mod-3681` | Replace "T" with "8" | Replaces every T with 8. |
| `mod-3682` | Replace "T" with "9" | Replaces every T with 9. |
| `mod-3683` | Replace "U" with "a" | Replaces every U with a. |
| `mod-3684` | Replace "U" with "b" | Replaces every U with b. |
| `mod-3685` | Replace "U" with "c" | Replaces every U with c. |
| `mod-3686` | Replace "U" with "d" | Replaces every U with d. |
| `mod-3687` | Replace "U" with "e" | Replaces every U with e. |
| `mod-3688` | Replace "U" with "f" | Replaces every U with f. |
| `mod-3689` | Replace "U" with "g" | Replaces every U with g. |
| `mod-3690` | Replace "U" with "h" | Replaces every U with h. |
| `mod-3691` | Replace "U" with "i" | Replaces every U with i. |
| `mod-3692` | Replace "U" with "j" | Replaces every U with j. |
| `mod-3693` | Replace "U" with "k" | Replaces every U with k. |
| `mod-3694` | Replace "U" with "l" | Replaces every U with l. |
| `mod-3695` | Replace "U" with "m" | Replaces every U with m. |
| `mod-3696` | Replace "U" with "n" | Replaces every U with n. |
| `mod-3697` | Replace "U" with "o" | Replaces every U with o. |
| `mod-3698` | Replace "U" with "p" | Replaces every U with p. |
| `mod-3699` | Replace "U" with "q" | Replaces every U with q. |
| `mod-3700` | Replace "U" with "r" | Replaces every U with r. |
| `mod-3701` | Replace "U" with "s" | Replaces every U with s. |
| `mod-3702` | Replace "U" with "t" | Replaces every U with t. |
| `mod-3703` | Replace "U" with "u" | Replaces every U with u. |
| `mod-3704` | Replace "U" with "v" | Replaces every U with v. |
| `mod-3705` | Replace "U" with "w" | Replaces every U with w. |
| `mod-3706` | Replace "U" with "x" | Replaces every U with x. |
| `mod-3707` | Replace "U" with "y" | Replaces every U with y. |
| `mod-3708` | Replace "U" with "z" | Replaces every U with z. |
| `mod-3709` | Replace "U" with "A" | Replaces every U with A. |
| `mod-3710` | Replace "U" with "B" | Replaces every U with B. |
| `mod-3711` | Replace "U" with "C" | Replaces every U with C. |
| `mod-3712` | Replace "U" with "D" | Replaces every U with D. |
| `mod-3713` | Replace "U" with "E" | Replaces every U with E. |
| `mod-3714` | Replace "U" with "F" | Replaces every U with F. |
| `mod-3715` | Replace "U" with "G" | Replaces every U with G. |
| `mod-3716` | Replace "U" with "H" | Replaces every U with H. |
| `mod-3717` | Replace "U" with "I" | Replaces every U with I. |
| `mod-3718` | Replace "U" with "J" | Replaces every U with J. |
| `mod-3719` | Replace "U" with "K" | Replaces every U with K. |
| `mod-3720` | Replace "U" with "L" | Replaces every U with L. |
| `mod-3721` | Replace "U" with "M" | Replaces every U with M. |
| `mod-3722` | Replace "U" with "N" | Replaces every U with N. |
| `mod-3723` | Replace "U" with "O" | Replaces every U with O. |
| `mod-3724` | Replace "U" with "P" | Replaces every U with P. |
| `mod-3725` | Replace "U" with "Q" | Replaces every U with Q. |
| `mod-3726` | Replace "U" with "R" | Replaces every U with R. |
| `mod-3727` | Replace "U" with "S" | Replaces every U with S. |
| `mod-3728` | Replace "U" with "T" | Replaces every U with T. |
| `mod-3729` | Replace "U" with "V" | Replaces every U with V. |
| `mod-3730` | Replace "U" with "W" | Replaces every U with W. |
| `mod-3731` | Replace "U" with "X" | Replaces every U with X. |
| `mod-3732` | Replace "U" with "Y" | Replaces every U with Y. |
| `mod-3733` | Replace "U" with "Z" | Replaces every U with Z. |
| `mod-3734` | Replace "U" with "0" | Replaces every U with 0. |
| `mod-3735` | Replace "U" with "1" | Replaces every U with 1. |
| `mod-3736` | Replace "U" with "2" | Replaces every U with 2. |
| `mod-3737` | Replace "U" with "3" | Replaces every U with 3. |
| `mod-3738` | Replace "U" with "4" | Replaces every U with 4. |
| `mod-3739` | Replace "U" with "5" | Replaces every U with 5. |
| `mod-3740` | Replace "U" with "6" | Replaces every U with 6. |
| `mod-3741` | Replace "U" with "7" | Replaces every U with 7. |
| `mod-3742` | Replace "U" with "8" | Replaces every U with 8. |
| `mod-3743` | Replace "U" with "9" | Replaces every U with 9. |
| `mod-3744` | Replace "V" with "a" | Replaces every V with a. |
| `mod-3745` | Replace "V" with "b" | Replaces every V with b. |
| `mod-3746` | Replace "V" with "c" | Replaces every V with c. |
| `mod-3747` | Replace "V" with "d" | Replaces every V with d. |
| `mod-3748` | Replace "V" with "e" | Replaces every V with e. |
| `mod-3749` | Replace "V" with "f" | Replaces every V with f. |
| `mod-3750` | Replace "V" with "g" | Replaces every V with g. |
| `mod-3751` | Replace "V" with "h" | Replaces every V with h. |
| `mod-3752` | Replace "V" with "i" | Replaces every V with i. |
| `mod-3753` | Replace "V" with "j" | Replaces every V with j. |
| `mod-3754` | Replace "V" with "k" | Replaces every V with k. |
| `mod-3755` | Replace "V" with "l" | Replaces every V with l. |
| `mod-3756` | Replace "V" with "m" | Replaces every V with m. |
| `mod-3757` | Replace "V" with "n" | Replaces every V with n. |
| `mod-3758` | Replace "V" with "o" | Replaces every V with o. |
| `mod-3759` | Replace "V" with "p" | Replaces every V with p. |
| `mod-3760` | Replace "V" with "q" | Replaces every V with q. |
| `mod-3761` | Replace "V" with "r" | Replaces every V with r. |
| `mod-3762` | Replace "V" with "s" | Replaces every V with s. |
| `mod-3763` | Replace "V" with "t" | Replaces every V with t. |
| `mod-3764` | Replace "V" with "u" | Replaces every V with u. |
| `mod-3765` | Replace "V" with "v" | Replaces every V with v. |
| `mod-3766` | Replace "V" with "w" | Replaces every V with w. |
| `mod-3767` | Replace "V" with "x" | Replaces every V with x. |
| `mod-3768` | Replace "V" with "y" | Replaces every V with y. |
| `mod-3769` | Replace "V" with "z" | Replaces every V with z. |
| `mod-3770` | Replace "V" with "A" | Replaces every V with A. |
| `mod-3771` | Replace "V" with "B" | Replaces every V with B. |
| `mod-3772` | Replace "V" with "C" | Replaces every V with C. |
| `mod-3773` | Replace "V" with "D" | Replaces every V with D. |
| `mod-3774` | Replace "V" with "E" | Replaces every V with E. |
| `mod-3775` | Replace "V" with "F" | Replaces every V with F. |
| `mod-3776` | Replace "V" with "G" | Replaces every V with G. |
| `mod-3777` | Replace "V" with "H" | Replaces every V with H. |
| `mod-3778` | Replace "V" with "I" | Replaces every V with I. |
| `mod-3779` | Replace "V" with "J" | Replaces every V with J. |
| `mod-3780` | Replace "V" with "K" | Replaces every V with K. |
| `mod-3781` | Replace "V" with "L" | Replaces every V with L. |
| `mod-3782` | Replace "V" with "M" | Replaces every V with M. |
| `mod-3783` | Replace "V" with "N" | Replaces every V with N. |
| `mod-3784` | Replace "V" with "O" | Replaces every V with O. |
| `mod-3785` | Replace "V" with "P" | Replaces every V with P. |
| `mod-3786` | Replace "V" with "Q" | Replaces every V with Q. |
| `mod-3787` | Replace "V" with "R" | Replaces every V with R. |
| `mod-3788` | Replace "V" with "S" | Replaces every V with S. |
| `mod-3789` | Replace "V" with "T" | Replaces every V with T. |
| `mod-3790` | Replace "V" with "U" | Replaces every V with U. |
| `mod-3791` | Replace "V" with "W" | Replaces every V with W. |
| `mod-3792` | Replace "V" with "X" | Replaces every V with X. |
| `mod-3793` | Replace "V" with "Y" | Replaces every V with Y. |
| `mod-3794` | Replace "V" with "Z" | Replaces every V with Z. |
| `mod-3795` | Replace "V" with "0" | Replaces every V with 0. |
| `mod-3796` | Replace "V" with "1" | Replaces every V with 1. |
| `mod-3797` | Replace "V" with "2" | Replaces every V with 2. |
| `mod-3798` | Replace "V" with "3" | Replaces every V with 3. |
| `mod-3799` | Replace "V" with "4" | Replaces every V with 4. |
| `mod-3800` | Replace "V" with "5" | Replaces every V with 5. |
| `mod-3801` | Replace "V" with "6" | Replaces every V with 6. |
| `mod-3802` | Replace "V" with "7" | Replaces every V with 7. |
| `mod-3803` | Replace "V" with "8" | Replaces every V with 8. |
| `mod-3804` | Replace "V" with "9" | Replaces every V with 9. |
| `mod-3805` | Replace "W" with "a" | Replaces every W with a. |
| `mod-3806` | Replace "W" with "b" | Replaces every W with b. |
| `mod-3807` | Replace "W" with "c" | Replaces every W with c. |
| `mod-3808` | Replace "W" with "d" | Replaces every W with d. |
| `mod-3809` | Replace "W" with "e" | Replaces every W with e. |
| `mod-3810` | Replace "W" with "f" | Replaces every W with f. |
| `mod-3811` | Replace "W" with "g" | Replaces every W with g. |
| `mod-3812` | Replace "W" with "h" | Replaces every W with h. |
| `mod-3813` | Replace "W" with "i" | Replaces every W with i. |
| `mod-3814` | Replace "W" with "j" | Replaces every W with j. |
| `mod-3815` | Replace "W" with "k" | Replaces every W with k. |
| `mod-3816` | Replace "W" with "l" | Replaces every W with l. |
| `mod-3817` | Replace "W" with "m" | Replaces every W with m. |
| `mod-3818` | Replace "W" with "n" | Replaces every W with n. |
| `mod-3819` | Replace "W" with "o" | Replaces every W with o. |
| `mod-3820` | Replace "W" with "p" | Replaces every W with p. |
| `mod-3821` | Replace "W" with "q" | Replaces every W with q. |
| `mod-3822` | Replace "W" with "r" | Replaces every W with r. |
| `mod-3823` | Replace "W" with "s" | Replaces every W with s. |
| `mod-3824` | Replace "W" with "t" | Replaces every W with t. |
| `mod-3825` | Replace "W" with "u" | Replaces every W with u. |
| `mod-3826` | Replace "W" with "v" | Replaces every W with v. |
| `mod-3827` | Replace "W" with "w" | Replaces every W with w. |
| `mod-3828` | Replace "W" with "x" | Replaces every W with x. |
| `mod-3829` | Replace "W" with "y" | Replaces every W with y. |
| `mod-3830` | Replace "W" with "z" | Replaces every W with z. |
| `mod-3831` | Replace "W" with "A" | Replaces every W with A. |
| `mod-3832` | Replace "W" with "B" | Replaces every W with B. |
| `mod-3833` | Replace "W" with "C" | Replaces every W with C. |
| `mod-3834` | Replace "W" with "D" | Replaces every W with D. |
| `mod-3835` | Replace "W" with "E" | Replaces every W with E. |
| `mod-3836` | Replace "W" with "F" | Replaces every W with F. |
| `mod-3837` | Replace "W" with "G" | Replaces every W with G. |
| `mod-3838` | Replace "W" with "H" | Replaces every W with H. |
| `mod-3839` | Replace "W" with "I" | Replaces every W with I. |
| `mod-3840` | Replace "W" with "J" | Replaces every W with J. |
| `mod-3841` | Replace "W" with "K" | Replaces every W with K. |
| `mod-3842` | Replace "W" with "L" | Replaces every W with L. |
| `mod-3843` | Replace "W" with "M" | Replaces every W with M. |
| `mod-3844` | Replace "W" with "N" | Replaces every W with N. |
| `mod-3845` | Replace "W" with "O" | Replaces every W with O. |
| `mod-3846` | Replace "W" with "P" | Replaces every W with P. |
| `mod-3847` | Replace "W" with "Q" | Replaces every W with Q. |
| `mod-3848` | Replace "W" with "R" | Replaces every W with R. |
| `mod-3849` | Replace "W" with "S" | Replaces every W with S. |
| `mod-3850` | Replace "W" with "T" | Replaces every W with T. |
| `mod-3851` | Replace "W" with "U" | Replaces every W with U. |
| `mod-3852` | Replace "W" with "V" | Replaces every W with V. |
| `mod-3853` | Replace "W" with "X" | Replaces every W with X. |
| `mod-3854` | Replace "W" with "Y" | Replaces every W with Y. |
| `mod-3855` | Replace "W" with "Z" | Replaces every W with Z. |
| `mod-3856` | Replace "W" with "0" | Replaces every W with 0. |
| `mod-3857` | Replace "W" with "1" | Replaces every W with 1. |
| `mod-3858` | Replace "W" with "2" | Replaces every W with 2. |
| `mod-3859` | Replace "W" with "3" | Replaces every W with 3. |
| `mod-3860` | Replace "W" with "4" | Replaces every W with 4. |
| `mod-3861` | Replace "W" with "5" | Replaces every W with 5. |
| `mod-3862` | Replace "W" with "6" | Replaces every W with 6. |
| `mod-3863` | Replace "W" with "7" | Replaces every W with 7. |
| `mod-3864` | Replace "W" with "8" | Replaces every W with 8. |
| `mod-3865` | Replace "W" with "9" | Replaces every W with 9. |
| `mod-3866` | Replace "X" with "a" | Replaces every X with a. |
| `mod-3867` | Replace "X" with "b" | Replaces every X with b. |
| `mod-3868` | Replace "X" with "c" | Replaces every X with c. |
| `mod-3869` | Replace "X" with "d" | Replaces every X with d. |
| `mod-3870` | Replace "X" with "e" | Replaces every X with e. |
| `mod-3871` | Replace "X" with "f" | Replaces every X with f. |
| `mod-3872` | Replace "X" with "g" | Replaces every X with g. |
| `mod-3873` | Replace "X" with "h" | Replaces every X with h. |
| `mod-3874` | Replace "X" with "i" | Replaces every X with i. |
| `mod-3875` | Replace "X" with "j" | Replaces every X with j. |
| `mod-3876` | Replace "X" with "k" | Replaces every X with k. |
| `mod-3877` | Replace "X" with "l" | Replaces every X with l. |
| `mod-3878` | Replace "X" with "m" | Replaces every X with m. |
| `mod-3879` | Replace "X" with "n" | Replaces every X with n. |
| `mod-3880` | Replace "X" with "o" | Replaces every X with o. |
| `mod-3881` | Replace "X" with "p" | Replaces every X with p. |
| `mod-3882` | Replace "X" with "q" | Replaces every X with q. |
| `mod-3883` | Replace "X" with "r" | Replaces every X with r. |
| `mod-3884` | Replace "X" with "s" | Replaces every X with s. |
| `mod-3885` | Replace "X" with "t" | Replaces every X with t. |
| `mod-3886` | Replace "X" with "u" | Replaces every X with u. |
| `mod-3887` | Replace "X" with "v" | Replaces every X with v. |
| `mod-3888` | Replace "X" with "w" | Replaces every X with w. |
| `mod-3889` | Replace "X" with "x" | Replaces every X with x. |
| `mod-3890` | Replace "X" with "y" | Replaces every X with y. |
| `mod-3891` | Replace "X" with "z" | Replaces every X with z. |
| `mod-3892` | Replace "X" with "A" | Replaces every X with A. |
| `mod-3893` | Replace "X" with "B" | Replaces every X with B. |
| `mod-3894` | Replace "X" with "C" | Replaces every X with C. |
| `mod-3895` | Replace "X" with "D" | Replaces every X with D. |
| `mod-3896` | Replace "X" with "E" | Replaces every X with E. |
| `mod-3897` | Replace "X" with "F" | Replaces every X with F. |
| `mod-3898` | Replace "X" with "G" | Replaces every X with G. |
| `mod-3899` | Replace "X" with "H" | Replaces every X with H. |
| `mod-3900` | Replace "X" with "I" | Replaces every X with I. |
| `mod-3901` | Replace "X" with "J" | Replaces every X with J. |
| `mod-3902` | Replace "X" with "K" | Replaces every X with K. |
| `mod-3903` | Replace "X" with "L" | Replaces every X with L. |
| `mod-3904` | Replace "X" with "M" | Replaces every X with M. |
| `mod-3905` | Replace "X" with "N" | Replaces every X with N. |
| `mod-3906` | Replace "X" with "O" | Replaces every X with O. |
| `mod-3907` | Replace "X" with "P" | Replaces every X with P. |
| `mod-3908` | Replace "X" with "Q" | Replaces every X with Q. |
| `mod-3909` | Replace "X" with "R" | Replaces every X with R. |
| `mod-3910` | Replace "X" with "S" | Replaces every X with S. |
| `mod-3911` | Replace "X" with "T" | Replaces every X with T. |
| `mod-3912` | Replace "X" with "U" | Replaces every X with U. |
| `mod-3913` | Replace "X" with "V" | Replaces every X with V. |
| `mod-3914` | Replace "X" with "W" | Replaces every X with W. |
| `mod-3915` | Replace "X" with "Y" | Replaces every X with Y. |
| `mod-3916` | Replace "X" with "Z" | Replaces every X with Z. |
| `mod-3917` | Replace "X" with "0" | Replaces every X with 0. |
| `mod-3918` | Replace "X" with "1" | Replaces every X with 1. |
| `mod-3919` | Replace "X" with "2" | Replaces every X with 2. |
| `mod-3920` | Replace "X" with "3" | Replaces every X with 3. |
| `mod-3921` | Replace "X" with "4" | Replaces every X with 4. |
| `mod-3922` | Replace "X" with "5" | Replaces every X with 5. |
| `mod-3923` | Replace "X" with "6" | Replaces every X with 6. |
| `mod-3924` | Replace "X" with "7" | Replaces every X with 7. |
| `mod-3925` | Replace "X" with "8" | Replaces every X with 8. |
| `mod-3926` | Replace "X" with "9" | Replaces every X with 9. |
| `mod-3927` | Replace "Y" with "a" | Replaces every Y with a. |
| `mod-3928` | Replace "Y" with "b" | Replaces every Y with b. |
| `mod-3929` | Replace "Y" with "c" | Replaces every Y with c. |
| `mod-3930` | Replace "Y" with "d" | Replaces every Y with d. |
| `mod-3931` | Replace "Y" with "e" | Replaces every Y with e. |
| `mod-3932` | Replace "Y" with "f" | Replaces every Y with f. |
| `mod-3933` | Replace "Y" with "g" | Replaces every Y with g. |
| `mod-3934` | Replace "Y" with "h" | Replaces every Y with h. |
| `mod-3935` | Replace "Y" with "i" | Replaces every Y with i. |
| `mod-3936` | Replace "Y" with "j" | Replaces every Y with j. |
| `mod-3937` | Replace "Y" with "k" | Replaces every Y with k. |
| `mod-3938` | Replace "Y" with "l" | Replaces every Y with l. |
| `mod-3939` | Replace "Y" with "m" | Replaces every Y with m. |
| `mod-3940` | Replace "Y" with "n" | Replaces every Y with n. |
| `mod-3941` | Replace "Y" with "o" | Replaces every Y with o. |
| `mod-3942` | Replace "Y" with "p" | Replaces every Y with p. |
| `mod-3943` | Replace "Y" with "q" | Replaces every Y with q. |
| `mod-3944` | Replace "Y" with "r" | Replaces every Y with r. |
| `mod-3945` | Replace "Y" with "s" | Replaces every Y with s. |
| `mod-3946` | Replace "Y" with "t" | Replaces every Y with t. |
| `mod-3947` | Replace "Y" with "u" | Replaces every Y with u. |
| `mod-3948` | Replace "Y" with "v" | Replaces every Y with v. |
| `mod-3949` | Replace "Y" with "w" | Replaces every Y with w. |
| `mod-3950` | Replace "Y" with "x" | Replaces every Y with x. |
| `mod-3951` | Replace "Y" with "y" | Replaces every Y with y. |
| `mod-3952` | Replace "Y" with "z" | Replaces every Y with z. |
| `mod-3953` | Replace "Y" with "A" | Replaces every Y with A. |
| `mod-3954` | Replace "Y" with "B" | Replaces every Y with B. |
| `mod-3955` | Replace "Y" with "C" | Replaces every Y with C. |
| `mod-3956` | Replace "Y" with "D" | Replaces every Y with D. |
| `mod-3957` | Replace "Y" with "E" | Replaces every Y with E. |
| `mod-3958` | Replace "Y" with "F" | Replaces every Y with F. |
| `mod-3959` | Replace "Y" with "G" | Replaces every Y with G. |
| `mod-3960` | Replace "Y" with "H" | Replaces every Y with H. |
| `mod-3961` | Replace "Y" with "I" | Replaces every Y with I. |
| `mod-3962` | Replace "Y" with "J" | Replaces every Y with J. |
| `mod-3963` | Replace "Y" with "K" | Replaces every Y with K. |
| `mod-3964` | Replace "Y" with "L" | Replaces every Y with L. |
| `mod-3965` | Replace "Y" with "M" | Replaces every Y with M. |
| `mod-3966` | Replace "Y" with "N" | Replaces every Y with N. |
| `mod-3967` | Replace "Y" with "O" | Replaces every Y with O. |
| `mod-3968` | Replace "Y" with "P" | Replaces every Y with P. |
| `mod-3969` | Replace "Y" with "Q" | Replaces every Y with Q. |
| `mod-3970` | Replace "Y" with "R" | Replaces every Y with R. |
| `mod-3971` | Replace "Y" with "S" | Replaces every Y with S. |
| `mod-3972` | Replace "Y" with "T" | Replaces every Y with T. |
| `mod-3973` | Replace "Y" with "U" | Replaces every Y with U. |
| `mod-3974` | Replace "Y" with "V" | Replaces every Y with V. |
| `mod-3975` | Replace "Y" with "W" | Replaces every Y with W. |
| `mod-3976` | Replace "Y" with "X" | Replaces every Y with X. |
| `mod-3977` | Replace "Y" with "Z" | Replaces every Y with Z. |
| `mod-34658` | Replace "Y" with "0" | Replaces every Y with 0. |
| `mod-3979` | Replace "Y" with "1" | Replaces every Y with 1. |
| `mod-3980` | Replace "Y" with "2" | Replaces every Y with 2. |
| `mod-3981` | Replace "Y" with "3" | Replaces every Y with 3. |
| `mod-3982` | Replace "Y" with "4" | Replaces every Y with 4. |
| `mod-3983` | Replace "Y" with "5" | Replaces every Y with 5. |
| `mod-3984` | Replace "Y" with "6" | Replaces every Y with 6. |
| `mod-3985` | Replace "Y" with "7" | Replaces every Y with 7. |
| `mod-3986` | Replace "Y" with "8" | Replaces every Y with 8. |
| `mod-3987` | Replace "Y" with "9" | Replaces every Y with 9. |
| `mod-3988` | Replace "Z" with "a" | Replaces every Z with a. |
| `mod-3989` | Replace "Z" with "b" | Replaces every Z with b. |
| `mod-3990` | Replace "Z" with "c" | Replaces every Z with c. |
| `mod-3991` | Replace "Z" with "d" | Replaces every Z with d. |
| `mod-3992` | Replace "Z" with "e" | Replaces every Z with e. |
| `mod-3993` | Replace "Z" with "f" | Replaces every Z with f. |
| `mod-3994` | Replace "Z" with "g" | Replaces every Z with g. |
| `mod-3995` | Replace "Z" with "h" | Replaces every Z with h. |
| `mod-3996` | Replace "Z" with "i" | Replaces every Z with i. |
| `mod-3997` | Replace "Z" with "j" | Replaces every Z with j. |
| `mod-3998` | Replace "Z" with "k" | Replaces every Z with k. |
| `mod-3999` | Replace "Z" with "l" | Replaces every Z with l. |
| `mod-4000` | Replace "Z" with "m" | Replaces every Z with m. |
| `mod-4001` | Replace "Z" with "n" | Replaces every Z with n. |
| `mod-4002` | Replace "Z" with "o" | Replaces every Z with o. |
| `mod-4003` | Replace "Z" with "p" | Replaces every Z with p. |
| `mod-4004` | Replace "Z" with "q" | Replaces every Z with q. |
| `mod-4005` | Replace "Z" with "r" | Replaces every Z with r. |
| `mod-4006` | Replace "Z" with "s" | Replaces every Z with s. |
| `mod-4007` | Replace "Z" with "t" | Replaces every Z with t. |
| `mod-4008` | Replace "Z" with "u" | Replaces every Z with u. |
| `mod-4009` | Replace "Z" with "v" | Replaces every Z with v. |
| `mod-4010` | Replace "Z" with "w" | Replaces every Z with w. |
| `mod-4011` | Replace "Z" with "x" | Replaces every Z with x. |
| `mod-4012` | Replace "Z" with "y" | Replaces every Z with y. |
| `mod-4013` | Replace "Z" with "z" | Replaces every Z with z. |
| `mod-4014` | Replace "Z" with "A" | Replaces every Z with A. |
| `mod-4015` | Replace "Z" with "B" | Replaces every Z with B. |
| `mod-4016` | Replace "Z" with "C" | Replaces every Z with C. |
| `mod-4017` | Replace "Z" with "D" | Replaces every Z with D. |
| `mod-4018` | Replace "Z" with "E" | Replaces every Z with E. |
| `mod-4019` | Replace "Z" with "F" | Replaces every Z with F. |
| `mod-4020` | Replace "Z" with "G" | Replaces every Z with G. |
| `mod-4021` | Replace "Z" with "H" | Replaces every Z with H. |
| `mod-4022` | Replace "Z" with "I" | Replaces every Z with I. |
| `mod-4023` | Replace "Z" with "J" | Replaces every Z with J. |
| `mod-4024` | Replace "Z" with "K" | Replaces every Z with K. |
| `mod-4025` | Replace "Z" with "L" | Replaces every Z with L. |
| `mod-4026` | Replace "Z" with "M" | Replaces every Z with M. |
| `mod-4027` | Replace "Z" with "N" | Replaces every Z with N. |
| `mod-4028` | Replace "Z" with "O" | Replaces every Z with O. |
| `mod-4029` | Replace "Z" with "P" | Replaces every Z with P. |
| `mod-4030` | Replace "Z" with "Q" | Replaces every Z with Q. |
| `mod-4031` | Replace "Z" with "R" | Replaces every Z with R. |
| `mod-4032` | Replace "Z" with "S" | Replaces every Z with S. |
| `mod-4033` | Replace "Z" with "T" | Replaces every Z with T. |
| `mod-4034` | Replace "Z" with "U" | Replaces every Z with U. |
| `mod-4035` | Replace "Z" with "V" | Replaces every Z with V. |
| `mod-4036` | Replace "Z" with "W" | Replaces every Z with W. |
| `mod-4037` | Replace "Z" with "X" | Replaces every Z with X. |
| `mod-4038` | Replace "Z" with "Y" | Replaces every Z with Y. |
| `mod-4039` | Replace "Z" with "0" | Replaces every Z with 0. |
| `mod-4040` | Replace "Z" with "1" | Replaces every Z with 1. |
| `mod-4041` | Replace "Z" with "2" | Replaces every Z with 2. |
| `mod-4042` | Replace "Z" with "3" | Replaces every Z with 3. |
| `mod-4043` | Replace "Z" with "4" | Replaces every Z with 4. |
| `mod-4044` | Replace "Z" with "5" | Replaces every Z with 5. |
| `mod-4045` | Replace "Z" with "6" | Replaces every Z with 6. |
| `mod-4046` | Replace "Z" with "7" | Replaces every Z with 7. |
| `mod-4047` | Replace "Z" with "8" | Replaces every Z with 8. |
| `mod-4048` | Replace "Z" with "9" | Replaces every Z with 9. |
| `mod-4049` | Replace "0" with "a" | Replaces every 0 with a. |
| `mod-4050` | Replace "0" with "b" | Replaces every 0 with b. |
| `mod-4051` | Replace "0" with "c" | Replaces every 0 with c. |
| `mod-4052` | Replace "0" with "d" | Replaces every 0 with d. |
| `mod-4053` | Replace "0" with "e" | Replaces every 0 with e. |
| `mod-4054` | Replace "0" with "f" | Replaces every 0 with f. |
| `mod-4055` | Replace "0" with "g" | Replaces every 0 with g. |
| `mod-4056` | Replace "0" with "h" | Replaces every 0 with h. |
| `mod-4057` | Replace "0" with "i" | Replaces every 0 with i. |
| `mod-4058` | Replace "0" with "j" | Replaces every 0 with j. |
| `mod-4059` | Replace "0" with "k" | Replaces every 0 with k. |
| `mod-4060` | Replace "0" with "l" | Replaces every 0 with l. |
| `mod-4061` | Replace "0" with "m" | Replaces every 0 with m. |
| `mod-4062` | Replace "0" with "n" | Replaces every 0 with n. |
| `mod-4063` | Replace "0" with "o" | Replaces every 0 with o. |
| `mod-4064` | Replace "0" with "p" | Replaces every 0 with p. |
| `mod-4065` | Replace "0" with "q" | Replaces every 0 with q. |
| `mod-4066` | Replace "0" with "r" | Replaces every 0 with r. |
| `mod-4067` | Replace "0" with "s" | Replaces every 0 with s. |
| `mod-4068` | Replace "0" with "t" | Replaces every 0 with t. |
| `mod-4069` | Replace "0" with "u" | Replaces every 0 with u. |
| `mod-4070` | Replace "0" with "v" | Replaces every 0 with v. |
| `mod-4071` | Replace "0" with "w" | Replaces every 0 with w. |
| `mod-4072` | Replace "0" with "x" | Replaces every 0 with x. |
| `mod-4073` | Replace "0" with "y" | Replaces every 0 with y. |
| `mod-4074` | Replace "0" with "z" | Replaces every 0 with z. |
| `mod-4075` | Replace "0" with "A" | Replaces every 0 with A. |
| `mod-4076` | Replace "0" with "B" | Replaces every 0 with B. |
| `mod-4077` | Replace "0" with "C" | Replaces every 0 with C. |
| `mod-4078` | Replace "0" with "D" | Replaces every 0 with D. |
| `mod-4079` | Replace "0" with "E" | Replaces every 0 with E. |
| `mod-4080` | Replace "0" with "F" | Replaces every 0 with F. |
| `mod-4081` | Replace "0" with "G" | Replaces every 0 with G. |
| `mod-4082` | Replace "0" with "H" | Replaces every 0 with H. |
| `mod-4083` | Replace "0" with "I" | Replaces every 0 with I. |
| `mod-4084` | Replace "0" with "J" | Replaces every 0 with J. |
| `mod-4085` | Replace "0" with "K" | Replaces every 0 with K. |
| `mod-4086` | Replace "0" with "L" | Replaces every 0 with L. |
| `mod-4087` | Replace "0" with "M" | Replaces every 0 with M. |
| `mod-4088` | Replace "0" with "N" | Replaces every 0 with N. |
| `mod-4089` | Replace "0" with "O" | Replaces every 0 with O. |
| `mod-4090` | Replace "0" with "P" | Replaces every 0 with P. |
| `mod-4091` | Replace "0" with "Q" | Replaces every 0 with Q. |
| `mod-4092` | Replace "0" with "R" | Replaces every 0 with R. |
| `mod-4093` | Replace "0" with "S" | Replaces every 0 with S. |
| `mod-4094` | Replace "0" with "T" | Replaces every 0 with T. |
| `mod-4095` | Replace "0" with "U" | Replaces every 0 with U. |
| `mod-4096` | Replace "0" with "V" | Replaces every 0 with V. |
| `mod-4097` | Replace "0" with "W" | Replaces every 0 with W. |
| `mod-4098` | Replace "0" with "X" | Replaces every 0 with X. |
| `mod-4099` | Replace "0" with "Y" | Replaces every 0 with Y. |
| `mod-4100` | Replace "0" with "Z" | Replaces every 0 with Z. |
| `mod-4101` | Replace "0" with "1" | Replaces every 0 with 1. |
| `mod-4102` | Replace "0" with "2" | Replaces every 0 with 2. |
| `mod-4103` | Replace "0" with "3" | Replaces every 0 with 3. |
| `mod-4104` | Replace "0" with "4" | Replaces every 0 with 4. |
| `mod-4105` | Replace "0" with "5" | Replaces every 0 with 5. |
| `mod-4106` | Replace "0" with "6" | Replaces every 0 with 6. |
| `mod-4107` | Replace "0" with "7" | Replaces every 0 with 7. |
| `mod-4108` | Replace "0" with "8" | Replaces every 0 with 8. |
| `mod-4109` | Replace "0" with "9" | Replaces every 0 with 9. |
| `mod-4110` | Replace "1" with "a" | Replaces every 1 with a. |
| `mod-4111` | Replace "1" with "b" | Replaces every 1 with b. |
| `mod-4112` | Replace "1" with "c" | Replaces every 1 with c. |
| `mod-4113` | Replace "1" with "d" | Replaces every 1 with d. |
| `mod-4114` | Replace "1" with "e" | Replaces every 1 with e. |
| `mod-4115` | Replace "1" with "f" | Replaces every 1 with f. |
| `mod-4116` | Replace "1" with "g" | Replaces every 1 with g. |
| `mod-4117` | Replace "1" with "h" | Replaces every 1 with h. |
| `mod-4118` | Replace "1" with "i" | Replaces every 1 with i. |
| `mod-4119` | Replace "1" with "j" | Replaces every 1 with j. |
| `mod-4120` | Replace "1" with "k" | Replaces every 1 with k. |
| `mod-4121` | Replace "1" with "l" | Replaces every 1 with l. |
| `mod-4122` | Replace "1" with "m" | Replaces every 1 with m. |
| `mod-4123` | Replace "1" with "n" | Replaces every 1 with n. |
| `mod-4124` | Replace "1" with "o" | Replaces every 1 with o. |
| `mod-4125` | Replace "1" with "p" | Replaces every 1 with p. |
| `mod-4126` | Replace "1" with "q" | Replaces every 1 with q. |
| `mod-4127` | Replace "1" with "r" | Replaces every 1 with r. |
| `mod-4128` | Replace "1" with "s" | Replaces every 1 with s. |
| `mod-4129` | Replace "1" with "t" | Replaces every 1 with t. |
| `mod-4130` | Replace "1" with "u" | Replaces every 1 with u. |
| `mod-4131` | Replace "1" with "v" | Replaces every 1 with v. |
| `mod-4132` | Replace "1" with "w" | Replaces every 1 with w. |
| `mod-4133` | Replace "1" with "x" | Replaces every 1 with x. |
| `mod-4134` | Replace "1" with "y" | Replaces every 1 with y. |
| `mod-4135` | Replace "1" with "z" | Replaces every 1 with z. |
| `mod-4136` | Replace "1" with "A" | Replaces every 1 with A. |
| `mod-4137` | Replace "1" with "B" | Replaces every 1 with B. |
| `mod-4138` | Replace "1" with "C" | Replaces every 1 with C. |
| `mod-4139` | Replace "1" with "D" | Replaces every 1 with D. |
| `mod-4140` | Replace "1" with "E" | Replaces every 1 with E. |
| `mod-4141` | Replace "1" with "F" | Replaces every 1 with F. |
| `mod-4142` | Replace "1" with "G" | Replaces every 1 with G. |
| `mod-4143` | Replace "1" with "H" | Replaces every 1 with H. |
| `mod-4144` | Replace "1" with "I" | Replaces every 1 with I. |
| `mod-4145` | Replace "1" with "J" | Replaces every 1 with J. |
| `mod-4146` | Replace "1" with "K" | Replaces every 1 with K. |
| `mod-4147` | Replace "1" with "L" | Replaces every 1 with L. |
| `mod-4148` | Replace "1" with "M" | Replaces every 1 with M. |
| `mod-4149` | Replace "1" with "N" | Replaces every 1 with N. |
| `mod-4150` | Replace "1" with "O" | Replaces every 1 with O. |
| `mod-4151` | Replace "1" with "P" | Replaces every 1 with P. |
| `mod-4152` | Replace "1" with "Q" | Replaces every 1 with Q. |
| `mod-4153` | Replace "1" with "R" | Replaces every 1 with R. |
| `mod-4154` | Replace "1" with "S" | Replaces every 1 with S. |
| `mod-4155` | Replace "1" with "T" | Replaces every 1 with T. |
| `mod-4156` | Replace "1" with "U" | Replaces every 1 with U. |
| `mod-4157` | Replace "1" with "V" | Replaces every 1 with V. |
| `mod-4158` | Replace "1" with "W" | Replaces every 1 with W. |
| `mod-4159` | Replace "1" with "X" | Replaces every 1 with X. |
| `mod-4160` | Replace "1" with "Y" | Replaces every 1 with Y. |
| `mod-4161` | Replace "1" with "Z" | Replaces every 1 with Z. |
| `mod-4162` | Replace "1" with "0" | Replaces every 1 with 0. |
| `mod-4163` | Replace "1" with "2" | Replaces every 1 with 2. |
| `mod-4164` | Replace "1" with "3" | Replaces every 1 with 3. |
| `mod-4165` | Replace "1" with "4" | Replaces every 1 with 4. |
| `mod-4166` | Replace "1" with "5" | Replaces every 1 with 5. |
| `mod-4167` | Replace "1" with "6" | Replaces every 1 with 6. |
| `mod-4168` | Replace "1" with "7" | Replaces every 1 with 7. |
| `mod-4169` | Replace "1" with "8" | Replaces every 1 with 8. |
| `mod-4170` | Replace "1" with "9" | Replaces every 1 with 9. |
| `mod-4171` | Replace "2" with "a" | Replaces every 2 with a. |
| `mod-4172` | Replace "2" with "b" | Replaces every 2 with b. |
| `mod-4173` | Replace "2" with "c" | Replaces every 2 with c. |
| `mod-4174` | Replace "2" with "d" | Replaces every 2 with d. |
| `mod-4175` | Replace "2" with "e" | Replaces every 2 with e. |
| `mod-4176` | Replace "2" with "f" | Replaces every 2 with f. |
| `mod-4177` | Replace "2" with "g" | Replaces every 2 with g. |
| `mod-4178` | Replace "2" with "h" | Replaces every 2 with h. |
| `mod-4179` | Replace "2" with "i" | Replaces every 2 with i. |
| `mod-4180` | Replace "2" with "j" | Replaces every 2 with j. |
| `mod-4181` | Replace "2" with "k" | Replaces every 2 with k. |
| `mod-4182` | Replace "2" with "l" | Replaces every 2 with l. |
| `mod-4183` | Replace "2" with "m" | Replaces every 2 with m. |
| `mod-4184` | Replace "2" with "n" | Replaces every 2 with n. |
| `mod-4185` | Replace "2" with "o" | Replaces every 2 with o. |
| `mod-4186` | Replace "2" with "p" | Replaces every 2 with p. |
| `mod-4187` | Replace "2" with "q" | Replaces every 2 with q. |
| `mod-4188` | Replace "2" with "r" | Replaces every 2 with r. |
| `mod-4189` | Replace "2" with "s" | Replaces every 2 with s. |
| `mod-4190` | Replace "2" with "t" | Replaces every 2 with t. |
| `mod-4191` | Replace "2" with "u" | Replaces every 2 with u. |
| `mod-4192` | Replace "2" with "v" | Replaces every 2 with v. |
| `mod-4193` | Replace "2" with "w" | Replaces every 2 with w. |
| `mod-4194` | Replace "2" with "x" | Replaces every 2 with x. |
| `mod-4195` | Replace "2" with "y" | Replaces every 2 with y. |
| `mod-4196` | Replace "2" with "z" | Replaces every 2 with z. |
| `mod-4197` | Replace "2" with "A" | Replaces every 2 with A. |
| `mod-4198` | Replace "2" with "B" | Replaces every 2 with B. |
| `mod-4199` | Replace "2" with "C" | Replaces every 2 with C. |
| `mod-4200` | Replace "2" with "D" | Replaces every 2 with D. |
| `mod-4201` | Replace "2" with "E" | Replaces every 2 with E. |
| `mod-4202` | Replace "2" with "F" | Replaces every 2 with F. |
| `mod-4203` | Replace "2" with "G" | Replaces every 2 with G. |
| `mod-4204` | Replace "2" with "H" | Replaces every 2 with H. |
| `mod-4205` | Replace "2" with "I" | Replaces every 2 with I. |
| `mod-4206` | Replace "2" with "J" | Replaces every 2 with J. |
| `mod-4207` | Replace "2" with "K" | Replaces every 2 with K. |
| `mod-4208` | Replace "2" with "L" | Replaces every 2 with L. |
| `mod-4209` | Replace "2" with "M" | Replaces every 2 with M. |
| `mod-4210` | Replace "2" with "N" | Replaces every 2 with N. |
| `mod-4211` | Replace "2" with "O" | Replaces every 2 with O. |
| `mod-4212` | Replace "2" with "P" | Replaces every 2 with P. |
| `mod-4213` | Replace "2" with "Q" | Replaces every 2 with Q. |
| `mod-4214` | Replace "2" with "R" | Replaces every 2 with R. |
| `mod-4215` | Replace "2" with "S" | Replaces every 2 with S. |
| `mod-4216` | Replace "2" with "T" | Replaces every 2 with T. |
| `mod-4217` | Replace "2" with "U" | Replaces every 2 with U. |
| `mod-4218` | Replace "2" with "V" | Replaces every 2 with V. |
| `mod-4219` | Replace "2" with "W" | Replaces every 2 with W. |
| `mod-4220` | Replace "2" with "X" | Replaces every 2 with X. |
| `mod-4221` | Replace "2" with "Y" | Replaces every 2 with Y. |
| `mod-4222` | Replace "2" with "Z" | Replaces every 2 with Z. |
| `mod-4223` | Replace "2" with "0" | Replaces every 2 with 0. |
| `mod-4224` | Replace "2" with "1" | Replaces every 2 with 1. |
| `mod-4225` | Replace "2" with "3" | Replaces every 2 with 3. |
| `mod-4226` | Replace "2" with "4" | Replaces every 2 with 4. |
| `mod-4227` | Replace "2" with "5" | Replaces every 2 with 5. |
| `mod-4228` | Replace "2" with "6" | Replaces every 2 with 6. |
| `mod-4229` | Replace "2" with "7" | Replaces every 2 with 7. |
| `mod-4230` | Replace "2" with "8" | Replaces every 2 with 8. |
| `mod-4231` | Replace "2" with "9" | Replaces every 2 with 9. |
| `mod-4232` | Replace "3" with "a" | Replaces every 3 with a. |
| `mod-4233` | Replace "3" with "b" | Replaces every 3 with b. |
| `mod-4234` | Replace "3" with "c" | Replaces every 3 with c. |
| `mod-4235` | Replace "3" with "d" | Replaces every 3 with d. |
| `mod-4236` | Replace "3" with "e" | Replaces every 3 with e. |
| `mod-4237` | Replace "3" with "f" | Replaces every 3 with f. |
| `mod-4238` | Replace "3" with "g" | Replaces every 3 with g. |
| `mod-4239` | Replace "3" with "h" | Replaces every 3 with h. |
| `mod-4240` | Replace "3" with "i" | Replaces every 3 with i. |
| `mod-4241` | Replace "3" with "j" | Replaces every 3 with j. |
| `mod-4242` | Replace "3" with "k" | Replaces every 3 with k. |
| `mod-4243` | Replace "3" with "l" | Replaces every 3 with l. |
| `mod-4244` | Replace "3" with "m" | Replaces every 3 with m. |
| `mod-4245` | Replace "3" with "n" | Replaces every 3 with n. |
| `mod-4246` | Replace "3" with "o" | Replaces every 3 with o. |
| `mod-4247` | Replace "3" with "p" | Replaces every 3 with p. |
| `mod-4248` | Replace "3" with "q" | Replaces every 3 with q. |
| `mod-4249` | Replace "3" with "r" | Replaces every 3 with r. |
| `mod-4250` | Replace "3" with "s" | Replaces every 3 with s. |
| `mod-4251` | Replace "3" with "t" | Replaces every 3 with t. |
| `mod-4252` | Replace "3" with "u" | Replaces every 3 with u. |
| `mod-4253` | Replace "3" with "v" | Replaces every 3 with v. |
| `mod-4254` | Replace "3" with "w" | Replaces every 3 with w. |
| `mod-4255` | Replace "3" with "x" | Replaces every 3 with x. |
| `mod-4256` | Replace "3" with "y" | Replaces every 3 with y. |
| `mod-4257` | Replace "3" with "z" | Replaces every 3 with z. |
| `mod-4258` | Replace "3" with "A" | Replaces every 3 with A. |
| `mod-4259` | Replace "3" with "B" | Replaces every 3 with B. |
| `mod-4260` | Replace "3" with "C" | Replaces every 3 with C. |
| `mod-4261` | Replace "3" with "D" | Replaces every 3 with D. |
| `mod-4262` | Replace "3" with "E" | Replaces every 3 with E. |
| `mod-4263` | Replace "3" with "F" | Replaces every 3 with F. |
| `mod-4264` | Replace "3" with "G" | Replaces every 3 with G. |
| `mod-4265` | Replace "3" with "H" | Replaces every 3 with H. |
| `mod-4266` | Replace "3" with "I" | Replaces every 3 with I. |
| `mod-4267` | Replace "3" with "J" | Replaces every 3 with J. |
| `mod-4268` | Replace "3" with "K" | Replaces every 3 with K. |
| `mod-4269` | Replace "3" with "L" | Replaces every 3 with L. |
| `mod-4270` | Replace "3" with "M" | Replaces every 3 with M. |
| `mod-4271` | Replace "3" with "N" | Replaces every 3 with N. |
| `mod-4272` | Replace "3" with "O" | Replaces every 3 with O. |
| `mod-4273` | Replace "3" with "P" | Replaces every 3 with P. |
| `mod-4274` | Replace "3" with "Q" | Replaces every 3 with Q. |
| `mod-4275` | Replace "3" with "R" | Replaces every 3 with R. |
| `mod-4276` | Replace "3" with "S" | Replaces every 3 with S. |
| `mod-4277` | Replace "3" with "T" | Replaces every 3 with T. |
| `mod-4278` | Replace "3" with "U" | Replaces every 3 with U. |
| `mod-4279` | Replace "3" with "V" | Replaces every 3 with V. |
| `mod-4280` | Replace "3" with "W" | Replaces every 3 with W. |
| `mod-4281` | Replace "3" with "X" | Replaces every 3 with X. |
| `mod-4282` | Replace "3" with "Y" | Replaces every 3 with Y. |
| `mod-4283` | Replace "3" with "Z" | Replaces every 3 with Z. |
| `mod-4284` | Replace "3" with "0" | Replaces every 3 with 0. |
| `mod-4285` | Replace "3" with "1" | Replaces every 3 with 1. |
| `mod-4286` | Replace "3" with "2" | Replaces every 3 with 2. |
| `mod-4287` | Replace "3" with "4" | Replaces every 3 with 4. |
| `mod-4288` | Replace "3" with "5" | Replaces every 3 with 5. |
| `mod-4289` | Replace "3" with "6" | Replaces every 3 with 6. |
| `mod-4290` | Replace "3" with "7" | Replaces every 3 with 7. |
| `mod-4291` | Replace "3" with "8" | Replaces every 3 with 8. |
| `mod-4292` | Replace "3" with "9" | Replaces every 3 with 9. |
| `mod-4293` | Replace "4" with "a" | Replaces every 4 with a. |
| `mod-4294` | Replace "4" with "b" | Replaces every 4 with b. |
| `mod-4295` | Replace "4" with "c" | Replaces every 4 with c. |
| `mod-4296` | Replace "4" with "d" | Replaces every 4 with d. |
| `mod-4297` | Replace "4" with "e" | Replaces every 4 with e. |
| `mod-4298` | Replace "4" with "f" | Replaces every 4 with f. |
| `mod-4299` | Replace "4" with "g" | Replaces every 4 with g. |
| `mod-4300` | Replace "4" with "h" | Replaces every 4 with h. |
| `mod-4301` | Replace "4" with "i" | Replaces every 4 with i. |
| `mod-4302` | Replace "4" with "j" | Replaces every 4 with j. |
| `mod-4303` | Replace "4" with "k" | Replaces every 4 with k. |
| `mod-4304` | Replace "4" with "l" | Replaces every 4 with l. |
| `mod-4305` | Replace "4" with "m" | Replaces every 4 with m. |
| `mod-4306` | Replace "4" with "n" | Replaces every 4 with n. |
| `mod-4307` | Replace "4" with "o" | Replaces every 4 with o. |
| `mod-4308` | Replace "4" with "p" | Replaces every 4 with p. |
| `mod-4309` | Replace "4" with "q" | Replaces every 4 with q. |
| `mod-4310` | Replace "4" with "r" | Replaces every 4 with r. |
| `mod-4311` | Replace "4" with "s" | Replaces every 4 with s. |
| `mod-4312` | Replace "4" with "t" | Replaces every 4 with t. |
| `mod-4313` | Replace "4" with "u" | Replaces every 4 with u. |
| `mod-4314` | Replace "4" with "v" | Replaces every 4 with v. |
| `mod-4315` | Replace "4" with "w" | Replaces every 4 with w. |
| `mod-4316` | Replace "4" with "x" | Replaces every 4 with x. |
| `mod-4317` | Replace "4" with "y" | Replaces every 4 with y. |
| `mod-4318` | Replace "4" with "z" | Replaces every 4 with z. |
| `mod-4319` | Replace "4" with "A" | Replaces every 4 with A. |
| `mod-4320` | Replace "4" with "B" | Replaces every 4 with B. |
| `mod-4321` | Replace "4" with "C" | Replaces every 4 with C. |
| `mod-4322` | Replace "4" with "D" | Replaces every 4 with D. |
| `mod-4323` | Replace "4" with "E" | Replaces every 4 with E. |
| `mod-4324` | Replace "4" with "F" | Replaces every 4 with F. |
| `mod-4325` | Replace "4" with "G" | Replaces every 4 with G. |
| `mod-4326` | Replace "4" with "H" | Replaces every 4 with H. |
| `mod-4327` | Replace "4" with "I" | Replaces every 4 with I. |
| `mod-4328` | Replace "4" with "J" | Replaces every 4 with J. |
| `mod-4329` | Replace "4" with "K" | Replaces every 4 with K. |
| `mod-4330` | Replace "4" with "L" | Replaces every 4 with L. |
| `mod-4331` | Replace "4" with "M" | Replaces every 4 with M. |
| `mod-4332` | Replace "4" with "N" | Replaces every 4 with N. |
| `mod-4333` | Replace "4" with "O" | Replaces every 4 with O. |
| `mod-4334` | Replace "4" with "P" | Replaces every 4 with P. |
| `mod-4335` | Replace "4" with "Q" | Replaces every 4 with Q. |
| `mod-4336` | Replace "4" with "R" | Replaces every 4 with R. |
| `mod-4337` | Replace "4" with "S" | Replaces every 4 with S. |
| `mod-4338` | Replace "4" with "T" | Replaces every 4 with T. |
| `mod-4339` | Replace "4" with "U" | Replaces every 4 with U. |
| `mod-4340` | Replace "4" with "V" | Replaces every 4 with V. |
| `mod-4341` | Replace "4" with "W" | Replaces every 4 with W. |
| `mod-4342` | Replace "4" with "X" | Replaces every 4 with X. |
| `mod-4343` | Replace "4" with "Y" | Replaces every 4 with Y. |
| `mod-4344` | Replace "4" with "Z" | Replaces every 4 with Z. |
| `mod-4345` | Replace "4" with "0" | Replaces every 4 with 0. |
| `mod-4346` | Replace "4" with "1" | Replaces every 4 with 1. |
| `mod-4347` | Replace "4" with "2" | Replaces every 4 with 2. |
| `mod-4348` | Replace "4" with "3" | Replaces every 4 with 3. |
| `mod-4349` | Replace "4" with "5" | Replaces every 4 with 5. |
| `mod-4350` | Replace "4" with "6" | Replaces every 4 with 6. |
| `mod-4351` | Replace "4" with "7" | Replaces every 4 with 7. |
| `mod-4352` | Replace "4" with "8" | Replaces every 4 with 8. |
| `mod-4353` | Replace "4" with "9" | Replaces every 4 with 9. |
| `mod-4354` | Replace "5" with "a" | Replaces every 5 with a. |
| `mod-4355` | Replace "5" with "b" | Replaces every 5 with b. |
| `mod-4356` | Replace "5" with "c" | Replaces every 5 with c. |
| `mod-4357` | Replace "5" with "d" | Replaces every 5 with d. |
| `mod-4358` | Replace "5" with "e" | Replaces every 5 with e. |
| `mod-4359` | Replace "5" with "f" | Replaces every 5 with f. |
| `mod-4360` | Replace "5" with "g" | Replaces every 5 with g. |
| `mod-4361` | Replace "5" with "h" | Replaces every 5 with h. |
| `mod-4362` | Replace "5" with "i" | Replaces every 5 with i. |
| `mod-4363` | Replace "5" with "j" | Replaces every 5 with j. |
| `mod-4364` | Replace "5" with "k" | Replaces every 5 with k. |
| `mod-4365` | Replace "5" with "l" | Replaces every 5 with l. |
| `mod-4366` | Replace "5" with "m" | Replaces every 5 with m. |
| `mod-4367` | Replace "5" with "n" | Replaces every 5 with n. |
| `mod-4368` | Replace "5" with "o" | Replaces every 5 with o. |
| `mod-4369` | Replace "5" with "p" | Replaces every 5 with p. |
| `mod-4370` | Replace "5" with "q" | Replaces every 5 with q. |
| `mod-4371` | Replace "5" with "r" | Replaces every 5 with r. |
| `mod-4372` | Replace "5" with "s" | Replaces every 5 with s. |
| `mod-4373` | Replace "5" with "t" | Replaces every 5 with t. |
| `mod-4374` | Replace "5" with "u" | Replaces every 5 with u. |
| `mod-4375` | Replace "5" with "v" | Replaces every 5 with v. |
| `mod-4376` | Replace "5" with "w" | Replaces every 5 with w. |
| `mod-4377` | Replace "5" with "x" | Replaces every 5 with x. |
| `mod-4378` | Replace "5" with "y" | Replaces every 5 with y. |
| `mod-4379` | Replace "5" with "z" | Replaces every 5 with z. |
| `mod-4380` | Replace "5" with "A" | Replaces every 5 with A. |
| `mod-4381` | Replace "5" with "B" | Replaces every 5 with B. |
| `mod-4382` | Replace "5" with "C" | Replaces every 5 with C. |
| `mod-4383` | Replace "5" with "D" | Replaces every 5 with D. |
| `mod-4384` | Replace "5" with "E" | Replaces every 5 with E. |
| `mod-4385` | Replace "5" with "F" | Replaces every 5 with F. |
| `mod-4386` | Replace "5" with "G" | Replaces every 5 with G. |
| `mod-4387` | Replace "5" with "H" | Replaces every 5 with H. |
| `mod-4388` | Replace "5" with "I" | Replaces every 5 with I. |
| `mod-4389` | Replace "5" with "J" | Replaces every 5 with J. |
| `mod-4390` | Replace "5" with "K" | Replaces every 5 with K. |
| `mod-4391` | Replace "5" with "L" | Replaces every 5 with L. |
| `mod-4392` | Replace "5" with "M" | Replaces every 5 with M. |
| `mod-4393` | Replace "5" with "N" | Replaces every 5 with N. |
| `mod-4394` | Replace "5" with "O" | Replaces every 5 with O. |
| `mod-4395` | Replace "5" with "P" | Replaces every 5 with P. |
| `mod-4396` | Replace "5" with "Q" | Replaces every 5 with Q. |
| `mod-4397` | Replace "5" with "R" | Replaces every 5 with R. |
| `mod-4398` | Replace "5" with "S" | Replaces every 5 with S. |
| `mod-4399` | Replace "5" with "T" | Replaces every 5 with T. |
| `mod-4400` | Replace "5" with "U" | Replaces every 5 with U. |
| `mod-4401` | Replace "5" with "V" | Replaces every 5 with V. |
| `mod-4402` | Replace "5" with "W" | Replaces every 5 with W. |
| `mod-4403` | Replace "5" with "X" | Replaces every 5 with X. |
| `mod-4404` | Replace "5" with "Y" | Replaces every 5 with Y. |
| `mod-4405` | Replace "5" with "Z" | Replaces every 5 with Z. |
| `mod-4406` | Replace "5" with "0" | Replaces every 5 with 0. |
| `mod-4407` | Replace "5" with "1" | Replaces every 5 with 1. |
| `mod-4408` | Replace "5" with "2" | Replaces every 5 with 2. |
| `mod-4409` | Replace "5" with "3" | Replaces every 5 with 3. |
| `mod-4410` | Replace "5" with "4" | Replaces every 5 with 4. |
| `mod-4411` | Replace "5" with "6" | Replaces every 5 with 6. |
| `mod-4412` | Replace "5" with "7" | Replaces every 5 with 7. |
| `mod-4413` | Replace "5" with "8" | Replaces every 5 with 8. |
| `mod-4414` | Replace "5" with "9" | Replaces every 5 with 9. |
| `mod-4415` | Replace "6" with "a" | Replaces every 6 with a. |
| `mod-4416` | Replace "6" with "b" | Replaces every 6 with b. |
| `mod-4417` | Replace "6" with "c" | Replaces every 6 with c. |
| `mod-4418` | Replace "6" with "d" | Replaces every 6 with d. |
| `mod-4419` | Replace "6" with "e" | Replaces every 6 with e. |
| `mod-4420` | Replace "6" with "f" | Replaces every 6 with f. |
| `mod-4421` | Replace "6" with "g" | Replaces every 6 with g. |
| `mod-4422` | Replace "6" with "h" | Replaces every 6 with h. |
| `mod-4423` | Replace "6" with "i" | Replaces every 6 with i. |
| `mod-4424` | Replace "6" with "j" | Replaces every 6 with j. |
| `mod-4425` | Replace "6" with "k" | Replaces every 6 with k. |
| `mod-4426` | Replace "6" with "l" | Replaces every 6 with l. |
| `mod-4427` | Replace "6" with "m" | Replaces every 6 with m. |
| `mod-4428` | Replace "6" with "n" | Replaces every 6 with n. |
| `mod-4429` | Replace "6" with "o" | Replaces every 6 with o. |
| `mod-4430` | Replace "6" with "p" | Replaces every 6 with p. |
| `mod-4431` | Replace "6" with "q" | Replaces every 6 with q. |
| `mod-4432` | Replace "6" with "r" | Replaces every 6 with r. |
| `mod-4433` | Replace "6" with "s" | Replaces every 6 with s. |
| `mod-4434` | Replace "6" with "t" | Replaces every 6 with t. |
| `mod-4435` | Replace "6" with "u" | Replaces every 6 with u. |
| `mod-4436` | Replace "6" with "v" | Replaces every 6 with v. |
| `mod-4437` | Replace "6" with "w" | Replaces every 6 with w. |
| `mod-4438` | Replace "6" with "x" | Replaces every 6 with x. |
| `mod-4439` | Replace "6" with "y" | Replaces every 6 with y. |
| `mod-4440` | Replace "6" with "z" | Replaces every 6 with z. |
| `mod-4441` | Replace "6" with "A" | Replaces every 6 with A. |
| `mod-4442` | Replace "6" with "B" | Replaces every 6 with B. |
| `mod-4443` | Replace "6" with "C" | Replaces every 6 with C. |
| `mod-4444` | Replace "6" with "D" | Replaces every 6 with D. |
| `mod-4445` | Replace "6" with "E" | Replaces every 6 with E. |
| `mod-4446` | Replace "6" with "F" | Replaces every 6 with F. |
| `mod-4447` | Replace "6" with "G" | Replaces every 6 with G. |
| `mod-4448` | Replace "6" with "H" | Replaces every 6 with H. |
| `mod-4449` | Replace "6" with "I" | Replaces every 6 with I. |
| `mod-4450` | Replace "6" with "J" | Replaces every 6 with J. |
| `mod-4451` | Replace "6" with "K" | Replaces every 6 with K. |
| `mod-4452` | Replace "6" with "L" | Replaces every 6 with L. |
| `mod-4453` | Replace "6" with "M" | Replaces every 6 with M. |
| `mod-4454` | Replace "6" with "N" | Replaces every 6 with N. |
| `mod-4455` | Replace "6" with "O" | Replaces every 6 with O. |
| `mod-4456` | Replace "6" with "P" | Replaces every 6 with P. |
| `mod-4457` | Replace "6" with "Q" | Replaces every 6 with Q. |
| `mod-4458` | Replace "6" with "R" | Replaces every 6 with R. |
| `mod-4459` | Replace "6" with "S" | Replaces every 6 with S. |
| `mod-4460` | Replace "6" with "T" | Replaces every 6 with T. |
| `mod-4461` | Replace "6" with "U" | Replaces every 6 with U. |
| `mod-4462` | Replace "6" with "V" | Replaces every 6 with V. |
| `mod-4463` | Replace "6" with "W" | Replaces every 6 with W. |
| `mod-4464` | Replace "6" with "X" | Replaces every 6 with X. |
| `mod-4465` | Replace "6" with "Y" | Replaces every 6 with Y. |
| `mod-4466` | Replace "6" with "Z" | Replaces every 6 with Z. |
| `mod-4467` | Replace "6" with "0" | Replaces every 6 with 0. |
| `mod-4468` | Replace "6" with "1" | Replaces every 6 with 1. |
| `mod-4469` | Replace "6" with "2" | Replaces every 6 with 2. |
| `mod-4470` | Replace "6" with "3" | Replaces every 6 with 3. |
| `mod-4471` | Replace "6" with "4" | Replaces every 6 with 4. |
| `mod-4472` | Replace "6" with "5" | Replaces every 6 with 5. |
| `mod-4473` | Replace "6" with "7" | Replaces every 6 with 7. |
| `mod-4474` | Replace "6" with "8" | Replaces every 6 with 8. |
| `mod-4475` | Replace "6" with "9" | Replaces every 6 with 9. |
| `mod-4476` | Replace "7" with "a" | Replaces every 7 with a. |
| `mod-4477` | Replace "7" with "b" | Replaces every 7 with b. |
| `mod-4478` | Replace "7" with "c" | Replaces every 7 with c. |
| `mod-4479` | Replace "7" with "d" | Replaces every 7 with d. |
| `mod-4480` | Replace "7" with "e" | Replaces every 7 with e. |
| `mod-4481` | Replace "7" with "f" | Replaces every 7 with f. |
| `mod-4482` | Replace "7" with "g" | Replaces every 7 with g. |
| `mod-4483` | Replace "7" with "h" | Replaces every 7 with h. |
| `mod-4484` | Replace "7" with "i" | Replaces every 7 with i. |
| `mod-4485` | Replace "7" with "j" | Replaces every 7 with j. |
| `mod-4486` | Replace "7" with "k" | Replaces every 7 with k. |
| `mod-4487` | Replace "7" with "l" | Replaces every 7 with l. |
| `mod-4488` | Replace "7" with "m" | Replaces every 7 with m. |
| `mod-4489` | Replace "7" with "n" | Replaces every 7 with n. |
| `mod-4490` | Replace "7" with "o" | Replaces every 7 with o. |
| `mod-4491` | Replace "7" with "p" | Replaces every 7 with p. |
| `mod-4492` | Replace "7" with "q" | Replaces every 7 with q. |
| `mod-4493` | Replace "7" with "r" | Replaces every 7 with r. |
| `mod-4494` | Replace "7" with "s" | Replaces every 7 with s. |
| `mod-4495` | Replace "7" with "t" | Replaces every 7 with t. |
| `mod-4496` | Replace "7" with "u" | Replaces every 7 with u. |
| `mod-4497` | Replace "7" with "v" | Replaces every 7 with v. |
| `mod-4498` | Replace "7" with "w" | Replaces every 7 with w. |
| `mod-4499` | Replace "7" with "x" | Replaces every 7 with x. |
| `mod-4500` | Replace "7" with "y" | Replaces every 7 with y. |
| `mod-4501` | Replace "7" with "z" | Replaces every 7 with z. |
| `mod-4502` | Replace "7" with "A" | Replaces every 7 with A. |
| `mod-4503` | Replace "7" with "B" | Replaces every 7 with B. |
| `mod-4504` | Replace "7" with "C" | Replaces every 7 with C. |
| `mod-4505` | Replace "7" with "D" | Replaces every 7 with D. |
| `mod-4506` | Replace "7" with "E" | Replaces every 7 with E. |
| `mod-4507` | Replace "7" with "F" | Replaces every 7 with F. |
| `mod-4508` | Replace "7" with "G" | Replaces every 7 with G. |
| `mod-4509` | Replace "7" with "H" | Replaces every 7 with H. |
| `mod-4510` | Replace "7" with "I" | Replaces every 7 with I. |
| `mod-4511` | Replace "7" with "J" | Replaces every 7 with J. |
| `mod-4512` | Replace "7" with "K" | Replaces every 7 with K. |
| `mod-4513` | Replace "7" with "L" | Replaces every 7 with L. |
| `mod-4514` | Replace "7" with "M" | Replaces every 7 with M. |
| `mod-4515` | Replace "7" with "N" | Replaces every 7 with N. |
| `mod-4516` | Replace "7" with "O" | Replaces every 7 with O. |
| `mod-4517` | Replace "7" with "P" | Replaces every 7 with P. |
| `mod-4518` | Replace "7" with "Q" | Replaces every 7 with Q. |
| `mod-4519` | Replace "7" with "R" | Replaces every 7 with R. |
| `mod-4520` | Replace "7" with "S" | Replaces every 7 with S. |
| `mod-4521` | Replace "7" with "T" | Replaces every 7 with T. |
| `mod-4522` | Replace "7" with "U" | Replaces every 7 with U. |
| `mod-4523` | Replace "7" with "V" | Replaces every 7 with V. |
| `mod-4524` | Replace "7" with "W" | Replaces every 7 with W. |
| `mod-4525` | Replace "7" with "X" | Replaces every 7 with X. |
| `mod-4526` | Replace "7" with "Y" | Replaces every 7 with Y. |
| `mod-4527` | Replace "7" with "Z" | Replaces every 7 with Z. |
| `mod-4528` | Replace "7" with "0" | Replaces every 7 with 0. |
| `mod-4529` | Replace "7" with "1" | Replaces every 7 with 1. |
| `mod-4530` | Replace "7" with "2" | Replaces every 7 with 2. |
| `mod-4531` | Replace "7" with "3" | Replaces every 7 with 3. |
| `mod-4532` | Replace "7" with "4" | Replaces every 7 with 4. |
| `mod-4533` | Replace "7" with "5" | Replaces every 7 with 5. |
| `mod-4534` | Replace "7" with "6" | Replaces every 7 with 6. |
| `mod-4535` | Replace "7" with "8" | Replaces every 7 with 8. |
| `mod-4536` | Replace "7" with "9" | Replaces every 7 with 9. |
| `mod-4537` | Replace "8" with "a" | Replaces every 8 with a. |
| `mod-4538` | Replace "8" with "b" | Replaces every 8 with b. |
| `mod-4539` | Replace "8" with "c" | Replaces every 8 with c. |
| `mod-4540` | Replace "8" with "d" | Replaces every 8 with d. |
| `mod-4541` | Replace "8" with "e" | Replaces every 8 with e. |
| `mod-4542` | Replace "8" with "f" | Replaces every 8 with f. |
| `mod-4543` | Replace "8" with "g" | Replaces every 8 with g. |
| `mod-4544` | Replace "8" with "h" | Replaces every 8 with h. |
| `mod-4545` | Replace "8" with "i" | Replaces every 8 with i. |
| `mod-4546` | Replace "8" with "j" | Replaces every 8 with j. |
| `mod-4547` | Replace "8" with "k" | Replaces every 8 with k. |
| `mod-4548` | Replace "8" with "l" | Replaces every 8 with l. |
| `mod-4549` | Replace "8" with "m" | Replaces every 8 with m. |
| `mod-4550` | Replace "8" with "n" | Replaces every 8 with n. |
| `mod-4551` | Replace "8" with "o" | Replaces every 8 with o. |
| `mod-4552` | Replace "8" with "p" | Replaces every 8 with p. |
| `mod-4553` | Replace "8" with "q" | Replaces every 8 with q. |
| `mod-4554` | Replace "8" with "r" | Replaces every 8 with r. |
| `mod-4555` | Replace "8" with "s" | Replaces every 8 with s. |
| `mod-4556` | Replace "8" with "t" | Replaces every 8 with t. |
| `mod-4557` | Replace "8" with "u" | Replaces every 8 with u. |
| `mod-4558` | Replace "8" with "v" | Replaces every 8 with v. |
| `mod-4559` | Replace "8" with "w" | Replaces every 8 with w. |
| `mod-4560` | Replace "8" with "x" | Replaces every 8 with x. |
| `mod-4561` | Replace "8" with "y" | Replaces every 8 with y. |
| `mod-4562` | Replace "8" with "z" | Replaces every 8 with z. |
| `mod-4563` | Replace "8" with "A" | Replaces every 8 with A. |
| `mod-4564` | Replace "8" with "B" | Replaces every 8 with B. |
| `mod-4565` | Replace "8" with "C" | Replaces every 8 with C. |
| `mod-4566` | Replace "8" with "D" | Replaces every 8 with D. |
| `mod-4567` | Replace "8" with "E" | Replaces every 8 with E. |
| `mod-4568` | Replace "8" with "F" | Replaces every 8 with F. |
| `mod-4569` | Replace "8" with "G" | Replaces every 8 with G. |
| `mod-4570` | Replace "8" with "H" | Replaces every 8 with H. |
| `mod-4571` | Replace "8" with "I" | Replaces every 8 with I. |
| `mod-4572` | Replace "8" with "J" | Replaces every 8 with J. |
| `mod-4573` | Replace "8" with "K" | Replaces every 8 with K. |
| `mod-4574` | Replace "8" with "L" | Replaces every 8 with L. |
| `mod-4575` | Replace "8" with "M" | Replaces every 8 with M. |
| `mod-4576` | Replace "8" with "N" | Replaces every 8 with N. |
| `mod-4577` | Replace "8" with "O" | Replaces every 8 with O. |
| `mod-4578` | Replace "8" with "P" | Replaces every 8 with P. |
| `mod-4579` | Replace "8" with "Q" | Replaces every 8 with Q. |
| `mod-4580` | Replace "8" with "R" | Replaces every 8 with R. |
| `mod-4581` | Replace "8" with "S" | Replaces every 8 with S. |
| `mod-4582` | Replace "8" with "T" | Replaces every 8 with T. |
| `mod-4583` | Replace "8" with "U" | Replaces every 8 with U. |
| `mod-4584` | Replace "8" with "V" | Replaces every 8 with V. |
| `mod-4585` | Replace "8" with "W" | Replaces every 8 with W. |
| `mod-4586` | Replace "8" with "X" | Replaces every 8 with X. |
| `mod-4587` | Replace "8" with "Y" | Replaces every 8 with Y. |
| `mod-4588` | Replace "8" with "Z" | Replaces every 8 with Z. |
| `mod-4589` | Replace "8" with "0" | Replaces every 8 with 0. |
| `mod-4590` | Replace "8" with "1" | Replaces every 8 with 1. |
| `mod-4591` | Replace "8" with "2" | Replaces every 8 with 2. |
| `mod-4592` | Replace "8" with "3" | Replaces every 8 with 3. |
| `mod-4593` | Replace "8" with "4" | Replaces every 8 with 4. |
| `mod-4594` | Replace "8" with "5" | Replaces every 8 with 5. |
| `mod-4595` | Replace "8" with "6" | Replaces every 8 with 6. |
| `mod-4596` | Replace "8" with "7" | Replaces every 8 with 7. |
| `mod-4597` | Replace "8" with "9" | Replaces every 8 with 9. |
| `mod-4598` | Replace "9" with "a" | Replaces every 9 with a. |
| `mod-4599` | Replace "9" with "b" | Replaces every 9 with b. |
| `mod-4600` | Replace "9" with "c" | Replaces every 9 with c. |
| `mod-4601` | Replace "9" with "d" | Replaces every 9 with d. |
| `mod-4602` | Replace "9" with "e" | Replaces every 9 with e. |
| `mod-4603` | Replace "9" with "f" | Replaces every 9 with f. |
| `mod-4604` | Replace "9" with "g" | Replaces every 9 with g. |
| `mod-4605` | Replace "9" with "h" | Replaces every 9 with h. |
| `mod-4606` | Replace "9" with "i" | Replaces every 9 with i. |
| `mod-4607` | Replace "9" with "j" | Replaces every 9 with j. |
| `mod-4608` | Replace "9" with "k" | Replaces every 9 with k. |
| `mod-4609` | Replace "9" with "l" | Replaces every 9 with l. |
| `mod-4610` | Replace "9" with "m" | Replaces every 9 with m. |
| `mod-4611` | Replace "9" with "n" | Replaces every 9 with n. |
| `mod-4612` | Replace "9" with "o" | Replaces every 9 with o. |
| `mod-4613` | Replace "9" with "p" | Replaces every 9 with p. |
| `mod-4614` | Replace "9" with "q" | Replaces every 9 with q. |
| `mod-4615` | Replace "9" with "r" | Replaces every 9 with r. |
| `mod-4616` | Replace "9" with "s" | Replaces every 9 with s. |
| `mod-4617` | Replace "9" with "t" | Replaces every 9 with t. |
| `mod-4618` | Replace "9" with "u" | Replaces every 9 with u. |
| `mod-4619` | Replace "9" with "v" | Replaces every 9 with v. |
| `mod-4620` | Replace "9" with "w" | Replaces every 9 with w. |
| `mod-4621` | Replace "9" with "x" | Replaces every 9 with x. |
| `mod-4622` | Replace "9" with "y" | Replaces every 9 with y. |
| `mod-4623` | Replace "9" with "z" | Replaces every 9 with z. |
| `mod-4624` | Replace "9" with "A" | Replaces every 9 with A. |
| `mod-4625` | Replace "9" with "B" | Replaces every 9 with B. |
| `mod-4626` | Replace "9" with "C" | Replaces every 9 with C. |
| `mod-4627` | Replace "9" with "D" | Replaces every 9 with D. |
| `mod-4628` | Replace "9" with "E" | Replaces every 9 with E. |
| `mod-4629` | Replace "9" with "F" | Replaces every 9 with F. |
| `mod-4630` | Replace "9" with "G" | Replaces every 9 with G. |
| `mod-4631` | Replace "9" with "H" | Replaces every 9 with H. |
| `mod-4632` | Replace "9" with "I" | Replaces every 9 with I. |
| `mod-4633` | Replace "9" with "J" | Replaces every 9 with J. |
| `mod-4634` | Replace "9" with "K" | Replaces every 9 with K. |
| `mod-4635` | Replace "9" with "L" | Replaces every 9 with L. |
| `mod-4636` | Replace "9" with "M" | Replaces every 9 with M. |
| `mod-4637` | Replace "9" with "N" | Replaces every 9 with N. |
| `mod-4638` | Replace "9" with "O" | Replaces every 9 with O. |
| `mod-4639` | Replace "9" with "P" | Replaces every 9 with P. |
| `mod-4640` | Replace "9" with "Q" | Replaces every 9 with Q. |
| `mod-4641` | Replace "9" with "R" | Replaces every 9 with R. |
| `mod-4642` | Replace "9" with "S" | Replaces every 9 with S. |
| `mod-4643` | Replace "9" with "T" | Replaces every 9 with T. |
| `mod-4644` | Replace "9" with "U" | Replaces every 9 with U. |
| `mod-4645` | Replace "9" with "V" | Replaces every 9 with V. |
| `mod-4646` | Replace "9" with "W" | Replaces every 9 with W. |
| `mod-4647` | Replace "9" with "X" | Replaces every 9 with X. |
| `mod-4648` | Replace "9" with "Y" | Replaces every 9 with Y. |
| `mod-4649` | Replace "9" with "Z" | Replaces every 9 with Z. |
| `mod-4650` | Replace "9" with "0" | Replaces every 9 with 0. |
| `mod-4651` | Replace "9" with "1" | Replaces every 9 with 1. |
| `mod-4652` | Replace "9" with "2" | Replaces every 9 with 2. |
| `mod-4653` | Replace "9" with "3" | Replaces every 9 with 3. |
| `mod-4654` | Replace "9" with "4" | Replaces every 9 with 4. |
| `mod-4655` | Replace "9" with "5" | Replaces every 9 with 5. |
| `mod-4656` | Replace "9" with "6" | Replaces every 9 with 6. |
| `mod-4657` | Replace "9" with "7" | Replaces every 9 with 7. |
| `mod-4658` | Replace "9" with "8" | Replaces every 9 with 8. |

